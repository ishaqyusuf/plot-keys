# Done

## Purpose
This file records completed work milestones.

## How To Use
- Add concise completion notes with enough context for future readers.

## Done
- Initialized Project Brain for the real-estate SaaS repository
- Scaffolded the Bun + Turbo monorepo with `apps/api`, `apps/dashboard`, `apps/website`, `apps/tenant-site`, and the agreed shared packages
- Added validated starter configs for Next.js, Tailwind CSS, Hono + tRPC, Biome, and shared TypeScript setup
- Chose Supabase Postgres + Drizzle as the database foundation and documented the Better Auth membership model in ADR-001
- Scaffolded the first Drizzle-backed database package and membership-aware API request context
- Expanded the Supabase package with env-safe client factories and tenant storage helpers
- Added app-level `.env.example` documentation and aligned the repo convention for future env files
- Added `.env.example` files to every current app directory for a uniform setup convention
- Added Docker-based local Postgres setup and root database lifecycle scripts
- Generated the initial Drizzle migration for the tenancy foundation tables in `packages/db`
- Added a dedicated Brain design-system foundation document
- Introduced a provider-based database boundary in `packages/db` so app code is no longer architecturally tied to Supabase
- Replaced the platform website shell with a polished SaaS landing page in `apps/website`
- Defined the first canonical UI token contract and starter shared primitives in `packages/ui`
- Implemented the first structured tenant website template and expanded section library in `packages/section-registry`
- Replaced the dashboard placeholder page with an MVP-oriented onboarding and roadmap workspace
- Restructured `packages/db` so Prisma owns schema and migrations while Drizzle remains available for specialist queries
- Added starter dashboard routes for sign-up, sign-in, verify-email, and onboarding to begin the tenant entry flow
- Implemented Prisma-backed tenant signup, verification, onboarding, session cookies, and dashboard route guards
- Added Prisma schema and migration support for onboarding fields and tenant `site_configurations`
- Implemented code-backed platform templates plus tenant draft/publish website builder flow across `apps/dashboard`, `packages/auth`, `packages/section-registry`, and `apps/tenant-site`
- Added subdomain-first signup UX with PlotKeys website and dashboard hostname previews
- Added Prisma `tenant_domains` schema plus onboarding creation of pending `sitefront` and `dashboard` hostname records
- Added dashboard-triggered Vercel tenant-domain sync logic plus hostname-aware public site lookup fallbacking to slug-based preview mode
- Added shared notifications packages with a framework-agnostic core, a React adapter for hooks/providers, and provider wiring across the three web apps
- Extended the notifications core with a typed registry and the first `signup_successful` notification surfaced in the signup-to-onboarding flow
- Reshaped `packages/email` toward the Midday/GND React Email package layout with starter components, defaults, render helper, and a welcome email template
- Moved sign-up, sign-in, and verify-email into `apps/api` tRPC mutations, added a Midday-style dashboard `client.tsx`/`server.tsx` tRPC setup with a same-origin `/api/trpc` route, added `use-zod-form`, and introduced a temporary session bridge route for auth cookie persistence
- Started the Midday-style backend structure by moving auth API contracts into `apps/api/src/schemas` and creating the first reusable Prisma-backed query modules under `packages/db/src/queries`
- Added GND-style channel planning to the notifications core for `email`, `whatsapp`, and `in_app`, introduced the first verification-email template, and changed signup into a verification-first flow that routes through `/verify-email` before session persistence
- Extended signup to capture and store the owner phone number, added Prisma support for `users.phone_number`, and made auth verification notifications WhatsApp-eligible through shared recipient contacts
- Reworked `packages/notifications` toward the GND notification structure with `payload-utils`, a `NotificationService` trigger layer in `services/`, and auth notification triggering migrated onto the new service-oriented API
- Wired the notification services to real channel senders using Resend-compatible email delivery and a new `packages/app-store` WhatsApp client, and documented the required API env vars
- Activated the remaining discussed auth notification types by triggering `auth_email_verified` after verify-email and adding a reusable `onboarding_reminder` notification trigger alongside the existing signup verification flow
- Moved notification definitions into separate files under `packages/notifications/src/types` and replaced the remaining direct dashboard notification creation with `NotificationService` usage so domain notifications now consistently flow through the service layer
- Added tenant subscription-tier support on `Company`, introduced shared plan-entitlement helpers, tagged code-backed templates with Starter/Plus/Pro tiers, restricted onboarding to Starter templates, and enforced builder draft creation through server-side template access checks
- Moved onboarding, builder, and tenant-domain mutation orchestration into API-owned `workspace` tRPC procedures, added dedicated API transport schemas under `apps/api/src/schemas/workspace.schema.ts`, extracted reusable Prisma-backed onboarding/site-configuration/tenant-domain query helpers into `packages/db/src/queries/*`, and reduced dashboard server actions to cookie/redirect wrappers
- Replaced header-based auth scaffolding with Better Auth session verification and adapter wiring: added Better Auth Prisma models (`Session`, `Account`, `Verification`), configured `betterAuth()` with Prisma adapter and email/password support in `packages/auth/src/better-auth.ts`, replaced `resolveSessionFromHeaders` with `getAppSessionFromBetterAuth` in the API context, added the Better Auth route handler in `apps/api/src/index.ts`, updated all auth actions to create Better Auth sessions via `createBetterAuthSession`, and migrated sign-up to create Better Auth `Account` credential records alongside users
- Connected the first website template to live property records: added `Property` and `Agent` Prisma models with company relations, added `listFeaturedProperties` and `listAgentsForCompany` queries in `packages/db/src/queries/`, updated `resolveWebsitePresentation` in the section-registry to accept `liveListings?: LiveListingItem[]`, and updated the tenant-site page to load and pass featured properties to the template renderer
- Added inline hover editing, click-to-focus section editing, and publish confirmation UX in the builder: created `BuilderPreviewPanel` client component with per-section click-to-focus and inline `FieldEditor` sub-components with save and AI-fill actions, created `PublishConfirmationDialog` with name editing before publish, and replaced the static builder preview with the new interactive panel
- Moved Vercel tenant-domain sync from synchronous request-path execution into background jobs with durable exponential-backoff retries: added `runWithRetry` and `runInBackground` queue helpers to `packages/jobs`, extracted `domainSyncHandler` with per-domain error isolation, and updated the `syncTenantDomains` workspace mutation to queue the job instead of blocking the request
- Added resumable onboarding persistence: introduced `TenantOnboarding` Prisma model (`tenant_onboardings` table) with `companyName`, `subdomain`, `market`, `templateKey`, `currentStep`, and `completedAt`; sign-up now calls `upsertTenantOnboarding` immediately after user creation so company identity is durable from the start; the onboarding page loads saved state from the DB and pre-fills market and template (showing "Resume and finish" when in-progress values exist); `completeOnboardingAction` reads DB as source of truth and falls back to cookie for pre-migration accounts; `completeOnboarding` workspace procedure uses the saved record and marks it complete; new `saveOnboardingProgress` and `getOnboardingState` procedures let the dashboard persist partial progress on each step
- Expanded onboarding into a 6-step multi-step flow: extended `TenantOnboarding` Prisma model with Step 1 (businessType, primaryGoal, tagline), Step 2 (locations, propertyTypes, targetAudience), Step 3 (tone, stylePreference, preferredColorHint), Step 4 (phone, contactEmail, whatsapp, officeAddress), and Step 5 (hasLogo, hasListings, hasExistingContent, hasAgents, hasProjects, hasTestimonials, hasBlogContent) fields; rewrote the onboarding page into a URL-param-driven stepped form with per-step server actions (`saveOnboardingStepAction`) that save progress to DB and redirect forward; added a visual checklist side panel showing completed/current/pending steps; extended `saveOnboardingProgressInputSchema` and `saveOnboardingStepProgress` DB helper to accept all new step fields; updated `getOnboardingState` to return the full onboarding record
- Generated and stored onboarding business summary + tenant profile derivation + template recommendation scoring: added `businessSummary`, `segment`, `designIntent`, `conversionFocus`, `complexity`, and `recommendedTemplateKey` fields to `TenantOnboarding`; created `packages/section-registry/src/recommendation.ts` with `buildBusinessSummary()`, `deriveProfile()`, and `scoreTemplates()` pure functions; `saveOnboardingProgress` now re-derives and persists the full profile after every step save; added `getTemplateRecommendations` tRPC query returning scored template list with reason strings; the Launch step now shows templates as scored radio cards with a "Recommended" badge on the best-fit pick and displays the auto-generated business summary as a profile preview
- Added font-fallback resolution in packages/section-registry/src/fonts.ts: resolveFontStack() and resolveHeadingFontStack() map canonical font family names (Satoshi, Inter, Playfair, Georgia, Fraunces, etc.) to full CSS font-family stacks with web-safe and system fallbacks; exported from section-registry index
- Added listTenantDomainsForCompany() DB query returning all non-deleted domain records with status/error/provisionedAt; added getTenantDomainStatus tRPC procedure exposing per-domain provisioning state plus hasFailure/allProvisioned summary flags for dashboard alerting
- Added apps/api/src/routers/forms.route.ts as a public tRPC router (no auth required) with submitContact, submitInquiry, and submitNewsletterSignup procedures that validate input, resolve the company by subdomain, and return a typed receipt — scaffolded with TODO hooks for future Trigger.dev notification jobs; registered as forms namespace in _app.ts
- Added BillingLineItem Prisma model with BillingLineItemKind (subscription/template_purchase/stock_image/domain_addon/ai_credits) and BillingLineItemStatus (pending/active/cancelled/expired) enums; stores amount in minor currency units, currency, providerRef, arbitrary meta JSON, and period/paid/cancelled timestamps; relation added to Company
- Added TenantTemplateLicense Prisma model with TemplateLicenseSource enum (free/plan_included/purchased/admin_granted); added findTemplateLicensesForCompany, findLicensedTemplateKeys, hasTemplateLicense, grantTemplateLicense, revokeTemplateLicense, syncPlanIncludedLicenses DB helpers; added assertTemplateAccessWithLicense helper that checks license table before falling back to plan-tier check; updated createTemplateDraft to use license-aware check; added getTemplateLicenses, claimFreeTemplateLicense, and syncPlanLicenses tRPC procedures; added updateSiteThemeField procedure for per-field theme property updates; added updateSiteConfigurationThemeField DB helper; added getSiteRenderData public tRPC query returning full site render payload (company, template, content, theme, status) by subdomain
- Added template metadata (marketingTagline, purchasable, previewImageUrl) to TemplateDefinition; renamed template display names to Aster Grove, Atlas Urban, Palmstone; added countCompaniesByTemplateKey() DB query using Prisma groupBy; added getTemplateCatalog tRPC query returning enriched template list with live usage counts; added SubmitButton primitive to packages/ui using useFormStatus with spinner and loadingLabel prop, exported as @plotkeys/ui/submit-button; replaced smart-fill placeholder with Claude Haiku 4.5-backed generateFieldContent() in apps/api/src/lib.ai.ts that builds a prompt from company name, market, businessSummary, shortDetail, longDetail, and preferredLength — falls back to deterministic copy when ANTHROPIC_API_KEY is absent; extended smartFillFieldInputSchema to include longDetail and preferredLength; smartFillField procedure fetches onboarding context to enrich the AI prompt
- Expanded recommendation engine and onboarding-driven site bootstrap: added `upgradeRequired` + `fallbackKey` fields to `TemplateRecommendation` so the UI can show upgrade prompts; added `deriveDesignConfig()` mapping profile → font+color system presets (editorial/bold/warm/clean); added `derivePageComposition()` returning section order + leadWith strategy; added `deriveSectionVisibility()` returning per-module boolean flags from content-readiness answers; added `derivePersonalizedContent()` generating segment+tone-aware hero/story/CTA copy without any AI call; updated `completeOnboarding` to build the first draft with personalized content and derived design rather than template defaults; added `refreshOnboardingProfile` tRPC mutation for rerunning derivation on demand; `getTemplateRecommendations` now respects plan-tier access and surfaces accessible fallback when the top pick requires upgrade
- Wired live property records, expanded template catalog, and added seed script: connected `getSiteRenderData` to `listFeaturedProperties` for live listing data; expanded code-backed template catalog from 3 to 6 (Meridian/plus/clean, Thornfield/pro/bold, Crestview/pro/warm) with scoring tags in recommendation engine; added `seed:templates` script in packages/db for seeding plan-included licenses across all companies
- Added Next.js middleware-based hostname resolution: dashboard middleware extracts tenant slug from `dashboard.{slug}.plotkeys.com` and injects `x-tenant-subdomain` header; tenant-site middleware resolves slug from `{slug}.plotkeys.com` or custom domain and injects both `x-tenant-subdomain` and `x-tenant-hostname`; tenant-site page now prefers middleware headers over query params; added `getTenantSlugFromHost()` helper in dashboard session.ts; dashboard middleware gates non-public routes behind session cookie presence check
- Implemented app-wide dark mode via next-themes: added ThemeProvider (wrapping NextThemesProvider) and ThemeToggle components in packages/ui; exported as `@plotkeys/ui/theme-provider` and `@plotkeys/ui/theme-toggle`; wired ThemeProvider into dashboard root layout with class-based dark mode and system default; existing `.dark` CSS token block in globals.css powers the token swap
- Added upgrade/downgrade lifecycle handling: added `updateCompanyPlan(db, companyId, planTier, planStatus)` DB helper; added `changePlanInputSchema` with planTier/planStatus/providerRef; added `changePlan` tRPC mutation that updates Company plan, records a billing line item for the providerRef, and syncs plan-included template licenses to match the new tier
- Added tenant logo asset storage: added `logoUrl` nullable field to Company Prisma model; added `updateCompanyLogo(db, companyId, logoUrl)` DB helper; added `setCompanyLogo` tRPC mutation accepting a URL or null — callers upload to storage and provide the public URL; AI-assisted generation hooks into this same procedure after writing the generated image
- Added mode-aware draft rendering: added `RenderMode` type (`"live" | "draft" | "preview"`) to section-registry; added `renderMode` field to `ResolveTemplateOptions` and `ResolvedWebsitePresentation`; added `isContentFieldEmpty()` and `draftPlaceholderClass()` utilities for section components to use when highlighting unfilled fields in draft mode; builder page now passes `renderMode: "draft"` and tenant-site page passes `renderMode: "live"`
- Defined page inventory and section-matrix overrides: added `page-inventory.ts` to section-registry with `SectionSlot`, `PageDefinition`, and `TemplatePageInventory` types; defined base home sections (hero, market-stats, featured-listings, story, testimonials, cta) and per-template overrides for templates 1-6 (e.g. Meridian leads with listings, Atlas Urban enables testimonials); exported `getTemplatePageInventory()`, `getEnabledSections()`, and `collectContentKeys()` helpers
- Designed Website/WebsiteVersion migration path: added `Website` (one per company, owns publishing lifecycle) and `WebsiteVersion` (immutable snapshots with draft/published/archived status, version number, legacyConfigId for migration tracing) Prisma models; added relation from Company and User; documented 4-phase migration from SiteConfiguration (dual-write → cutover → cleanup) in model file comments; `SiteConfiguration` remains authoritative in Phase 1
- Evolved editable strings to structured content nodes: added `content-nodes.ts` with `ContentNode` type (key, kind, provenance, value, aiContext, editedAt, richTextDelta); `ContentNodeKind` enum (heading/label/paragraph/rich_text/url/cta); `ContentNodeProvenance` enum (human/ai_generated/ai_assisted/template_default/imported); helper functions `liftFlatContent()`, `flattenContentNodes()`, `applyHumanEdit()`, `applyAiGeneration()`; exported from section-registry index; backward compatible with flat contentJson via flatten/lift
- Defined Trigger.dev job boundaries: added `plans.sync` handler (updateCompanyPlan + syncPlanIncludedLicenses + billing line item); added `website.content.generate` handler (AI field-by-field generation for site configurations skipping human-edited fields); added `notifications.dispatch` handler with contact_form/property_inquiry/newsletter_signup cases; registered all three in jobIds map; handlers use dynamic imports for DB/AI clients to support serverless environments
- Defined exact pricing amounts and billing intervals: added `packages/utils/src/pricing.ts` with Starter (free), Plus (₦15k/mo or ₦144k/yr), Pro (₦35k/mo or ₦336k/yr) plan prices in NGN kobo; plus template purchase (₦25k), stock image (₦2.5k), domain addon (₦10k/yr), AI credits block (₦5k/100 credits); documented upgrade/downgrade/cancellation/past-due rules as `billingRules` constant; exported from `@plotkeys/utils`
- Added controlled template config storage: added `template-config.ts` in section-registry with typed `TemplateConfig` (accentColor/backgroundColor/fontFamily/headingFontFamily/logo/market/stylePreset/supportLine/namedImages); `StylePreset` enum (bold/clean/editorial/warm); `stylePresets` map with full color+font presets for each; `serializeTemplateConfig`/`deserializeTemplateConfig` for themeJson round-trips; `fromDerivedDesignConfig()` adapts recommendation output; `applyConfigUpdate()` merges named image assignments; `updateSiteConfigurationTheme()` DB helper for full theme replacement
- Added WebsiteVersion persistence: added `packages/db/src/queries/website.ts` with `findWebsiteForCompany`, `upsertWebsite`, `findPublishedWebsiteVersion`, `getOrCreateDraftVersion` (auto-increment version number), `updateDraftVersion`, and `publishWebsiteVersion` (archives previous published, promotes draft, updates Website.publishedVersionId in a transaction); exported from db package index
- Added runtime host resolution from tenant_domains: added `resolveTenantByHostname(db, hostname)` DB query that looks up active domain records and returns companyId/companySlug/hostname; updated tenant-site page to use this query as the primary resolution path before falling back to slug-based lookup for dev/preview mode
- Added stock image catalog: added `stock-images.ts` in section-registry with `StockImage`, `StockImageCategory`, `StockImageLicenseTier` types; 7 placeholder catalog entries (3 free, 4 standard-licensed); `getFreeStockImages`, `getStockImageById`, `getStockImagesByCategory`, `getStockImagesForSlot` helpers; documented licensing flow (preview watermark → purchase → BillingLineItem → full URL); exported from section-registry index
- Renamed packages/supabase to @plotkeys/platform-integrations: updated package.json name and added sub-path exports (./storage, ./supabase); added provider-agnostic `StorageProvider` interface with upload/getPublicUrl/delete; added `buildTenantStoragePath` and `storageBuckets` constants (logos/stock-images/tenant-assets); Supabase implementation moved to ./supabase.ts; index re-exports both modules
- Fixed templateCatalog array bug (templates 4-6 were orphaned outside the array); expanded catalog from 6 to 13 templates: 5 starters (Aster Grove, Vega Lite, Nova Basic, Lyra Basic, Myra Basic), 4 plus (Atlas Urban, Meridian, Maia Growth, Horizon Plus), 4 pro (Palmstone, Thornfield, Crestview, Nova Pro); each template now defines namedImageSlots (heroImage, aboutImage, ctaBackground) with default Unsplash URLs; added namedImageSlots field to TemplateDefinition type
- Updated StylePresets from 4 generic names (bold/clean/editorial/warm) to 5 brain-defined named presets: Vega/Nova/Maia/Myra/Lyra; each preset now has density (compact/comfortable/spacious) and radius (none/sm/md/lg/full) fields aligned with the Template Config Mode brain doc; added StylePresetDefinition type
- Added full color system support: ColorSystem type with light+dark ColorTokenSet (20 shadcn tokens each); 3 initial named systems (slate/ocean/forest); colorSystem field added to TemplateConfig; colorSystems map exported from section-registry
- Added font fallback resolution: fontFallbacks map per selected font with per-slot overrides (subscribeButton, badge, eyebrow); resolveSlotFont() helper; FontFallbackMap type; exported from section-registry index
- Added resolvePresetConfig() helper that merges a style preset with tenant overrides; exported from section-registry index
- Added tenant runtime React context: created runtime-context.tsx with WebsiteRuntimeProvider, useRenderMode, useIsDraftMode, useTemplateConfig, useColorSystem, useTemplateStylePreset, useResolvedFont, useTemplateImage hooks; WebsiteRuntimeContextValue shape wires RenderMode + TemplateConfig + ColorSystem + StylePresetDefinition; exported from section-registry index
- Extracted RenderMode to types.ts to break circular import between index.ts and section components; updated all section files and runtime-context.tsx to import from types.ts
- Added extended section components in sections/extended-sections.tsx: PropertyGridSection (live listing grid with skeleton state), AgentShowcaseSection (agent avatar grid), ContactSection (contact form + map placeholder), FAQAccordionSection (expand/collapse), NewsletterSection (email subscribe strip); all accept config + theme props matching brain sections-inventory patterns; exported all types and components from section-registry index
- Added section-utils.ts with draftPlaceholderClass, draftEditableClass, isContentFieldEmpty shared utilities; wired renderMode prop into HeroBannerSection, StoryGridSection, and CtaBandSection so empty fields show amber dashed outlines and editable sections show blue hover ring in draft mode
- Added form action registry in form-registry.ts: FormActionKind, FormAction, SectionFormBinding types; sectionFormBindings default map (contact_section, property_inquiry_form, newsletter_section, seller_form, buyer_form); getFormAction(), isSectionFormBound(), getFormProcedurePath() helpers; maps section types to forms.* tRPC procedures; exported from section-registry index

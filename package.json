{
  "name": "plotkeys",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "brand:logos": "node ./scripts/generate-plotkeys-logo-assets.mjs",
    "build": "bun scripts/with-workspace-env.mjs --mode production -- turbo build",
    "db:down": "docker compose down",
    "db:generate": "bun scripts/with-workspace-env.mjs --mode local -- bun run --cwd packages/db db:generate",
    "db:logs": "docker compose logs -f postgres",
    "db:migrate": "bun run db:start && bun scripts/with-workspace-env.mjs --mode local -- bun run --cwd packages/db db:migrate",
    "db:migrate:deploy": "bun scripts/with-workspace-env.mjs --mode production -- bun run --cwd packages/db db:migrate:deploy",
    "db:push": "bun scripts/with-workspace-env.mjs --mode production -- bun run --cwd packages/db db:push",
    "db:start": "bun scripts/with-workspace-env.mjs --mode local -- sh ./scripts/start-dev-db.sh",
    "db:studio": "bun run db:start && bun scripts/with-workspace-env.mjs --mode local -- bun run --cwd packages/db db:studio",
    "db:drizzle:studio": "bun run db:start && bun scripts/with-workspace-env.mjs --mode local -- bun run --cwd packages/db db:drizzle:studio",
    "db:up": "bun run db:start",
    "email:test": "bun scripts/with-workspace-env.mjs --mode local -- bun scripts/send-test-email.mjs",
    "kill:ports": "bun scripts/with-workspace-env.mjs --mode local -- sh ./scripts/kill-port.sh",
    "dev:prepare": "bun run kill:ports && bun run db:start",
    "dev": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev --parallel",
    "dev:api": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev --filter=@plotkeys/api",
    "dev:api:portless": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev:portless --filter=@plotkeys/api",
    "dev:dashboard": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev --filter=@plotkeys/dashboard",
    "dev:dashboard:portless": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev:portless --filter=@plotkeys/dashboard",
    "dev:tenant-site": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev --filter=@plotkeys/tenant-site",
    "dev:tenant-site:portless": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev:portless --filter=@plotkeys/tenant-site",
    "dev:portless": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev:portless --parallel",
    "dev:website": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev --filter=@plotkeys/website",
    "dev:website:portless": "bun run dev:prepare && bun scripts/with-workspace-env.mjs --mode local -- turbo dev:portless --filter=@plotkeys/website",
    "lint": "turbo lint",
    "format": "biome format --write .",
    "terminal": "bash ./scripts/open-bun-script-terminal.sh",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@trigger.dev/build": "3.3.14",
    "png-to-ico": "^3.0.1",
    "sharp": "^0.34.5",
    "turbo": "^2.5.0",
    "typescript": "^5.9.0"
  }
}

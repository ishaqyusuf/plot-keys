# In Progress

## Purpose
This file tracks work currently being executed.

## How To Use
- Keep this list short and current.
- Move completed work to `done.md`.

## In Progress

### Dashboard UI + Structure Migration
- Priority: High
- Description: Migrate dashboard pages one at a time to the approved Midday-inspired structure, using `/Users/M1PRO/Documents/code/_kitchen_sink/midday/apps/dashboard/src/app/[locale]/(app)/(sidebar)/invoices/page.tsx` and its table modules as the current page reference.
  - Related Feature: Dashboard Feature Plan; Design System
  - Status: In Progress
  - Current Page: Remaining Dashboard Parity Audit
  - Migration Notes:
  - The full source-level page conformance audit is recorded in `.brain/tasks/dashboard-midday-conformance.md`. Every authenticated, settings, builder, auth/onboarding, invite, and sandbox route is mapped to its closest Midday pattern, with shared table, URL-state, sheet, modal, settings, and app-shell ownership verified from source. QA maintenance was rebuilt with a platform-admin route guard, metadata, paired prefetch/Suspense queries, hydration, error recovery, feature header, and stable skeleton. Onboarding is now a thin compositional route over `components/onboarding/onboarding-page.tsx`, and its five steps are split into Midday-style modules. F6 API conformance is complete: Overview uses Midday's exact `overview.summary` ownership, Builder/Website uses dedicated Website and Templates routers, Stock Images owns its entitlement lifecycle, and the generic Workspace router has been removed. F7 package checks and authenticated browser QA remain.
  - Billing checkout, payment confirmation, Paystack webhooks, and dashboard tenant-host resolution now follow Midday's API-owned boundaries. `billing.getInfo`, `billing.initializeCheckout`, and `billing.confirmCheckout` own protected billing behavior; the API REST surface owns Paystack event processing and public dashboard tenant-state lookup, while the existing dashboard webhook URL remains a thin compatibility bridge. Billing and tenant-domain query helpers now require an injected `Db`, proxy/session resolve tenant state through the public API, focused billing/tenant-scope tests pass, and the dashboard has zero runtime `@plotkeys/db` imports with only the live-preview type contract remaining.
  - Dashboard company-app state and notification-bell data now follow Midday's protected tRPC ownership boundary instead of dashboard server helpers importing self-connecting DB queries. The app layout resolves enabled apps through `apps.get`, prefetches and hydrates `notifications.bell`, and the client bell owns that query directly; app toggles and all notification read mutations invalidate the matching cache keys. Company-app and bell DB helpers now require an injected `Db`, unused self-connecting install/uninstall and dashboard notification wrappers were removed, and tenant/user scope is covered by focused tests.
  - Onboarding, invite join, invite profile completion, and onboarding-logo upload now follow Midday's dashboard-to-tRPC-to-query boundary instead of importing self-connecting DB helpers into dashboard routes. Onboarding reads `onboarding.get`, join reads the safe public invite contract, profile completion reads through an authenticated team procedure, and invite acceptance/profile mutation use `authenticatedProcedure` rather than public procedures with hand-written session checks. Team invite API inputs now live in `apps/api/src/schemas/team.schema.ts`; DB invite/profile helpers accept `Db` explicitly; API responses expose only the page fields needed; and unused self-connecting onboarding/team convenience wrappers were removed. Focused invite email/company-scope tests pass, the four migrated dashboard callers have no runtime DB-query imports, and onboarding/team query modules have no internal Prisma-client creation.
  - Builder workspace, live preview, and dashboard-home draft bootstrap now follow Midday's dashboard-to-tRPC-to-query boundary instead of calling `@plotkeys/db/queries` from dashboard runtime components. Workspace procedures derive company/user scope from `membershipProcedure`, the membership middleware exposes its verified database handle as non-null context, website helpers accept `Db` explicitly, and live-preview hostname/subdomain lookup is constrained to the active company. Draft creation and legacy/published configuration migration now belong only to `ensureBuilderConfigurationExists`; `getBuilderWorkspace` and `getLivePreview` are read-only queries. Focused website tenant-scope tests pass, API/dashboard package typechecks pass, and the direct dashboard website-query/runtime-client scans are clean.
  - API routers now follow Midday's explicit database import boundary throughout: auth, projects, QA maintenance, template sandbox, and the remaining workspace procedures import reusable operations from `@plotkeys/db/queries`, while `@plotkeys/db` is limited to Prisma client/runtime construction, `Db`, and the shared membership schema type. The query barrel now exports QA maintenance helpers, preserving all existing router schemas, authorization, mutations, jobs, and response contracts while removing the remaining API query imports from the broad DB root.
  - Dashboard app-shell composition now matches Midday's sidebar layout boundary more exactly: the authenticated app root uses a relative shell, the non-builder content column uses `md:ml-[70px] pb-4`, every non-builder page receives `px-4 md:px-8` from shared dashboard chrome, and `GlobalSheetsProvider` is a sibling of the shifted content column while remaining inside the shared `SiteNav` context. Builder routes remain intentionally full-bleed.
  - Remaining QA maintenance, blog lifecycle, and template-profile direct mutations now use the shared Midday-style `SubmitButton`: QA adoption/purge keep their selection, confirmation-token, and blocker guards while the primitive owns pending feedback; blog publish/draft/archive derives the active target status from mutation variables; template clone/archive derives the active profile id. This preserves navigation buttons, destructive variants, status payloads, query invalidation, redirects/refreshes, and stable labels while clearing the dashboard plain-button mutation-action scan.
  - Property media and project detail row actions now follow Midday's direct mutation-action contract: cover selection, media deletion, phase transitions, milestone visibility/completion, update visibility, issue resolution, worker activation/removal, payroll-run transitions, and budget-line removal render through the shared `SubmitButton`. TanStack mutation variables identify the active record so only that row owns the spinner and stable label while the operation-level pending flag disables sibling controls, preserving existing mutation payloads, cache invalidation, errors, table/list composition, and domain status behavior.
  - Notifications header bulk-read action now follows the shared Midday-style `SubmitButton` pending-action pattern: `NotificationsActions` uses `SubmitButton` for `Mark all read`, lets the shared primitive own pending disabling and spinner/stable-label rendering through `markAllReadMutation.isPending`, and no longer carries local plain-button pending wiring. This preserves unread-count gating, column visibility placement, notification list/unread-count invalidation, and the protected notifications route redirect behavior while reducing remaining dashboard pending-action residue.
  - Pending invite revoke buttons now use the shared Midday-style `SubmitButton` pending-action primitive across Agent, Employee, and Team pending invite lists. The active row is still derived from `revokeInviteMutation.isPending` plus mutation variables, while the shared button now owns pending disabling, spinner placement, and stable `Revoke` copy. This preserves revoke error handling, invite-list invalidation, development invite links, role/work-role badges, and table-like invite shells while clearing the remaining plain-button pending-action residue in those invite rows.
  - Pending invite revoke rows now follow Midday-style mutation-owned pending state more closely: Agent, Employee, and Team pending invite lists no longer mirror the active invite id in separate React state and instead derive row disabling from `revokeInviteMutation.isPending` plus the mutation variables. This preserves per-row revoke disabling, revoke error clearing, invite-list invalidation, development invite links, role/work-role badges, and table-like invite shells while reducing duplicated local state around TanStack mutations.
  - Dashboard-wide `SubmitButton` call-shape residue is now cleared outside the dedicated form folder as well: project inline forms, project customer-access/team actions, template sandbox generation, auth sign-in/sign-up, invite signup/profile completion, and property media add forms no longer pass redundant `type="submit"` to the shared pending-action button. Explicit non-submit buttons keep their `type="button"` guards, and the full dashboard TSX scan now has no remaining `type="submit"` matches while preserving native form submission, mutation pending spinners, disabled guards, project/detail mutations, invite flows, template sandbox generation, auth redirects, and property media upload behavior.
  - Publish confirmation modal pending state now follows the Midday-style TanStack mutation ownership pattern: `PublishConfirmationDialog` no longer shadows `publishSiteConfiguration` with separate local pending state, and the action footer reads `publishMutation.isPending` directly. This preserves confirmation form validation, error reset on close, builder URL replacement, router refresh, stable `SubmitButton` copy/spinner behavior, quick fill, disabled upgrade state, and the publish summary/live-note modal split while reducing modal-local state residue.
  - Remaining dashboard mutation forms now match the Midday-style `SubmitButton` submit-action call surface more closely: the last `type="submit"` props were removed from `SubmitButton` usages under `apps/dashboard/src/components/forms`, while explicit cancel/action `type="button"` guards remain. This preserves native form submission, pending spinner ownership, disabled guards, quick-fill placement, create/edit/cancel behavior, upload readiness checks, invite submissions, payroll/leave/project/budget mutations, customer/property/agent forms, and domain connection flow without carrying redundant submit-button prop noise.
  - Estate launch edit sheet data behavior now follows the same Midday-style cached list-row edit flow as the migrated agent, customer, and property sheets: `EstateLaunchDetailsSheet` reads the cached estate list row by slug and renders the editable launch form while the full detail query loads, then remounts with authoritative detail data once available. The estate launch form contract is narrowed to the editable fields it actually consumes so list rows and detail rows can both seed the form safely, while preserving URL-backed sheet open/close behavior, detail query ownership, skeleton fallback when no cached row exists, list/detail invalidation, upload fields, quick fill, and publish-state editing.
  - Property edit sheet data behavior now follows the same Midday-style cached table-row edit flow as agents and customers: `PropertyEditSheet` reads the cached properties table pages and renders a valid edit form from the selected row while the full property detail query loads, then remounts the form with authoritative detail data once available. This preserves URL-backed edit-sheet opening, detail query ownership, skeleton fallback when no cached row exists, optional payment-plan/detail fields, form success close behavior, and property list/detail invalidation while reducing blank-sheet reloads from the properties table action menu.
  - Customer edit sheet data behavior now follows Midday's cached-row edit-sheet pattern more closely: `CustomerEditSheet` reads the cached customer table pages and renders matching editable row values immediately while the detail query loads, then remounts the form with authoritative detail data once available. This preserves company-scoped `getById` fetching, skeleton fallback when no cached row exists, delete invalidations, URL sheet close behavior, and customer form context ownership while reducing blank-sheet reloads from the migrated table-to-sheet flow.
  - Dashboard SubmitButton pending-disable call shapes now match Midday's shared pending-action pattern more closely across remaining form/auth/modal/template-sandbox, billing, onboarding, builder, project, property media, blog, and AI-credit submit actions: pending-only `disabled={...isPending}` / `disabled={pending}` duplication was removed from submit controls while real non-pending guards such as missing token, missing employees, missing source URL, read-only mode, parent disabled props, or disabled provisioning remain. `SubmitButton` continues to own pending disabling through `isSubmitting`, preserving stable labels, spinner state, quick-fill flows, domain provisioning guards, estate-plan upload readiness, template sandbox create behavior, checkout pending protection, builder read-only protection, and project/detail create actions.
  - Dashboard non-table-core private helper inputs now match Midday's local/scoped naming boundary more closely: `QuickFill`, development quick-fill, horizontal pagination, and builder preview routing use local `Props` or scoped `*Input` aliases for private component/hook inputs, while shared table-core `*Props` contracts remain intentionally unchanged because they match Midday's own table core reference naming. This preserves quick-fill behavior, dev presets, horizontal scroll controls, and builder preview page-link routing.
  - Dashboard private helper prop boundaries now match Midday's local/scoped naming convention more closely across project budget/workforce helpers, connected-domain cards, onboarding side-panel, header tabs, builder sidebar/workspace notice helpers, search-filter menu helpers, and template-sandbox floating/workbench controls. Private helper types now use local `Props` or scoped `*Input` aliases instead of exported-sounding `*Props` names while preserving project budget/workforce rendering, connected-domain links, onboarding checklist styling, header-tab classes, builder configuration controls, search filter behavior, and template-sandbox mutations.
  - Tenant-site public chat route now shares the Midday-style query-layer context boundary with the API chat router: `apps/tenant-site/src/app/api/chat/route.ts` uses `getTenantChatContext` from `@plotkeys/db/queries` for company, featured-property, agent, and onboarding-summary data, leaving the handler focused on HTTP validation, message normalization, AI chat orchestration, and response/status mapping.
  - Tenant-site public contact and analytics route handlers now follow the Midday query-layer boundary more closely: `apps/tenant-site/src/app/api/contact/route.ts` and `apps/tenant-site/src/app/api/track/route.ts` import company lookup plus lead/analytics write helpers from `@plotkeys/db/queries`, while keeping HTTP validation, status-code mapping, visitor fingerprinting, and notification job dispatch in the route handlers.
  - Dev tenant launcher API now follows the Midday query-layer boundary more closely: `apps/api/src/routers/dev.route.ts` imports `listActiveCompaniesForDevLauncher` from `@plotkeys/db/queries`, while `packages/db/src/queries/company.ts` owns the active-company selection and ordering. The dev router keeps the production guard, DB availability fallback, and launcher response mapping without inline company Prisma access.
  - Stock-image purchase billing now follows a dedicated `stockImages` API boundary: image validation, tenant context, license checks, billing-line creation, grant orchestration, and response shaping no longer share a generic feature router.
  - Lead status and conversion contracts now live in the dedicated `leads` router, while `packages/db/src/queries/leads.ts` owns company-scoped single status updates, bulk status updates, and lead-to-customer conversion.
  - Payroll creation now lives in the dedicated `payroll` router, while `packages/db/src/queries/payroll.ts` owns employee-scoped payroll entry creation through `createPayrollEntryForCompany`.
  - Leave-request mutations now live in the dedicated `leaveRequests` router, while `packages/db/src/queries/leave-request.ts` owns employee-scoped creation and company-scoped single/bulk status updates.
  - Team API membership/company lookup now follows the Midday query-layer boundary more closely: `apps/api/src/routers/team.route.ts` imports team/company/auth query helpers from `@plotkeys/db/queries`, delegates duplicate-member checks, owner/self removal target reads, bulk removal target reads, and invite plan company lookup to `packages/db/src/queries/team.ts` / company queries, and keeps the router focused on role assertions, expected `TRPCError` mapping, notifications, and mutation orchestration.
  - Public forms API tenant lookup now follows the Midday query-layer boundary more closely: `apps/api/src/routers/forms.route.ts` imports `findCompanyBySlug` from `@plotkeys/db/queries` instead of keeping an inline `db.company.findFirst` in the public router, preserving contact, property-inquiry, newsletter, not-found, and notification-dispatch behavior while leaving the router focused on input validation and job orchestration.
  - Tenant-site chat API context loading now follows the Midday query-layer boundary more closely: `apps/api/src/routers/chat.route.ts` delegates tenant/company, featured-property, agent, and onboarding-summary lookup to `packages/db/src/queries/chat.ts` via `@plotkeys/db/queries`, leaving the router focused on public input validation, not-found errors, chat-message mapping, and `@plotkeys/chat-bot` orchestration. This preserves public subdomain lookup, context fallback behavior, AI prompt shape, and unavailable-chat error handling.
  - API asset-service imports now follow the Midday query/runtime split more closely: reusable asset/property query helpers and the `AssetOriginKindValue` contract come from `@plotkeys/db/queries`, while Prisma runtime creation and `Db` typing stay on the DB package boundary. This preserves upload/import validation, storage writes, asset status updates, provider moves, display URL resolution, and tenant property ownership checks.
  - Property media and public image API routers now follow the Midday query boundary more closely by importing media/property helpers from `@plotkeys/db/queries`; public image import also reuses `getPropertyForCompany` and `syncPropertyCoverImageUrl` instead of keeping inline property lookup/update logic in the router, preserving asset import, provider attribution, media creation, cover selection, and tenant ownership checks.
  - Customers and Notifications API routers now import reusable query helpers from the explicit `@plotkeys/db/queries` barrel instead of the package root, matching Midday's `@midday/db/queries` API boundary while preserving the existing customer list/detail/stats/mutation behavior and notification list/read/preference procedures.
  - Live Preview route/content ownership now matches the guarded Midday-style route boundary more tightly: `/live` keeps `requireOnboardedSession()` at the page entrypoint, passes the active company slug into `LivePreviewContent`, and the content component now owns only live-preview data loading and view composition. This preserves hostname/subdomain preview params, Suspense/error shell, live-preview skeleton, tenant hostname extraction, and preview rendering while removing the remaining hidden session guard from active dashboard feature content.
  - Connect Domain and Builder Preview route entrypoints now own the explicit onboarded-session guard before composing their feature content, matching the guarded Midday-style route boundary already applied to Live Preview, App Store, table pages, and detail pages; `ConnectDomainContent` is now focused on provisioning readiness and view composition while preserving its Suspense/error shell, Vercel readiness flag, builder preview skeleton, and full-screen preview behavior.
  - App Store and Integrations card helper prop boundaries now follow the same Midday-style private/scoped input convention as the migrated report tables: helper-only logo/action cells use `AppLogoInput`, `IntegrationLogoInput`, and `IntegrationActionsCellInput` instead of generic `LogoProps` / `ActionsProps`, while preserving app availability, integration status, installed badges, configure/connect routing, docs links, and card rendering.
  - Dashboard global-search shell chrome now keeps Midday's command/search layout while moving the remaining hardcoded dark modal surface color to the semantic `background` token with the same near-opaque dark treatment; this preserves command search routing, keyboard close behavior, footer controls, blur shell, borders, and logo/footer composition while reducing dashboard token drift.
  - Report table helper prop ownership now follows the Midday-style local/scoped input boundary more closely: `AgentPerformanceReportTable`, `ListingsPerformanceReportTable`, and `ReportTableFrame` use scoped private `*Input` aliases instead of generic `AgentProps` / `ListingsProps` / `FrameProps`, while preserving report row typing, semantic table tokens, horizontal overflow framing, and report table rendering.
  - Shared table empty-state source now reads closer to Midday's compact table empty-state references: the core `EmptyState` / `NoResults` primitive keeps its local private prop interfaces, optional custom action slot, outline clear action, no-results copy, and every domain table consumer while removing local narration comments from the helper contract.
  - Domain table header source shape now matches the cleaned shared table-header factory style across active migrated tables: each factory `primaryColumn` config is expanded into an explicit `id` / `label` / `sortField` object layout while preserving table ids, domain sort fields, and shared `createCoreDataTableHeader` ownership.
  - Property form listing-type copy now removes the remaining dashboard-facing `(legacy)` labels from commercial, industrial, and mixed-use options while preserving the existing stored enum values, create/edit defaults, validation schema, mutation payloads, and property sheet form ownership.
  - Shared table bulk action pending button now matches Midday's invoice/vault bottom-bar `SubmitButton` call surface more closely: `BulkClientAction` keeps `isSubmitting`, `onClick`, `disabled`, `variant`, and small sizing but no longer passes a local `type="button"` prop, preserving notification, lead, leave-request, and payroll bulk action behavior.
  - Domain table empty-state create actions now match Midday's outline create-button call shape more closely: customer, project, and property empty-state actions pass `variant="outline"` before their URL-param open handlers and no longer carry local `type="button"` noise, while preserving permission gating, status-filter suppression, create-sheet params, and empty-state copy.
  - Shared table empty-state prop ownership now matches Midday's private core helper boundary more closely: `EmptyStateProps` and `NoResultsProps` are local interfaces instead of exported surfaces, while preserving Plot Keys' optional custom action slot for permission-gated create actions, no-results clearing, semantic muted description text, and every domain table empty-state consumer.
  - Shared table helper prop boundaries now match the exact Midday table primitive references more closely: `DraggableHeader` and table-core `BottomBar` use the reference `DraggableHeaderProps` / `BottomBarProps` interface names while preserving Plot Keys' shared icon namespace, DnD header behavior, bottom-bar portal/motion layout, selected-count copy, deselect action, and injected bulk-action slot.
  - Shared date-range filter prop ownership now matches the active Midday-style local component contract used by the migrated search/filter helpers: `DateRangeFilter` uses a local `Props` alias instead of the exported-sounding `DateRangeFilterProps` interface while preserving preset selection, calendar range selection, ISO date serialization, fixed week-start behavior, and every table filter consumer.
  - Customer table API pagination now matches the migrated Midday-style infinite-list contract: `customers.route.ts` accepts string or numeric cursors like the other active list routers and the shared DB list cursor normalizer, preserving customer filters, sorting, stats invalidation, route/table prefetching, and the `getNextPageParam: ({ meta }) => meta?.cursor` dashboard table flow.
  - Settings general route composition now matches Midday's `settings/page.tsx` analogue more closely: the server page owns only the onboarding/session guard, role permission derivation, settings prefetch, hydration boundary, and direct card-section composition, while `settings-sections.tsx` owns the feature-level company/profile/workspace/branding/danger-zone card grouping. This preserves existing settings mutations, logo upload, read-only permission behavior, and workspace danger-zone gating while removing the over-broad route ErrorBoundary/Suspense/skeleton shell from the prior parity pass.
  - Shared template editing/footer class composition now matches the Midday utility boundary more closely: `EditableText`, `EditableImage`, and the tenant `RegisterFooter` use the shared `cn` helper instead of local array/filter/join class assembly, and `RegisterFooter` now uses a local `Props` alias while preserving draft inline editing affordances, image replacement wrapping, scoped footer links, footer group spacing, and tenant-site template rendering.
  - Project overview section ownership now follows the Midday-style feature composition boundary more closely: the large `ProjectOverviewSections` composer moved out of `project-detail-content.tsx` into dedicated `project-overview-sections.tsx` with a local `Props` type, leaving `ProjectDetailContent` focused on project detail query loading, not-found recovery, header/stats composition, overview section mounting, and AI insights placement while preserving every phase, milestone, update, issue, team, budget, workforce, payroll, and customer-access subsection.
  - Project budget summary ownership now follows the Midday-style local component boundary more closely: `BudgetSummary` moved out of the broader project budget content module into a dedicated `project-budget-summary.tsx` file with a local `Props` type, preserving the create-summary form fallback, approved/forecast/actual/variance totals, line-item rollups, semantic card tokens, and both budget page and project overview consumers.
  - Dev tenant launcher loading now follows the shared Midday-style skeleton-state pattern: `DevTenantFab` replaces the ad hoc `Loading tenants...` body label with stable skeleton rows using the shared UI skeleton primitive while preserving the development-only guard, popover shell, tenant query, external launch URLs, error and empty states, and tenant row actions.
  - Notifications package validation now has the same Bun test type dependency boundary as other test-bearing workspace packages: `@plotkeys/notifications` declares `@types/bun` so its `bun:test` files participate in package typecheck instead of leaving notification boundary validation dependent only on runtime tests.
  - Onboarding signup notifications now respect the Midday-style client/server package boundary: the dashboard client component imports `makeUserRecipients` from `@plotkeys/notifications/react-client`, and that browser-safe entry exports the pure recipient helper without pulling the server `Notifications` class, Prisma, or pg into the browser bundle. This preserves signup-success notification dispatch through `notifications-react` while clearing the dev-server client bundle failure exposed by onboarding smoke QA.
  - Onboarding checkbox-card rows now have explicit stable `id` / `htmlFor` associations for property-type and content-readiness options, preserving React Hook Form controller state, option toggling, card styling, and quick-fill compatibility while clearing the accessibility validation blockers surfaced during the SubmitButton call-shape pass.
  - Remaining dashboard `SubmitButton` submit-action call-shape residues were cleared in onboarding step actions, billing payment repair, and publish-confirmation actions by removing redundant `type="submit"` props while preserving form-owned submission, pending spinner behavior, disabled state, variant styling, quick fill placement, and action copy.
  - Dashboard proxy static logo/icon handling now keeps public brand assets out of the unauthenticated route gate: `proxy.ts` allows the dashboard `logo*`, `icon*`, `apple-icon*`, and `favicon*` public asset files so sign-in/search/sidebar `PlotKeysLogo` renders actual images instead of redirecting asset requests to `/sign-in`, while preserving auth protection for dashboard product routes.
  - Shared Plot Keys logo composition now uses the shared Midday-style `cn` utility instead of a local `cx` / `filter(Boolean).join(" ")` class helper, preserving mark/wordmark source selection, caller className overrides, sign-in/search/sidebar logo rendering, and package-level UI ownership.
  - AI credit top-up now follows the shared Midday-style pending-action pattern: the purchase control uses `SubmitButton` with stable `Buy <credits> Credits` copy and spinner-owned pending state while preserving purchase mutation execution, credit-info invalidation, top-up helper copy, and inline destructive error display.
  - Verify-email confirmation now follows the shared Midday-style pending-action pattern: the primary verify action uses `SubmitButton` with stable `Verify and continue` copy and spinner-owned pending state while preserving token disabling, hidden onboarding/session inputs, auth error display, session persistence, redirect behavior, router refresh, and the secondary sign-up return link.
  - Domain connect and provisioning sync actions now follow the shared Midday-style pending-action pattern: `ConnectDomainForm` and `DomainControlCard` use `SubmitButton` with stable visible copy and spinner-owned pending state while preserving disabled provisioning guards, connect-domain validation, quick fill, router navigation, sync retry/default variants, error placement, and tenant-domain/DNS instruction invalidation.
  - Dashboard dropdown-menu sign-out now matches the stable pending-action treatment while preserving Midday-style menu ownership: `SignOut` stays a `DropdownMenuItem`, keeps stable `Sign out` copy, disables itself while clearing the session, and shows a centered spinner during the pending state. This preserves user-menu placement, sign-in redirect, router refresh, and menu item semantics while removing the final scanned dynamic loading-label residue from active dashboard components.
  - Customer destructive confirmation actions now match the shared Midday-style pending-action treatment more closely while preserving alert-dialog semantics: the edit sheet and customers table delete confirmations keep `AlertDialogAction` as the dialog action owner, hold stable `Delete` copy in the button, and show a centered spinner during pending deletion. This preserves cancel disabling, customer delete mutation callbacks, query invalidation, URL close behavior, row-action dialog state, and table row propagation guards while removing the old dynamic `Deleting...` label swaps.
  - Settings company-logo upload now matches Midday's avatar upload/card split more closely: the branding card composes a dedicated `CompanyLogoUpload` interaction component, while the uploader owns hidden file input selection, local uploaded-logo preview, spinner loading state, workspace logo mutation, settings-query invalidation, optional upload callback, and file-input reset. This preserves the settings branding copy/card placement and moves the upload trigger behavior toward Midday's `UserAvatar` / `AvatarUpload` ownership pattern.
  - Direct sign-out button pending behavior now matches Midday's shared pending-action pattern more closely: `SignOutButton` uses `SubmitButton` with stable visible copy and spinner-owned pending state while preserving optional icon/label rendering, ghost styling, session clearing, sign-in redirect, and router refresh behavior. Dropdown menu sign-out remains on `DropdownMenuItem` because it owns menu semantics.
  - Blog detail destructive action now matches Midday's shared pending-action pattern more closely: the detail header delete control uses `SubmitButton` with stable visible copy and spinner-owned pending state while preserving the ghost/destructive styling, delete mutation callback, surrounding publish/archive actions, blog status badges, and detail header layout.
  - Project status and delete action buttons now match Midday's shared pending-action pattern more closely: `UpdateProjectStatusButton` and `DeleteProjectButton` use `SubmitButton` with stable visible copy and spinner-owned pending state while preserving project status/delete mutation payloads, variants, compact sizing, click ownership, and project cache invalidation.
  - Builder preview frame page navigation now routes active in-frame page tab controls through the shared Midday-style `Button` primitive while preserving compact bordered tab styling, active/inactive page state, horizontal overflow behavior, page navigation callbacks, preview hostname copy, section/page metadata, and the read-only upgrade notice. The shared search-filter raw dropdown trigger remains intentionally unchanged because it matches Midday's invoice search reference.
  - Customer edit and development helper action triggers now route more visible actions through the shared Midday-style `Button` primitive: the customer edit sheet menu trigger uses an icon ghost button, and dev tenant/signup quick-fill row actions use full-width ghost buttons while preserving dropdown ownership, delete confirmation, tenant launch URLs, preset fill callbacks, random-user generation, and dev-only guards. The exact Midday-matching search-filter raw dropdown trigger remains intentionally unchanged.
  - Builder template preview controls now remove inert button affordances and route active chrome through the shared Midday-style button primitive: the floating configuration rail menu toggle uses `Button` while preserving expanded-state labels, pin/collapse behavior, and rail layout, and the standalone template preview style/color cells render as non-interactive bordered preview cells instead of focusable no-op buttons.
  - Project customer-access/team assignment and removable-domain actions now match Midday's shared pending-action pattern more closely: revoke/grant/send-notice, assign-member, and remove-domain controls use `SubmitButton` with stable visible copy and spinner-owned pending state, while preserving mutation payloads, disabled guards, project cache invalidation, domain query invalidation, error display, variants, sizes, and button ownership.
  - Export CSV and project AI action buttons now match Midday's shared pending-action pattern more closely: `ExportCsvButton`, `GenerateSummaryButton`, `RiskFlagsButton`, and `GenerateCustomerDraftButton` use `SubmitButton` with stable visible copy and spinner-owned pending state, while preserving export download behavior, AI mutations, icons, button variants/sizes, result panels, error alerts, and project-specific cache/result state.
  - Sign-in and invite-acceptance pending actions now match Midday's shared submit/pending action pattern more closely: `SignInForm` and `AcceptInviteButton` use `SubmitButton` with stable visible copy and spinner-owned pending state, while preserving sign-in validation, dev-account selector behavior, redirect handling, invite acceptance mutation, role-based completion routing, and router refresh behavior.
  - Billing callback route composition now matches the guarded Midday-style dashboard route boundary more closely: the page runs `requireOnboardedSession()` before resolving Paystack URL params and verifies the transaction metadata `companyId` matches the active membership before activating the subscription. This preserves reference/trxref normalization, Paystack verification, allowed-template derivation, subscription activation, revalidation, and redirect-only control flow while tightening tenant scoping for dashboard-owned billing callbacks.
  - App Store and Live Preview route entrypoints now run the tenant/onboarding guard before resolving URL search params, matching the guarded Midday-style route boundary used by the active dashboard table and detail pages. This preserves App Store search/tab normalization, cached company-app context loading, live-preview hostname/subdomain controls, Suspense skeletons, ErrorBoundary handling, and the existing content-level session reuse while removing the last scanned authenticated dashboard pages that parsed route-owned search params before an explicit page guard.
  - Appointments route composition now matches the guarded Midday-style table page boundary used by the rest of the active dashboard tables: `/appointments` runs `requireOnboardedSession()` before resolving URL search params, then loads appointment filters/sort, table settings, grouped prefetches, and the hydrated table shell. This preserves appointment filters, stats/list/agent prefetches, Suspense skeletons, and table rendering while removing the last scanned table route that parsed search params before the authenticated tenant guard.
  - Dashboard status and warning chrome now uses shared semantic tokens instead of direct palette utilities: App Store and Integration installed pills use `success` token classes, notification unread badges use `text-destructive-foreground`, and builder read-only/locked-template notices, locked-template upgrade copy, and estate land-price labels use `warning` token classes. This preserves installed-state display, unread-count behavior, builder upgrade routing/copy, and estate offer-card pricing labels while reducing remaining dashboard color drift.
  - Dashboard positive-value and builder/project prop-boundary residues now follow the shared Midday-style token and local typing contracts: report emphasized numbers, analytics completed counts, and positive project budget variance use the semantic `text-success` token instead of fixed `text-[#00C969]`, while the builder page picker and project budget summary use scoped private prop aliases instead of generic `PageProps` / `SummaryProps`. This preserves report table cells, analytics agent rows, project budget variance behavior, builder template/page selection, and cross-file `BudgetSummary` imports while reducing remaining dashboard styling and prop-boundary drift.
  - Dashboard summary and modal card residues now follow the same semantic card/surface token contract: payroll summary cards, blog status summary cards, AI credit summary and skeleton cards, publish-confirmation summaries, and template-recommendation profile summaries use `border-border` and `bg-card` instead of fixed light/dark border/background utilities. This preserves payroll TRPC summary values, blog status counts, AI credit totals/loading geometry, publish confirmation copy, and recommendation profile details while closing the remaining known raw summary/modal surface token gap under dashboard components.
  - Dashboard estate list/detail surfaces now follow the semantic card/surface token contract: estate summary cards, estate detail stat cards, estate list/detail skeleton cards, estate launch metric cells, estate detail location/landmark panels, payment-plan panels, and purchase pipeline stage cards use `border-border` and `bg-card` instead of fixed light/dark border/background utilities. This preserves estate detail queries, launch cards, plan upload flow, offer/inventory sections, and skeleton geometry while moving estate pages closer to the shared Midday-style design system.
  - Dashboard project detail and project budget summary cards now follow the semantic card/surface token contract: project overview stat cards, budget approved/forecast/actual/variance cards, and matching project detail/budget skeleton cards use `border-border` and `bg-card` instead of fixed light/dark border/background utilities. This preserves project detail counts, budget calculations, positive/negative variance styling, section composition, and skeleton geometry while moving project subpages closer to the shared Midday-style design system.
  - Dashboard auth/onboarding helper panels now follow the semantic surface-token contract: the tenant-scoped sign-in notice, signup/onboarding hostname preview, and onboarding company/subdomain summary use `border-border` and `bg-card` instead of fixed light/dark border/background utilities. This preserves sign-in form state, dev-account autofill copy, onboarding step logic, subdomain input control, and hostname preview behavior while making the locally visible protected-route auth surfaces match the shared Midday-style design system.
  - Dashboard home and customer-summary surfaces now use the semantic card/surface token contract: home stat links, connected-domain cards, publishing-control panels, quick-action links, customer summary cards, and customer summary skeletons use `border-border`, `bg-card`, `bg-background`, and `hover:bg-muted` instead of fixed light/dark border, background, and hover palette utilities. This preserves dashboard navigation links, publishing/domain actions, customer stats queries, and summary skeleton geometry while moving the dashboard home and table-summary surfaces closer to the shared Midday-style design system.
  - Dashboard analytics and reports panels now follow the semantic surface-token contract: report summary cards, report table surfaces, analytics metric cards, analytics panels, matching skeleton panels, and analytics ranked/recent-event row dividers use `border-border`, `bg-card`, and `border-border` separators instead of fixed light/dark border and background palette utilities. This preserves the existing route composition, export actions, report tables, analytics charts/lists, and skeleton geometry while making these dashboard pages theme-aware under the shared Midday-style design system.
  - Dashboard app/domain/billing/template support copy now follows the semantic text-token contract across the remaining marketplace and provisioning surfaces: App Store cards and empty states, integration cards, domain provisioning/instruction copy, connect-domain helper copy, billing period metadata, and the template sandbox empty state use `text-muted-foreground` or `text-foreground` instead of fixed light/dark palette utilities. This preserves the existing Midday-style card anatomy, domain action flow, billing plan summary, template sandbox profile workflow, and app enable/upgrade controls while keeping these dashboard pages theme-aware.
  - Shared dashboard filter/detail/sheet text styling now follows the repository semantic-token contract while preserving Midday-style component ownership: filter chips, generic table empty-state descriptions, stacked sheet descriptions, secondary navigation labels, dashboard user-menu metadata, command footer icons, and customer/property detail subtitles use `text-muted-foreground` or `text-foreground` instead of fixed gray/black palette utilities. This keeps the page-filter, table empty state, sheet header, menu, and detail-sheet surfaces theme-aware without changing URL filters, row actions, sheet state, or detail queries.
  - Dashboard header/status tab styling now follows one shared token-led contract: `HeaderLinkTab` exposes reusable tab class constants using semantic `muted`, `muted-foreground`, `foreground`, and `accent` classes instead of fixed light/dark palette values, and App Store tabs now consume the same constants rather than duplicating the Midday tab class strings locally. Formatting passed for the tab files, the targeted fixed tab-palette scan is clean, `@plotkeys/dashboard` typecheck remains green, and a Playwright smoke screenshot of `/app-store` confirmed the protected dashboard route still renders via the sign-in redirect.
  - Dashboard report, customer form-context, onboarding logo/notification, blog editor, property pricing-plan, and dev quick-fill helper boundaries now match Midday's private local prop convention more closely: private component props use local `Props` or scoped helper aliases while exported domain contracts such as `CustomerFormValues` and `PricingPlanDraft` remain descriptive. This preserves report tables, schema-backed customer form context, onboarding logo upload/notification behavior, blog markdown editing, pricing-plan draft helpers, and development quick-fill surfaces. Formatting passed for the touched files, the targeted old-alias scan is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard table hover styling now follows the shared semantic token contract across active Midday-style table surfaces: the core virtual row hover state, full-width actions cell, select column, and sticky/action column metadata for agents, appointments, blog, customers, departments, employees, leads, leave requests, notifications, payroll, projects, properties, and teams use `muted` token hover classes instead of fixed light/dark hex colors. The customer table actions cell also moved propagation handling onto the actual menu/dialog controls so the table row click contract is preserved without a static interactive wrapper. Formatting passed for the touched table files, the fixed table hover-color scan is clean, `@plotkeys/dashboard` typecheck remains green, and a Playwright smoke screenshot of `/customers` confirmed the protected dashboard route still renders via the sign-in redirect.
  - Dashboard payroll, invite, flow, domain, and blog helper component boundaries now match Midday's private local prop convention more closely: payroll summary/period tabs, agent/employee/team invite helpers, collapsible summary, flow shell, domain section/connect-domain view, create blog post button, and blog detail content/section/notice now use local `Props` aliases instead of exported-sounding feature `*Props` names, while preserving the existing TRPC queries, invite revocation, payroll period links, onboarding shell composition, domain connection flow, blog creation redirect, and blog detail mutations. Formatting passed for the touched files, the targeted old-alias scan is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Shared dashboard utility component boundaries now match Midday's local/private prop convention more closely: `HeaderLinkTab`/`HeaderLinkTabList`, `Portal`, `ScrollableContent`, and `ExportCsvButton` use local `Props` or scoped aliases instead of exported-sounding private `*Props` names, while preserving tab links, portal mounting, scroll-offset content behavior, CSV export behavior, and existing button call shape. Formatting passed for the touched utility files, the targeted old-alias scan is clean, the stale `useState` import warning in `ExportCsvButton` was removed, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard property detail redirect now matches the URL-backed sheet contract more tightly: `/properties/[id]` redirects into the properties table detail sheet with only the `details` and `propertyId` params owned by `usePropertyParams`, removing stale `imageProvider`/`imageQuery` forwarding that no current property sheet/form consumer reads. Formatting passed for the route, the targeted stale-param scan is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard notifications table no-results behavior now matches the shared Midday-style table filter contract: the notification `NoResults` action clears the full URL-backed notification filter state instead of only clearing `q`, so no-result states caused by unread tabs, date ranges, or search can recover through the same "Clear filters" action used by other dashboard table domains. Formatting passed for the touched notifications empty-state file, the targeted table no-results scan is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard signup/onboarding subdomain field behavior now stays aligned with the same state that powers its hostname preview: `SubdomainField` uses a local `Props` alias and renders the input from the tracked subdomain value instead of leaving `defaultValue`/reused uncontrolled callers visually empty, while preserving the existing React Hook Form controlled signup path, `.plotkeys.com` input addon, website/dashboard hostname previews, and normalized preview generation. Formatting passed for the touched field, the old private prop alias scan is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard search/filter helper component boundaries now match Midday's local/private prop convention more closely: the shared `SearchFilter` root, `FilterList`, filter menu item, and filter checkbox item use local `Props` or scoped helper aliases instead of exported-sounding feature `*Props` names, while preserving generic filter descriptors, URL-backed search/filter controls, chips, clear behavior, date-range filtering, hotkeys, and domain filter consumers. Formatting passed for the touched search/filter files, the targeted old-alias scan is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Shared dashboard table-core helper component boundaries now match Midday's local/private prop convention more closely while preserving the exported generic table contracts: `BottomBar`, `CoreDataTableShell`, `BulkClientAction`, `BulkClientDeleteAction`, `CoreColumnVisibility`, and `CoreDataTableContent` now use local `Props` aliases instead of private `*Props` names, while exported factory/runtime contracts such as `CoreDataTableHeaderProps`, `CoreDataTableContentHeaderProps`, and runtime types remain descriptive. Formatting passed for the touched table-core files, the targeted old-alias scan is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard page-section and detail helper component boundaries now match Midday's private local prop convention more closely across analytics, reports, billing, AI credits, live preview, project detail/budget/workforce, and estate detail/summary surfaces. Section wrappers, report tables/view, billing content, AI summary, live preview content/frame/unavailable states, project section/detail stats/content/budget/workforce helpers, and estate section/detail stats/content/summary helpers now use local `Props` or scoped helper aliases instead of exported-sounding feature `*Props` names, preserving report exports, analytics panels, billing plan flow, AI credit summaries, live preview rendering, project overview/budget/workforce queries and mutations, estate launch detail sections, estate plan upload, offer cards, inventory table, and purchase pipeline behavior. Formatting passed for the touched files, targeted old-alias scans are clean, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard shell/header/home and app-card helper component boundaries now match Midday's private local prop convention more closely: notification bell/content/item, dashboard chrome, dashboard home root/stat/section/publishing/domain helpers, App Store view/content/toggle/logo helpers, and integrations section/card/logo/actions helpers use local `Props`/scoped helper aliases instead of exported-sounding feature `*Props` names, preserving header notification data, tenant-aware navigation, dashboard overview stats, publishing links, connected-domain cards, app enable toggles, plan-gated app actions, integration installed states, and docs/configuration links. Formatting passed for the touched files, targeted old-alias scans are clean, and `@plotkeys/dashboard` typecheck remains green.
  - Builder sidebar/workspace and preview helper component boundaries now match Midday's private local prop convention more closely: read-only notice, editable-fields note, drawer trigger/content, control stack/items, section group/fields, configuration summary, controls, workspace/sidebar shells, template/page/theme picker helpers, section/SEO/image helpers, template picker/sidebar/frame/status, AI tool control, preview runtime bodies, field editor, and preview section/list helpers use local `Props`/scoped helper aliases instead of exported-sounding `Builder*Props` names, preserving drawer controls, template/page/theme selectors, read-only plan messaging, configuration summary, section visibility, SEO controls, editable-field counts, template preview navigation, publish status, registry/runtime preview rendering, workspace composition, and inline editing behavior. Formatting passed for the touched files, targeted old-alias scans are clean, and `@plotkeys/dashboard` typecheck remains green.
  - Active dashboard sheet roots now follow Midday's close-aware sheet lifecycle more closely: create/edit/invite/detail/builder sheets receive the `open` argument in `onOpenChange` and clear URL state only when the sheet is closing, preserving all existing `setParams(null)` form/header close callbacks, detail-sheet parameter clearing, always-mounted `GlobalSheets` registration, stacked sheet content, and builder sidebar drawer behavior. The builder sidebar drawer's private prop type also now uses a local `Props` alias, the sheet residue scan no longer finds unconditional `onOpenChange={() => setParams(...)}` handlers, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard modal component boundaries now match Midday's local modal prop convention more closely: publish-confirmation and recommend-template modal roots plus their action, disabled, live-note, summary, fields, error, and profile-summary subcomponents use private `Props` aliases instead of feature-specific `*Props` names, with formatting and `@plotkeys/dashboard` typecheck passing and the strict modal prop-alias scan showing only local `Props` entries.
  - Another dashboard local prop-boundary parity slice was completed: the settings logo upload form, invite sign-up/profile/accept entry components, invite agent/member/employee forms, builder preview frame/register/shell/panel/header components, draggable table header, and select-column header now use local `Props` aliases where the type is private to the component, matching the Midday route/component convention while leaving exported shared core table contracts descriptive. Targeted residue search for those old aliases is clean, and `@plotkeys/dashboard` typecheck remains green.
  - Dashboard final-verification blockers were cleared for the current F5 parity slice: URL filter loader aliases now use resolved `Awaited<ReturnType<...>>` values across active table filter hooks; shared pagination metadata now carries the dashboard list contract's `hasNextPage`; customer detail/edit sheets no longer treat lightweight list rows as full detail placeholders; builder preview/register link handlers share one `href + event` contract; invite acceptance handles the full membership-role enum; and the remaining table-header, estate plan key, team invite cap, and template sandbox shuffle typings were aligned. `@plotkeys/dashboard`, `@plotkeys/api`, `@plotkeys/ui`, `@plotkeys/site-nav`, and `@plotkeys/utils` typechecks now pass for this verification slice, and the builder editable-text render test passes.
  - Focused validation blockers in the dashboard/template data path were cleared: section-registry carried-forward theme tests now type dynamic theme keys explicitly, DB employee/project query helpers satisfy strict Prisma/noUncheckedIndexedAccess typing without changing list contracts or update behavior, tenant-site and DB packages declare Bun test types, and the API CORS origin callback is explicitly typed. `@plotkeys/section-registry`, `@plotkeys/db`, `@plotkeys/tenant-site`, and `@plotkeys/api` typechecks now pass, with dashboard-wide typecheck still tracking a separate larger set of remaining migration errors.
  - Shared template/register class composition now matches Midday's `cn` utility boundary more closely: template UI variants, Riwaq template pages/section style helpers, and tenant RegisterNav use the shared `cn` export instead of local `joinClasses` helpers, with `section-registry` declaring the workspace utility dependency while preserving scoped sandbox/live nav hrefs, radius/menu variants, template button/input/surface classes, Riwaq layout classes, and builder/tenant preview rendering.
  - Remaining dashboard-owned auth/onboarding/invite/builder/template-sandbox route entrypoints now match Midday's route-local page prop boundary more closely: Sign In, Sign Up, Onboarding, Verify Email, Join Invite, Invite Profile Completion, Builder, and Template Sandbox detail pages use local `Props` aliases instead of feature-specific `*PageProps` names while preserving metadata, `Promise<SearchParams>` / `Promise<params>` inputs, tenant redirects, session guards, invite validation, builder URL state, template sandbox query normalization, and route composition.
  - Remaining active dashboard `(app)` route entrypoints now match Midday's route-local page prop boundary more closely: Reports, Properties, Property Detail Redirect, Blog list/detail, Notifications, Estate Detail, Billing, Billing Callback, Live Preview, Team, Projects list/detail/budget/workforce, App Store, and Leads pages use local `Props` aliases instead of feature-specific `*PageProps` names while preserving metadata, `Promise<SearchParams>` / `Promise<params>` inputs, redirect handling, session guards, filter/sort loaders, prefetching, HydrateClient boundaries, Suspense skeletons, table settings, detail content, and header/table/sheet composition.
  - Dashboard HR/list route entrypoints now match Midday's route-local page prop boundary more closely: Agents, Appointments, Departments, Employees, Leave Requests, and Payroll pages use local `Props` aliases instead of feature-specific `*PageProps` names while preserving metadata, `Promise<SearchParams>` inputs, session guards, filter/sort loaders, prefetching, HydrateClient boundaries, Suspense skeletons, table settings, invite visibility, and summary/header/table composition.
  - Private detail, subpage, live-preview, and stacked-sheet header components now match Midday's local header prop boundary more closely: Blog Detail, Estate Detail, Project Detail, Project Subpage, Live Preview, and Stacked Sheet headers use local `Props` aliases instead of feature-specific `*HeaderProps` names while preserving status badges, action buttons, back links, sheet close controls, launch/property sheet triggers, and published-site metadata.
  - Additional top-level dashboard headers now match Midday's local header prop boundary more closely: Payroll, Reports, Integrations, Billing, Domains, and Dashboard Home headers use local `Props` aliases instead of feature-specific `*HeaderProps` names while preserving period tabs, plan interval tabs, integration counts, domain provisioning status, dashboard CTAs, search controls, column visibility controls, and sheet triggers.
  - Core dashboard list headers now match Midday's local header prop boundary more closely: Agents, Customers, Employees, and Team headers use local `Props` aliases instead of feature-specific `*HeaderProps` names while preserving search/filter controls, column visibility controls, invite/create actions, permission gates, and status tabs.
  - Dashboard user menu now matches Midday's `UserMenu` source boundary more closely by using a local `Props` type instead of `DashboardUserMenuProps`, while preserving avatar trigger anatomy, initials fallback, account/settings rows, sign-out ownership, company/work-role metadata, and dropdown placement.
  - Prop-bearing open-sheet trigger components now match Midday's local trigger prop boundary more closely: Open Project Sheet, Open Property Sheet, and Open Estate Launch Details Sheet use local `Props` aliases instead of feature-specific `Open*SheetProps` names while preserving URL-param payloads, default property values, optional className handling, button anatomy, and icons.
  - Dashboard mutation form prop boundaries now match Midday's local `Props` pattern across non-invite forms: Property Form now uses a local non-exported `Props` type, after Create Estate, Agent, Blog Post, Project, Estate Plan Upload, Estate Launch Details, Create Budget Line, Create Worker, Create Payroll Run, Project Budget Summary, Connect Domain, Customer, Department, Appointment, Leave Request, and Payroll Entry were aligned. The remaining form-prop scan is limited to invite forms, which match Midday's descriptive invite-form exception.
  - Additional local project/domain mutation forms now match Midday's local form prop boundary more closely: Create Budget Line, Create Worker, Create Payroll Run, Project Budget Summary, and Connect Domain forms use a local `Props` type instead of feature-specific `*FormProps` names while preserving every field, schema, mutation/invalidation target, router refresh/push behavior, disabled/error handling, and success callback.
  - Core mutation forms now match Midday's local form prop boundary more closely: Customer, Department, Appointment, Leave Request, and Payroll Entry forms use a local `Props` type instead of feature-specific `*FormProps` names while preserving every field, schema, default value, mutation, invalidation target, cancel/success callback, and sheet-owned close behavior.
  - Invite-capable dashboard table routes now avoid runtime-varying `batchPrefetch` arrays: Agents prefetches its single list query with `prefetch(...)`, and Agents, Employees, and Team prefetch `trpc.team.listInvites` through explicit permission-gated `prefetch(...)` calls instead of conditional spread entries inside `batchPrefetch`. Always-needed grouped route data remains batched, preserving invite visibility, membership permission checks, HydrateClient boundaries, table settings, filters/sort inputs, and infinite query cursors.
  - Remaining static single-query route preloads now use Midday's `prefetch(...)` helper instead of `batchPrefetch([oneQuery])` in the Departments table route and Project Workforce detail route, preserving their authenticated session guards, HydrateClient boundaries, Suspense skeletons, table/content components, filters/sort handling, and query options while leaving true grouped and conditional prefetch arrays on `batchPrefetch`.
  - Active dashboard table `actions-menu.tsx` files now use Midday's local `Props` type boundary instead of `ActionsMenuProps` across Agents, Appointments, Blog, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team. Redundant `onClick` stop-propagation wrappers were removed from row action menu shells where the shared virtual-row action column already owns non-clickable behavior, and the stale Team display-name local was removed while preserving all menu actions, mutation invalidation, permission gates, route/sheet params, and sticky action-column rendering.
  - Prop-bearing domain table empty-state files now use Midday's local `Props` type boundary instead of feature-specific `EmptyStateProps` names in Blog, Customers, Employees, Leads, Leave Requests, Notifications, Payroll, and Projects while preserving their domain copy, clear-filter handlers, create actions, and route/sheet param behavior.
  - Active dashboard table `data-table.tsx` components now use Midday's local `Props` type boundary instead of the Plot Keys-specific `DataTableProps` name across Agents, Appointments, Blog, Customers, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team tables, preserving all table props, route wiring, settings hydration, selection, and bulk actions.
  - Single-query dashboard route preloads now use the Midday `prefetch(...)` helper instead of wrapping one query option in `batchPrefetch([...])`: Dashboard Home, Analytics, Reports, Billing, Integrations, AI Credits, Settings, Settings Notifications, Settings Integrations, Blog detail, Estate list/detail, and Project overview/budget detail pages were aligned while true grouped route preloads remain on `batchPrefetch`.
  - Template Sandbox server routes now use the Midday-style server query-client/options-proxy path for route-owned queries: the sandbox redirect and detail pages call `getQueryClient().fetchQuery(trpc.templateSandbox.*.queryOptions(...))`, and the dashboard tRPC server boundary no longer exports the ad hoc page-only `getServerTrpcClient` helper. This preserves default-profile redirect behavior, profile not-found handling, and preview `page` / `path` query normalization while aligning server page data access with the existing `batchPrefetch`/`HydrateClient` route pattern.
  - Runtime DB-backed dashboard route/helper modules now carry explicit server-only boundaries where they remain intentional server adaptations: session, notifications, company-app context, onboarding, invite join, invite profile completion, and billing callback modules import `server-only` before runtime `@plotkeys/db/queries` usage, matching the existing dashboard trpc/server, builder, live-preview, and dashboard-home builder guards while leaving API route handlers under their own server runtime.
  - Remaining dashboard-owned route entrypoints now match Midday's metadata-bearing URL-param boundary more closely: Sign In, Sign Up, Onboarding, Verify Email, Invite Join, Invite Profile Completion, and Template Sandbox detail pages export typed Next metadata and accept `Promise<SearchParams>` from `nuqs`; repeated query keys are normalized before existing auth, onboarding, invite, verification, and sandbox flows receive plain strings, preserving redirects, error display, tenant handoff URLs, profile setup, and template preview page/path selection.
  - Builder and Billing route entrypoint typing now match the Midday URL-driven page contract more closely: the full-screen Builder route exports typed Next metadata, and both Builder and Billing accept `Promise<SearchParams>` from `nuqs`; repeated query keys are normalized before passing builder `page`/`path`/notice flags and billing `interval` into feature code, preserving the authenticated builder shell, workspace loading flow, billing prefetching, hydration, and selected interval behavior. The dashboard package now also declares Midday's `server-only` dependency so existing and newly guarded server-only imports resolve through the package graph.
  - Runtime DB-backed dashboard server components now carry explicit server-only boundaries where they remain as domain adaptations: `LivePreviewContent` and `BuilderWorkspace` import `server-only` before their `@plotkeys/db/queries` imports, matching the existing dashboard-home builder guard and preventing those query-backed component entrypoints from drifting into client bundles while the larger API/tRPC boundary remains a future verified slice.
  - App Store card anatomy now matches Midday's `UnifiedAppComponent` header/status slot more closely: the installed pill sits inside the reference `flex items-center gap-2` wrapper, and the empty-search clear link keeps visual Button props grouped before `asChild`, while preserving plan-gated upgrade routing, enable/disable toggles, search/tab filtering, and the existing RSC cached company-app context boundary.
  - Shared dashboard `SearchField` source shape now matches Midday's app search-field reference more closely by using the same local `Props` type boundary while preserving the existing `shallow` prop surface, q query state, Escape clearing, input props, and App Store search behavior.
  - Remaining URL-driven dashboard query routes now match Midday's page boundary more closely: App Store, Live Preview, Billing callback, and the legacy Property detail redirect route accept `Promise<SearchParams>` from `nuqs` and normalize string-array query values before passing plain strings into their existing feature/control-flow components, preserving app search/tab state, tenant live-preview hostname/subdomain behavior, Paystack reference handling, and property media redirect query preservation.
  - Reports route URL-param typing now matches Midday's URL-driven page contract more closely: the server page accepts `Promise<SearchParams>` from `nuqs`, and the report-period resolver normalizes string-array values while preserving month/year defaults, prefetching, header tabs, and report query behavior.
  - Billing callback route metadata now matches the Midday route-entrypoint contract more closely: the authenticated billing callback `page.tsx` exports typed Next metadata while preserving the Paystack verification, subscription activation, revalidation, and redirect-only control flow.
  - Active dashboard table skeleton ownership now matches Midday feature skeleton references more closely: Agents, Appointments, Blog, Customers, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team skeleton files render `TableSkeleton` directly with domain columns, row count, sticky IDs, and actions column instead of using the local `createTableSkeleton` factory; the factory and exported skeleton prop helper types were removed from the core table surface while preserving each skeleton's previous columns, sticky columns, and loading geometry.
  - Shared table core header imports now match the Midday-style core boundary more closely: `data-table-header.tsx` imports `SelectColumnHeader`, sticky action classes, and `TableScrollState` from relative core modules instead of reaching back through the app alias/private `core/types` path, while preserving the shared header factory, sort controls, sticky-column behavior, DnD, resize handles, and all domain table-header consumers.
  - Shared table core barrel exports now match Midday's public primitive boundary more closely: `BottomBar`, empty states, skeleton cell/table skeleton, table constants/types, and `VirtualRow` lead `components/tables/core/index.ts` in the same order as the Midday reference, while Plot Keys' additional shared table actions, shells, select column helpers, and runtime hooks remain exported after the reference block.
  - Dashboard sheet/detail URL-param hooks now match Midday's selected-record-first schema shape more closely: property params declare `propertyId` before create/detail support params, and estate params declare `estateSlug` before create/edit flags, while preserving every URL key, setter contract, sheet open/close behavior, and all existing consumers.
  - Dashboard filter-param hooks now match Midday's URL filter hook return-shape contract more closely: agent, appointment, blog, customer, department, employee, lead, leave-request, notification, payroll, project, property, and team hooks return canonical setters before `hasFilters`, while preserving Plot Keys' `filters` / `setFilters` aliases, null-clearing wrappers, schemas, loaders, resolver helpers, and all URL state names.
  - Shared table config utilities now match Midday's public core table boundary more closely: `table-configs.ts` imports `StickyColumnConfig` and `TableConfig` from the `components/tables/core` barrel instead of reaching into `core/types`, while preserving every Plot Keys table id, sticky-column config, sort map, row height, summary height, and complete table configuration value.
  - Shared table settings utilities now match Midday's documented settings boundary more closely: `table-settings.ts` restores the reference comments for table IDs, single/all table settings, cookie ownership, default visibility/settings, merge defaults, column-id extraction, and saved-order normalization; settings return objects now use the Midday `columns` / `sizing` / `order` shape while preserving Plot Keys' `select`-first and `actions`-last column-order rules.
  - Shared `useStickyColumns` now matches Midday's table-config-backed hook contract more closely: it imports `STICKY_COLUMNS` and defaults optional sticky-column config to the closest local shared directory table (`customers`) instead of an empty array, while preserving all active table callers because headers, skeletons, and runtime column helpers already pass explicit table-specific sticky configurations.
  - Shared dashboard `ErrorFallback` now matches Midday's exact recoverable error-state source shape: the wrapper class order and retry `Button` prop order mirror the reference while preserving the router refresh retry behavior, visible copy, outline styling, and every ErrorBoundary consumer.
  - Shared `useScrollHeader` now matches Midday's hook source contract more closely: it restores the reference CSS-variable comments, options interface, hook documentation, typed refs, and `prev*` internal ref naming while preserving the existing header offset, route reset, requestAnimationFrame scroll handling, summary-grid extra offset, body overflow behavior, and all consumers.
  - Shared `ScrollableContent` now matches Midday's page-shell wrapper boundary more closely: it uses the reference interface props shape and documents the scroll-to-hide header contract for table/page content, while preserving the existing `--header-offset` transform, transition timing, will-change styling, and all dashboard route wrappers.
  - Shared `CollapsibleSummary` now matches Midday's summary-collapse component boundary more closely: it uses the reference interface props shape and documents the shared `--header-offset` / `--header-transition` CSS-variable contract while preserving the existing transform, transition, will-change styling, Customers summary collapse, and table scroll behavior.
  - Customers route summary grid composition now matches Midday's invoice/customer summary-grid class contract more closely: the `CollapsibleSummary` grid orders responsive column classes before gap and top-padding utilities, while preserving the Plot Keys customer summary cards, Suspense boundaries, prefetching, header, table, and scroll-collapse behavior.
  - Dashboard table summary scroll geometry now matches Midday's summary-bearing table contract more closely: Customers uses Midday's `180` pixel `SUMMARY_GRID_HEIGHTS` offset for its `CollapsibleSummary`, while Properties no longer declares or passes a summary-grid offset because its route has no collapsible summary section. Header collapse, table virtualization, sticky columns, and properties row behavior are preserved.
  - Active dashboard table row-height runtime now matches Midday's shared table baseline more closely: Agents, Appointments, Blog, Customers, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team all use the Midday `45` pixel virtual row height used by customers/transactions/vault and the shared table header/skeleton, removing legacy 52/56/64 pixel domain-specific row heights while preserving sticky columns, virtualization, selection, and row action behavior.
  - Active dashboard table action-column headers now match Midday's invoice/customer action column contract more closely: Agents, Appointments, Blog, Customers, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team use visible column header metadata `Actions` alongside existing headerLabel and icon skeleton metadata, while preserving action column widths, sticky classes, row menus, and table header rendering.
  - Active dashboard table action-column skeleton metadata now matches Midday's invoice/customer/vault action column references more closely: Agents, Appointments, Blog, Customers, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team action columns use `skeleton: { type: "icon" }` instead of text-width skeletons, while preserving action column widths, sticky classes, row menus, and loading row structure.
  - Active dashboard table sticky column metadata now matches Midday's invoice/customer column class contract more closely: primary sticky columns and action sticky columns in Agents, Appointments, Blog, Customers, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team use unprefixed sticky backgrounds, row-hover sticky backgrounds, z-index tokens, and action border classes shaped like the Midday references, while preserving column widths, row actions, selection behavior, and table virtualization.
  - Dashboard positive numeric value tokens now match Midday's amount-positive treatment more closely: report emphasized counts, analytics completed-appointment counts, and project budget positive variance use the Midday `text-[#00C969]` positive token, while preserving App Store/Integrations installed pills that already match Midday's `UnifiedApp` green-pill reference.
  - Dashboard cancel, disabled, and retry action buttons now match the migrated Midday visual-first button call contract more closely: form cancel actions, publish modal cancel, billing disabled plan actions, quick-fill trigger, and error retry pass `variant`/`size`/local classes before event/disabled/type props, while preserving cancel callbacks, disabled-state behavior, publish modal ownership, billing plan copy, quick-fill trigger behavior, and router refresh retry.
  - Dashboard link actions now match the migrated Midday visual-first button call contract more closely: route/header CTAs, auth/join/onboarding links, builder/template actions, notification footer, settings upgrade, and dashboard-home actions pass `variant`/`size`/local classes before `asChild`, while preserving live preview, builder, billing, domain, auth, invite, template sandbox, notification, and empty-state routing behavior. The active dashboard scan now has no `Button asChild` calls where visual props still follow `asChild`.
  - App Store and Integrations card actions now match Midday's `UnifiedAppComponent` action-button call shape more closely: locked app upgrade, integration configure, and integration docs buttons keep `variant` and local width class grouped before `asChild`, while preserving billing/settings/docs routing, locked-plan copy, external-link icon behavior, installed status pills, and app-card anatomy.
  - Onboarding logo upload, Template Sandbox create, and builder AI tool actions now match the shared Midday pending-action pattern more closely: `OnboardingBrandAvatar`, `TemplateSandboxIndex`, and `BuilderAiToolControl` use `SubmitButton` with stable visible copy and spinner-owned pending state, while preserving file-picker behavior, logo preview/error handling, router refresh, sandbox creation form submission, profile invalidation, post-create routing, AI generation triggers, disabled guards, result messages, and error copy.
  - Auth, billing, and upload submit actions now match Midday's submit/pending action pattern more closely: `SignUpForm`, invited signup, billing checkout actions, billing repair submit, and estate plan upload use the shared `SubmitButton` with stable visible copy and spinner-owned pending state where applicable, while preserving signup validation, dev-account capture, invite acceptance, redirects, checkout mutation payloads, billing callback routing, current-plan disabled state, repair-reference form handling, estate plan file upload, create-layout mutation, and estate detail invalidation.
  - Project subresource inline create submits now match Midday's form submit action pattern more closely: phase, milestone, issue, and update create forms use the shared `SubmitButton` with stable visible copy and spinner-owned pending state, while preserving inline form layout, required fields, controlled select fields, create mutations, project cache invalidation, form resets, and existing list/status action buttons.
  - Remaining non-submit dashboard action buttons with dynamic pending labels now match the shared Midday pending-action pattern more closely: `CreateBlogPostButton`, `BuilderPreviewFieldEditor`, shared `QuickFill`, and `DevFormQuickFillButton` use the shared `SubmitButton` with stable visible copy and spinner-owned pending state, while preserving blog create mutation/routing/query invalidation, optional add icon/variant/size props, builder save transition, AI fill transition, quick-fill busy guards, custom quick-fill labels, secondary full-width save styling, ghost compact AI-fill styling, and dev-only quick-fill visibility.
  - Invite profile completion submit behavior now matches Midday's form submit action pattern more closely: `InviteProfileCompletionForm` uses the shared `SubmitButton` with stable `Save and continue` copy and spinner-owned pending state, while preserving the skip link, quick-fill controls, uncontrolled field refs, invite profile completion mutation, token payload, and post-success routing.
  - Onboarding step footer submit behavior now matches Midday's onboarding submit action pattern more closely: shared `StepActions` uses the shared `SubmitButton` with stable step-specific copy and spinner-owned pending state, while preserving quick-fill placement, back/cancel link buttons, step save mutations, redirect behavior, dashboard URL generation, and the final `Open builder` label.
  - Property detail media upload submit behavior now matches Midday's form submit action pattern more closely: `PropertyDetails` uses the shared `SubmitButton` with stable `Add media` copy and spinner-owned pending state for the media upload form, while preserving detail-sheet ownership, media URL/type/cover inputs, add-media mutation, property/media/list invalidations, form reset, and existing per-media action buttons.
  - Leave-request create form submit behavior now matches Midday's form submit action pattern more closely: `LeaveRequestForm` uses the shared `SubmitButton` with stable `Submit request` copy and spinner-owned pending state, while preserving the cancel action, employee availability disabled guard, controlled employee/type selects, date validation, create mutation, error placement, leave-request list/stat invalidations, form reset, and sheet success callback.
  - Property create/edit form submit behavior now matches Midday's form submit action pattern more closely: `PropertyForm` uses the shared `SubmitButton` with stable create/edit copy and spinner-owned pending state, while preserving the cancel action, quick-fill controls, pricing-plan draft fields, land-listing field guards, field validation, active create/update mutation selection, error placement, property list/detail/stat invalidations, form reset, and sheet success callback.
  - Agent create/edit form submit behavior now matches Midday's form submit action pattern more closely: `AgentForm` uses the shared `SubmitButton` with stable create/edit copy and spinner-owned pending state, while preserving the cancel action, quick-fill controls, field validation, active create/update mutation selection, error placement, agent list/detail invalidations, form reset, and sheet success callback.
  - Estate launch details edit form submit behavior now matches Midday's form submit action pattern more closely: `EstateLaunchDetailsForm` uses the shared `SubmitButton` with stable `Save launch` copy and spinner-owned pending state, while preserving quick fill, publish-state control, asset upload helpers, field validation, update mutation, error placement, form reset from returned estate data, estate/detail/stat invalidations, and sheet success callback.
  - Estate launch create form submit behavior now matches Midday's form submit action pattern more closely: `CreateEstateForm` uses the shared `SubmitButton` with stable `Create launch` copy and spinner-owned pending state, while preserving quick fill, field validation, create mutation, error placement, form reset, estate list invalidation, and sheet success callback.
  - Blog post edit form submit behavior now matches Midday's form submit action pattern more closely: `BlogPostForm` uses the shared `SubmitButton` with stable `Save changes` copy and spinner-owned pending state, while preserving rich-text editing, validation, update mutation, saved-state alert, form reset from returned post data, and blog post/list/stats invalidations.
  - Payroll entry form submit behavior now matches Midday's form submit action pattern more closely: `PayrollEntryForm` uses the shared `SubmitButton` with stable `Add entry` copy and spinner-owned pending state, while preserving cancel behavior, employee/month/year controls, amount validation, empty-employee disabled guard, create mutation, error placement, payroll invalidations, form reset, and success callback.
  - Project payroll-run form submit behavior now matches Midday's form submit action pattern more closely: `CreatePayrollRunForm` uses the shared `SubmitButton` with stable `Create payroll run` copy and spinner-owned pending state, while preserving date validation, create mutation, error placement, project/workforce/payroll invalidations, form reset, router refresh, and sheet success callback.
  - Project budget line-item form submit behavior now matches Midday's form submit action pattern more closely: `CreateBudgetLineForm` uses the shared `SubmitButton` with stable `Add line item` copy and spinner-owned pending state, while preserving line validation, category select, amount/quantity parsing, create mutation, error placement, project/budget invalidations, form reset, router refresh, and sheet success callback.
  - Project budget summary form submit behavior now matches Midday's form submit action pattern more closely: `ProjectBudgetSummaryForm` uses the shared `SubmitButton` with stable `Create budget` copy and spinner-owned pending state, while preserving budget validation, currency normalization, upsert mutation, error placement, project/budget invalidations, router refresh, and sheet success callback.
  - Project workforce add-worker form submit behavior now matches Midday's form submit action pattern more closely: `CreateWorkerForm` uses the shared `SubmitButton` with stable `Add worker` copy and spinner-owned pending state, while preserving worker validation, pay-basis select, create mutation, error placement, project/workforce invalidations, form reset, router refresh, and sheet success callback.
  - Project create form submit behavior now matches Midday's form submit action pattern more closely: `ProjectForm` uses the shared `SubmitButton` with stable `Create project` copy and spinner-owned pending state, while preserving the cancel action, quick-fill controls, project type select, validation, create mutation, error placement, project invalidation, form reset, and sheet success callback.
  - Appointment create form submit behavior now matches Midday's form submit action pattern more closely: `AppointmentForm` uses the shared `SubmitButton` with stable `Schedule` copy and spinner-owned pending state, while preserving the cancel action, agent select, form validation, create mutation, error message placement, appointment/stat invalidations, form reset, and close callback.
  - Department create form submit behavior now matches Midday's create-form submit action pattern more closely: `DepartmentForm` uses the shared `SubmitButton` with stable `Add department` copy and spinner-owned pending state, while preserving the cancel action, error message placement, create mutation, form reset, department list invalidation, and sheet success callback.
  - Customer create/edit form submit behavior now matches Midday's customer form submit action pattern more closely: `CustomerForm` uses the shared `SubmitButton` with stable create/edit copy and spinner-owned pending state, while preserving the cancel action, active create/update mutation selection, error alert, cache invalidations, form reset, and sheet success callback.
  - Team/agent/employee invite forms now match Midday's invite-form submit action pattern more closely: their primary submit actions use the shared `SubmitButton` with stable `Send invite` copy and spinner-owned pending state, while preserving cancel buttons, form validation, role fields, quick-fill controls, invite mutations, query invalidation, and success callbacks.
  - Builder recommendation modal submit labeling now matches Midday's shared `SubmitButton` pending pattern more closely: `RecommendTemplateActions` keeps the primary action label stable while the shared submit button owns the spinner/hidden-label loading state, preserving the footer, disabled state, mutation trigger, and recommendation copy.
  - Builder publish and recommendation modal close behavior now matches Midday's reset-on-close modal pattern more closely: both dialog owners route `onOpenChange` through local handlers that clear transient error/result state and restore form/profile inputs on close, while preserving trigger placement, dialog header/content/footer ownership, mutations, router refreshes, and publish/recommendation action behavior.
  - Builder publish confirmation actions now match Midday's modal footer submit pattern more closely: `PublishConfirmationActions` uses an outline cancel `Button` followed by shared `SubmitButton` with `isSubmitting`, `disabled`, and `type="submit"`, while preserving quick-fill placement, cancel callback, form submission, pending disabled state, and publish action ownership.
  - Builder recommendation modal actions now match Midday's modal footer pending-action pattern more closely: `RecommendTemplateActions` uses a cancel `Button` followed by a shared `SubmitButton` with `isSubmitting`, `onClick`, and `disabled`, while preserving the dialog footer, cancel callback, recommendation mutation trigger, pending disabled state, and button copy.
  - Builder recommendation modal trigger now follows Midday's outline small-button call shape more closely: `RecommendTemplatePanel` passes `variant="outline"` then `size="sm"` before local width/disabled props, matching nearby Midday connector/action button references while preserving the dialog trigger, disabled state, profile fields, mutation, router refresh, and close behavior.
  - Customer edit sheet action trigger now matches Midday's customer edit sheet menu trigger more closely: the dropdown trigger is the reference plain `button type="button"` around `Icon.MoreVertical` instead of a locally classed/labelled button shell, while preserving the edit sheet header, delete confirmation, customer invalidations, URL close behavior, and form ownership.
  - Shared table header sort button call sites now match Midday's invoices/customers header ordering more closely: primary and generic sortable headers pass `label` and `sortField` before current sort state and `onSort`, while preserving the shared `CoreDataTableHeader`, sort-query behavior, horizontal pagination, sticky columns, DnD, resize handles, and all domain table-header consumers.
  - Shared bulk delete table actions now match Midday's transactions destructive bulk-action trigger more closely: `BulkClientDeleteAction` uses the reference `size="icon"` / `variant="ghost"` call shape, destructive hover class ordering, and explicit `Icon.Delete size={18}` while preserving the shared alert-dialog confirmation, disabled state, selected-count copy, and all selected-row table callers.
  - Blog create button call shape now follows Midday's create/open button ordering more closely: `CreateBlogPostButton` passes `variant` before `size` like the invoice/customer/add-transaction references, while preserving pending disabling, explicit button type, create mutation, route push, query invalidation, header icon usage, and empty-state text action.
  - Blog create action icon now matches Midday's create/add affordance naming more closely: `CreateBlogPostButton` uses the shared `Icon.Add` glyph instead of the local `Icon.PlusCircle` variant when the header asks for an icon, while preserving the blog create mutation, pending label, route push, query invalidation, header placement, and empty-state text action.
  - Invite openers now match Midday's member-invite trigger shape more closely: team, agent, and employee invite actions use text `Button` triggers instead of local outline icon-only `Icon.Users` buttons, while preserving Plot Keys' URL-backed global invite sheets, permission/header placement, invite form ownership, and success/cancel close behavior.
  - Builder template preview navigation now matches Midday's compact tracker/widget navigation references more closely: the builder back action uses the Midday-named `Icon.ArrowBack` affordance, and previous/next template buttons plus chevrons follow the reference `p-0 w-6 h-6 hover:bg-transparent ...` / `w-6 h-6` class ordering while preserving builder routing, template cycling callbacks, mobile picker, and theme toggle behavior.
  - Settings/edit trigger icons now match Midday's settings affordance naming more closely: builder sidebar settings and estate launch edit triggers use the Midday-shaped `Icon.Settings` glyph instead of the local `Icon.Settings2` variant, while preserving their existing outline button shells, mobile builder drawer behavior, URL-backed estate launch edit state, and `size-4` icon sizing used by nearby Midday settings controls.
  - Notification bell trigger chrome now matches Midday's notification-center trigger more closely: the header popover button uses the reference outline icon-button shell and explicit `Icon.Bell size={16}` call shape instead of the local ghost/p-0 trigger and `size-4` icon class, while preserving Plot Keys' unread count badge, screen-reader label, recent notification content, and popover placement.
  - Leave-request create open-sheet trigger now matches Midday's invoice/customer/tracker create-sheet references more closely: `OpenLeaveRequestSheet` renders the bare `Icon.Add` create affordance instead of a local calendar icon with `size-4`, while preserving the outline icon button shell, URL-backed `createLeaveRequest` state, and leave-request sheet ownership.
  - Shared table sort icons now match Midday's invoices/customers table-header references more closely: the shared `SortButton` renders active ascending/descending icons with explicit `size={16}` instead of local `size-4` classes, while preserving the ghost sort button, stop-propagation behavior, sort-field mapping, active sort state, and all domain table-header factories.
  - Shared search-filter trigger icon now matches Midday's invoice/transactions search-filter references more closely: the inline filter trigger renders bare `Icon.Filter` instead of a locally sized `size-4` icon, while preserving prompt state, filter menu visibility, active opacity, URL-filter updates, hotkeys, search input spacing, filter chips, and generic filter-list behavior.
  - Shared bulk table action buttons now match Midday's bottom-bar `SubmitButton` call shape more closely: `BulkClientAction` passes `isSubmitting`, `onClick`, then `disabled` before its local `variant` / `size` / `type` props, preserving the shared bulk action abstraction, explicit button type, small sizing, variant override, loading state, and all table bulk mutation callers.
  - Customer inline table action trigger and create-style open-sheet icons now match Midday's customers/open-sheet references more closely: the customer columns action cell uses the reference ghost `h-8 w-8 p-0` trigger with a `h-4 w-4` more icon and no local row `aria-label`, while create triggers for agents, appointments, customers, departments, estate creation, payroll entries, projects, and properties render the Midday-shaped bare `Icon.Add` inside the existing outline icon button; row actions, status changes, delete dialog, URL-param payloads, defaults, and domain-specific non-create icons are preserved.
  - Table actions-menu triggers now match Midday's invoices/orders action-menu references more closely across Agents, Appointments, Blog, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team: the row action trigger is the reference `Button variant="ghost" className="h-8 w-8 p-0"` with a `MoreHorizontal` icon using `h-4 w-4`, and the local per-row `aria-label` / `size-4` trigger residue was removed while preserving centered wrappers, stop-propagation boundaries, menu items, permission gates, mutations, invalidation, links, and row-specific action behavior.
  - Shared table core call shape now matches Midday's invoices/customers data-table references more closely: `useDashboardTable` orders the `useReactTable` options by the reference data/getRowId/columns/selection/core-model/state wiring sequence, and `CoreDataTableContent` now orders `DndContext`, `VirtualRow`, and fallback table-cell props like the Midday table bodies while preserving the shared Plot Keys table abstraction, row selection, virtualization, DnD, sticky columns, resizing, and domain-owned empty/no-results states.
  - Shared table bottom-bar implementation now reads closer to Midday's invoices bottom-bar reference: Plot Keys-only prop/component narration comments were removed while preserving the reference blur-layer comment, portal/motion structure, selected count text, deselect action, and injected bulk action slot.
  - Shared search-filter chips now match Midday's `FilterList` clear-icon naming more closely: the shared UI icon namespace exposes `Icon.Clear`, and the generic filter chip remove affordance uses it instead of `Icon.Close` while preserving the same glyph, hover reveal classes, chip class contract, date-range clearing behavior, and generic filter value rendering; unrelated close/dismiss controls intentionally remain on `Icon.Close`.
  - Shared search-filter chrome now matches Midday's invoice search/filter reference more closely: the shared `SearchFilter` keeps Plot Keys' generic filter-list contract while aligning `cn` import placement, submenu content prop order, checkbox item event/checked prop order, and the inline filter trigger's `onClick` / `type` / `className` call shape; the local hidden text child was removed from the filter trigger while search prompt state, escape/meta+s hotkeys, date-range filters, option toggling, filter chips, and URL-param updates are preserved.
  - Open-sheet trigger components now match Midday's `OpenCustomerSheet` / `OpenInvoiceSheet` trigger contract more closely: customers, properties, agents, appointments, departments, estates, team invites, employee invites, agent invites, leave requests, payroll entries, and projects use `variant="outline"` then `size="icon"` before their URL-param click handlers, no longer carry local `type="button"` / hidden-label children, and create triggers now use the Midday-named shared `Icon.Add` alias while preserving domain-specific URL payloads, property defaults, project className pass-through, and invite/edit icons.
  - Table/list route page stacks now match Midday's customers/invoices page-shell spacing more closely: Notifications, Employees, Leave Requests, Payroll, Departments, Blog, Projects, Team, Agents, Leads, and Appointments use `flex flex-col gap-6` for the main route body while preserving server guards, search-param loaders, table settings, prefetching, header/summary/invite composition, hydration, scrollable content, error boundaries, suspense fallbacks, and table skeletons.
  - Table header action groups now match Midday's mobile column-visibility behavior more closely: Customers, Properties, Agents, Blog, Employees, Appointments, Leave Requests, Projects, Departments, Leads, Payroll, Team, and Notifications keep column visibility visible in the action group on mobile, while create/invite/mark-all-read controls remain desktop-wrapped with `hidden sm:block`; permission gates, search/filter controls, tabs, sheet triggers, notification mutation behavior, and column visibility store wiring are preserved.
  - Shared virtual table row defaults now match Midday's local default boundary more closely: the extra exported `DEFAULT_NON_CLICKABLE_COLUMNS` surface was removed from `virtual-row.tsx`, and both `VirtualRow` and the Plot Keys-only `CoreDataTableContent` wrapper now use the same local `new Set(["select", "actions"])` default while preserving select/action non-clickability, row-click behavior, DnD, virtualization, sticky styling, and domain table factories.
  - Shared table header call shape now matches Midday's invoices/customers table-header references more closely: core primary-column `HorizontalPagination` props pass scroll callbacks before `className`, core `SortButton` uses `variant="ghost"` before the click handler, and `DraggableHeader` uses an explicit `size={14}` grip icon while preserving Plot Keys' shared icon namespace, sticky/DnD/resize behavior, sort mappings, and domain table-header factories.
  - Notification preference settings now match Midday's notifications settings component boundary more closely: the settings body maps each notification type to a dedicated `NotificationPreferenceSetting` row owner like Midday's `NotificationSetting`, while preserving Plot Keys' local taxonomy, tenant-backed preference query, email/in-app channels, pending-disabled checkboxes, and preference invalidation.
  - Integration settings save actions now match the migrated Midday settings-card `SubmitButton` call surface more closely by removing the remaining local `type="submit"` prop while preserving enclosing-form submission, pending spinner behavior, disabled state, mutation invalidation, and card footer layout.
  - Editable settings save buttons now match Midday's settings-card `SubmitButton` call surface more closely: local `type="submit"` props were removed from company name and primary market saves while preserving form submission, pending spinner behavior, disabled state, mutation invalidation, and card footer layout.
  - Settings read-only cell value typography now follows the migrated Midday settings/detail class contract more closely with explicit `text-sm font-semibold text-foreground`, preserving plan/status badges, billing upgrade link behavior, read-only labels, and disabled danger action behavior.
  - Shared settings/account secondary navigation now matches Midday's active-tab class contract more closely: `SecondaryMenu` uses the reference `text-primary font-medium underline underline-offset-8` order while preserving settings layout routing, tab labels, prefetch behavior, underline treatment, and the Plot Keys page-owned `ScrollableContent` wrapper.
  - Global sheets now mount after dashboard page content inside `DashboardChrome`, matching Midday's global overlay placement after children while remaining under `SiteNav.Provider` so Plot Keys global search keeps nav context; sheet registry entries, URL-state ownership, and page rendering behavior are unchanged.
  - Dashboard user-menu sign-out behavior now follows Midday's `SignOut` component boundary: `DashboardUserMenu` owns avatar/menu/profile/settings composition, while the local `SignOut` dropdown item owns session clearing, pending sign-out label, disabled duplicate-submit guard, `/sign-in` redirect, router refresh, and the `data-track="User Signed Out"` attribute.
  - Search modal footer arrow affordances now use Midday-named icon aliases: `Icon.ArrowUpward` and `Icon.ArrowDownward` were added centrally and used in the footer while preserving the underlying glyphs, footer layout, command-key strip, and existing `ArrowUp` / `ArrowDown` compatibility aliases.
  - Search modal footer chrome now matches Midday's icon-based footer more closely: a central `Icon.SubdirectoryArrowLeft` fallback was added and the footer return-key affordance now uses the shared icon instead of a local text glyph while preserving footer dimensions, border/background classes, logo mark, arrow-key affordances, and global search composition.
  - Shared `SiteNav` list structure now matches Midday's `MainMenu` wrapper shape more closely: `NavsList` owns the outer `mt-4 w-full` div and inner `nav.w-full` while preserving the flattened registry, deduping, active-state calculation, explicit item expansion, and mobile close behavior.
  - Shared `SiteNav` menu item chrome now follows Midday's `MainMenu` class contract more closely: item background rows, icon cells, label rows, chevron buttons, child-row transitions, and child-label utility order were aligned while preserving Plot Keys' generalized link registry, active-child detection, explicit expansion state, mobile close behavior, and fallback icon handling.
  - Shared mobile navigation trigger now follows Midday's `MobileMenu` call shape more closely: the outline/icon button prop order was aligned, the menu icon uses the reference `size={16}` call form, and `SheetContent` orders `side` before className while preserving Plot Keys' accessibility label, explicit button type, sr-only navigation title, shared nav list, and close-on-select behavior.
  - Dashboard header top-right chrome now matches Midday's `Header` / `UserMenu` class contract more closely: the action-group class order was aligned and the local `items-center` addition was removed, and the user-menu secondary label now follows Midday's `text-xs text-[#606060] font-normal` ordering while preserving notification, theme, avatar, settings, and sign-out behavior.
  - Shared sidebar shell class shape now follows Midday's `Sidebar` more closely: `SiteNav.Sidebar` uses the reference aside class grouping and main-menu wrapper ordering, the Plot Keys logo rail matches the reference logo strip order, and the local `text-sidebar-foreground` sidebar residue was removed while preserving shared nav expansion, delayed hover behavior, no-sidebar opt-out, logo placement, and nav rendering.
  - Dashboard Home skeleton ownership now follows Midday's overview skeleton grouping more closely: the one-use header/stat/section skeleton wrapper files were removed, and `dashboard/home/skeleton.tsx` owns the related loading placeholders together while preserving the existing header, stat-card, and section placeholder geometry.
  - Notification bell trigger ownership now sits in `NotificationBell`: the one-use trigger wrapper file was removed, and the popover owner directly renders the header button, icon, unread badge, and screen-reader label while preserving the content/item modules, unread count display, recent notification rendering, empty state, and notifications route link.
  - Dashboard user-menu ownership now matches Midday's `UserMenu` more closely: the one-use trigger and content wrapper files were removed, and `DashboardUserMenu` directly owns the avatar trigger, dropdown profile row, settings link, separators, and sign-out item while preserving Plot Keys' initials fallback, settings route, pending sign-out label, session clearing, sign-in redirect, and router refresh behavior.
  - Builder publish/recommendation modal header ownership now matches Midday modal references more closely: the no-prop header wrapper files were removed, and the dialog owners directly render `DialogHeader`, `DialogTitle`, and `DialogDescription` while preserving publish disabled chrome, summary/live-note modules, recommendation fields, profile summary, errors, footer actions, mutations, and close behavior.
  - Builder settings drawer header ownership now sits in `BuilderSidebarDrawer`: the no-prop `BuilderSidebarDrawerHeader` wrapper was removed, while the documented `BuilderSidebarDrawerContent` body split, URL-backed open state, left-side sheet shell, and builder controls are preserved.
  - Property detail ownership now matches Midday's detail component shape more closely: the one-use `PropertyDetailsHeader` wrapper was removed, and `PropertyDetails` directly owns its `SheetHeader`, not-found state, subtitle, featured/publish badges, and detail content while preserving publish-state badge variants, listing availability copy, and media/detail behavior.
  - Customer detail ownership now matches Midday's customer detail component more closely: the one-use customer detail header, unavailable, and row wrappers were removed, and `CustomerDetails` directly owns its `SheetHeader`, not-found state, status badge, formatted detail rows, and row layout while preserving loading behavior, status variant mapping, query placeholder data, date formatting, and sheet layout.
  - Customer edit sheet ownership now matches Midday's customer edit sheet more closely: the one-use `CustomerEditSheetHeader` wrapper was removed, and `CustomerEditSheet` directly owns its `SheetHeader`, action dropdown, and delete confirmation while preserving Plot Keys' delete pending state, disabled dialog actions, delete copy, cache invalidation, and URL close behavior.
  - Property details sheet ownership now matches Midday's customer details sheet shape more closely: the one-use `PropertyDetailsContent` wrapper was removed, and `PropertyDetailsSheet` directly owns `SheetContent` plus `PropertyDetails` while preserving detail URL close behavior and the 620px sheet width.
  - Customer create sheet ownership now matches Midday's customer create sheet more closely: the one-use `CustomerContent` wrapper was removed, and `CustomerCreateSheet` directly owns `SheetContent stack`, the stacked Midday-style header, `CustomerFormContext`, and `CustomerForm` while preserving close, cancel, success, and URL-param behavior.
  - Dashboard global search now follows Midday's search modal and global-overlay boundaries more closely: `search-modal.tsx` owns the `Dialog`, Midday's `meta+k` hotkey registration, conditional `Search`, and `SearchFooter`, the command/results body lives in `components/search/search.tsx`, `SearchModal` is mounted through `GlobalSheets`, and `GlobalSheetsProvider` now lives under the `SiteNav.Provider` dashboard shell instead of the app layout so search keeps its nav context; `OpenSearchButton` also matches Midday's explicit `size={18}` search-icon call shape, and the search body owns the Escape hotkey while preserving Plot Keys' SiteNav-derived results, tenant-aware routing, command labels, and icon namespace.
  - Remaining shared `Card` surfaces were rechecked against Midday's app-card and settings-card references: App Store/Integrations app tiles, Integration Settings, Notification Preferences, logo upload, and workspace settings remain intentional `Card` patterns, while settings and integration save actions now use the reference `isSubmitting`-before-`disabled` `SubmitButton` call shape and editable settings inputs keep their local class override before the autoComplete/autoCapitalize/autoCorrect/spellCheck block.
  - App Store and Integrations card surfaces were rechecked against Midday's `UnifiedAppComponent`; their shared `Card` anatomy remains intentional, and mapped app-card `Card` prop order plus outline action `Button` prop order now match the reference call shape while preserving app availability, plan-gated upgrade, enable toggles, integration configure/docs links, and skeleton layout.
  - Dev quick-fill helper chrome now uses the same plain `border border-border` shared-token strip as the migrated dev helper surfaces instead of a dashed panel, with compact preset button prop order aligned while preserving the production guard, preset labels, and fill callbacks.
  - Builder template picker initials now use the shared Midday-shaped `Avatar` / `AvatarFallback` contract instead of local square-avatar overrides, preserving template selection, locked-template state, usage counts, and labels.
  - Template Sandbox generated-websites empty state now uses a Midday-style centered app/connectors empty-state text hierarchy instead of a dashed no-data panel, and nearby clone/archive button prop order was aligned while preserving profile generation, configure/preview links, clone/archive mutations, and profile count behavior.
  - Agent pending invites now match the already migrated staff/team invite list shell by using the table-like `border-x border-b border-border` treatment, matching the heading wrapper, and restoring the destructive revoke action tone while preserving invite filtering, dev links, revoke mutation, and cache invalidation.
  - Remaining exact active dashboard `font-medium text-sm` typography residues and nearby `size`-before-`variant` button call residue were cleared across AI credits, billing, invite lists, notification preferences, property media, and blog empty actions while preserving copy, row data, invite revocation, media actions, and create-post behavior; the shared table core empty-state title intentionally keeps Midday's exact `font-medium text-lg` reference ordering.
  - Shared table core empty states now match Midday's exact `tables/core/empty-states.tsx` title class and default outline action button prop shape while preserving Plot Keys' optional custom action support for create actions and permission-gated empty states.
  - Active dashboard TSX source now has no raw `<select>` / `<option>` elements or `NativeSelect` usage after the focused form/project placeholder migration passes; remaining parity work can move beyond select primitives.
  - Project customer-access grant and notice former placeholder selectors now use controlled Midday-shaped shared `Select` controls with a local sentinel mapped back to the existing empty-string guard, preserving required-customer validation, email/customer labels, FormData payloads, cache invalidation, and post-success reset flow.
  - Project team member former placeholder selector now uses a controlled Midday-shaped shared `Select` with a local sentinel mapped back to the existing empty-string guard, preserving required-member validation, available-member filtering, role submission, assignment mutation, cache invalidation, and post-success reset flow.
  - Payroll entry employee selection now uses a controlled Midday-shaped shared `Select` with a local sentinel mapped back to the existing empty-string form value, preserving employee validation, disabled empty-employee submission guard, salary labels, payroll payload, summary/list invalidation, and form reset flow.
  - Leave request employee selection now uses a controlled Midday-shaped shared `Select` with a local sentinel mapped back to the existing empty-string form value, preserving employee validation, disabled empty-employee submission guard, request payload, cache invalidation, and form reset flow.
  - Project milestone optional phase selection now uses a controlled Midday-shaped shared `Select` with a local sentinel mapped back to `null`, preserving no-phase behavior, selected phase IDs, FormData submission, cache invalidation, and post-create form reset.
  - Appointment optional agent assignment now uses a controlled Midday-shaped shared `Select` with a local sentinel mapped back to the existing empty-string form value, preserving optional assignment semantics, create payload undefined conversion, appointment list/stat invalidation, and form reset flow.
  - Project create type selection now uses a controlled Midday-shaped shared `Select` with a local sentinel mapped back to the existing empty-string form value, preserving optional project type semantics, quick-fill compatibility, create payload null conversion, cache invalidation, and form reset flow.
  - Property form listing-type selection now uses a controlled Midday-shaped shared `Select` with a local sentinel mapped back to the existing empty-string form value, preserving optional listing-type semantics, land-listing conditional fields, create/edit payload null conversion, quick-fill behavior, and form reset flow.
  - Payroll entry month selection now uses a controlled Midday-shaped shared `Select`, preserving month labels, string month form values, numeric payload parsing, default/reset period values, payroll summary/list invalidation, and add-entry behavior.
  - Leave request type selection now uses a controlled Midday-shaped shared `Select`, preserving leave-type enum values, default annual type, label mapping, request payload, cache invalidation, and form reset flow.
  - Estate launch publish-state editing now uses a controlled Midday-shaped shared `Select`, preserving draft/published/archived enum values, estate default/reset values, update payload, cache invalidation, quick-fill compatibility, and launch save behavior.
  - Property form featured editing now uses a controlled Midday-shaped shared `Select`, preserving `"false"` / `"true"` schema values, default create/edit values, boolean payload conversion, quick-fill behavior, cache invalidation, and form reset flow.
  - Property form status editing now uses a controlled Midday-shaped shared `Select`, preserving active/sold/rented/off-market enum values, default active value, property create/edit payload, quick-fill behavior, cache invalidation, and form reset flow.
  - Project budget-line category creation now uses a Midday-shaped shared controlled `Select` inside the existing `FormField`, preserving budget category enum values, default other value, label mapping, budget-line payload, cache invalidation, reset, and router refresh flow.
  - Project workforce pay-basis creation now uses a Midday-shaped shared controlled `Select` inside the existing `FormField`, preserving pay-basis enum values, default daily value, label mapping, project worker payload, cache invalidation, reset, and router refresh flow.
  - Employee invite work-role selection now uses a controlled Midday-shaped shared `Select` instead of a native select, preserving the employee work-role value set, default operations role, label mapping, quick-fill adapter compatibility, and staff invite mutation payload.
  - Team invite role selection now uses a controlled Midday-shaped shared `Select` instead of a native select, preserving admin/agent/staff enum values, default staff role, descriptive option labels, quick-fill adapter compatibility, and team invite mutation flow.
  - Agent featured editing now uses a controlled Midday-shaped shared `Select` instead of a native select, preserving `"false"` / `"true"` schema values, default create/edit values, boolean payload conversion, quick-fill adapter compatibility, and agent create/update mutation flow.
  - Customer sheet status editing now uses the Midday-shaped shared controlled `Select` field instead of a native select, preserving active/VIP/inactive values, `FormField` validation ownership, create/update payload behavior, and sheet form reset/invalidation flow.
  - Project customer-access level grants now use the Midday-shaped shared `Select` primitive instead of a native select, preserving `name="level"`, default overview submission, access-level labels/options, and grant-access behavior alongside the separately migrated customer placeholder selectors.
  - Project team role assignment now uses the Midday-shaped shared `Select` primitive instead of a native select, preserving `name="projectRole"`, default viewer submission, role labels/options, and assignment behavior alongside the separately migrated membership placeholder selector.
  - Property media upload now uses the Midday-shaped shared `Select` primitive for media type and cover status instead of native selects, preserving `name="kind"`, `name="isCover"`, default submitted values, labels, options, and media upload behavior.
  - Project update type creation now uses the Midday-shaped shared `Select` primitive instead of a native select, preserving `name="kind"`, the default general update submission, labels, options, and project update creation behavior.
  - Project issue severity creation now uses the Midday-shaped shared `Select` primitive instead of a native select, preserving `name="severity"`, the default low severity submission, labels, options, and issue creation behavior; the later dashboard-wide audit confirmed no active dashboard TSX raw/native select residue remains.
  - Template Sandbox's controlled create-profile selectors now use the Midday-shaped shared `Select` primitive for template and plan selection while preserving form field names, default values, submitted values, catalog labels, and profile creation behavior; the later dashboard-wide audit confirmed no active dashboard TSX raw/native select residue remains.
  - Builder template preview header no longer depends on the local `ButtonGroup` primitive; previous/next template controls now use a Midday-style bordered flex shell with ghost compact icon buttons while preserving back navigation, template cycling callbacks, theme toggle, and picker behavior.
  - Shared dashboard search controls now match Midday's search-field and invoice-search-filter icon class surface more closely: the local explicit `size-4` token was removed from the absolutely positioned search icon while preserving query state, input props, filter menu behavior, and spacing.
  - Domain and billing muted metadata ordering was tightened where local panel helper text diverged from the migrated size-before-tone convention: domain status/helper copy and billing plan date metadata now place `text-sm/text-xs` before the raw Midday muted color token while preserving copy and layout.
  - Remaining auth/invite/estate page heading-label order now follows the migrated Midday typography shape: signup and verify-email side-panel labels use `text-sm font-medium ...`, and invite/estate page headings use `text-2xl font-semibold ...` while preserving copy, auth handoff behavior, invite states, and estate header content.
  - Estate launch/detail card headings and property detail analytics/media headings now follow Midday detail/value typography ordering (`text-base font-medium`, `text-lg font-medium`) while preserving estate plan/group content, upload behavior, property analytics values, media management, and sheet layout.
  - Billing plan-card typography now follows the Midday plan/detail value ordering more closely: current plan label, tier labels, and plan price use size-before-weight class order while tier labels, pricing, checkout behavior, current-plan disabling, and interval copy are preserved.
  - Active dashboard `tracking-normal` typography residue was removed from migrated page headers, public sign-in headings, summary/stat values, and project/domain/billing/integration/template headers after checking Midday dashboard references; copy, values, colors, routes, and layout structure remain unchanged.
  - Custom-domain panel headings now follow the Midday invoice/detail heading order more closely: domain control, DNS hostname, and hostname-intake labels use size-before-weight typography while keeping domain setup copy, DNS instructions, and provisioning behavior unchanged.
  - Remaining active dashboard error `Alert` call-shape residues were normalized where destructive alerts also carry local classes: onboarding and join-complete errors, invite revocation errors, domain provisioning/table errors, project mutation errors, and builder preview errors now use variant-first prop ordering while preserving variants, class strings, text, and behavior.
  - Editable settings card input/save-button shape was tightened again: company name and primary-market inputs now keep the local class override before the autoComplete/autoCapitalize/autoCorrect/spellCheck block, and settings save `SubmitButton` calls follow Midday's settings-card `isSubmitting`-before-`disabled` order.
  - Compact notification popover rows now use shared `cn` for unread-state classes and explicit side-before-token borders, with the popover header label in Midday's size-before-weight order; the onboarding checklist also moved from a template-string class join to shared `cn` while keeping current/done/upcoming state behavior unchanged.
  - Template Sandbox floating select popovers no longer carry stale shadcn-era `data-slot=select-viewport` selectors now that the shared select primitive follows Midday and does not emit those hooks; the same pass aligned remaining floating rail tiny labels and the notification count badge to Midday's size/weight/tone utility order without changing popper placement, mutation states, or count behavior.
  - Remaining active dashboard text/border ordering residues were reduced again: Estate Detail stage metrics, builder preview footer headings, and dashboard user menu secondary text now follow Midday's size/weight/tone utility order, while Analytics list separators now use side-before-token border ordering and retain the existing raw reference border colors and row behavior.
  - Active dashboard `Button` and shared `SubmitButton` call-shape residues no longer place `size` before `variant`; link/header actions, quick-fill, blog editor/detail actions, stacked sheet close controls, project action surfaces, export CSV, builder template preview navigation, and bulk table actions now follow the Midday `variant`-first button contract while preserving routes, mutations, disabled states, labels, and icons.
  - Development helper chrome now follows the same square bordered/shared-token treatment as the migrated dashboard: the dev FAB shell uses the shared `Button` primitive and neutral panel tokens, dev tenant/signup/quick-fill helper menus no longer use amber-only local chrome, and callbacks, tenant links, presets, guards, and toggle behavior are preserved. Estate Detail's land-price warning label now follows Midday's size/weight/tone class ordering while keeping its warning tone.
  - Remaining active dashboard `Badge` call-shape residues were cleared under `apps/dashboard/src/app` and `apps/dashboard/src/components`: report type/status, domain DNS type, customer/property detail, notification metadata/header, blog status, customer/property table, builder template, and flow shell chips now follow Midday's `variant`-first prop ordering while preserving all variants, labels, and status logic.
  - Remaining active dashboard `Button` call-shape residues were cleared under `apps/dashboard/src/app` and `apps/dashboard/src/components`: sign-in launch, estate launch assets, pricing-plan add, blog delete, builder picker, and shared bulk delete actions now follow Midday's `variant`-first prop ordering while preserving routes, labels, disabled states, destructive styling, refs, and mutations.
  - Blog detail header and edit-section text classes now follow Midday's detail/settings text ordering more closely, while preserving the blog editor layout, actions, status badges, and mutations.
  - Project and Estate detail/subpage headers plus reusable section headings now follow the same Midday detail/settings text ordering, while preserving badges, navigation, open-sheet actions, and mutation controls.
  - Domains, Connect Domain, Live Preview, and Integrations headers/sections now follow the same Midday page/detail text ordering while preserving links, badges, counts, setup content, and operational actions.
  - Billing, AI Credits, Dashboard Home, Analytics, Reports, and Template Sandbox headers/sections/cards now follow the same Midday page/detail text ordering while preserving tabs, actions, metrics, generated-profile behavior, and export controls.
  - Dashboard metric/stat card typography across Payroll, Blog, Estates, Projects, AI Credits, Customers, and Dashboard Home now follows Midday's label/value/meta class ordering while preserving counts, currencies, links, and budget variance color semantics.
  - Builder workspace/sidebar/template-preview labels and metadata now follow Midday's label/meta class ordering while preserving template selection, preview navigation, editable-field counts, and configuration summaries.
  - Remaining old-order dashboard text markers were cleared across builder, template sandbox, project workforce, billing, AI credits, domains, notification, invite, onboarding, sign-in, join, and property/detail surfaces so muted/destructive meta copy follows Midday's size-before-tone convention while behavior stays unchanged.
  - The last active dashboard `SheetTitle` / `SheetDescription` residue was removed by moving the builder settings drawer header to a Midday-style `SheetHeader` plus visible `h2`, and the remaining builder dialog roots now follow Midday's `open` before `onOpenChange` prop order while preserving drawer content, triggers, and mutation flows.
  - Remaining builder preview/editor chrome now follows Midday's plain bordered control treatment: preview page tabs, section hover labels, the focused field editor panel, template preview style/color controls, and the workspace configuration summary no longer carry local rounded wrappers, while focus rings, swatches, page navigation, and editable-field behavior are preserved.
  - Remaining non-search-footer dashboard border class ordering now follows Midday's side-before-token convention (`border-b border-border`, `border-t border-border`, `border-x border-b border-border`) across invite lists, report/project tables, project skeletons, builder sidebars/skeletons, and shared sidebar shells; the search footer remains unchanged because it already matches Midday's exact `border border-border border-t-[0px]` reference.
  - Remaining foreground/primary text class-order residues were cleared across estate detail, customer details, project workforce, flow shell, secondary navigation, dashboard publishing status, template sandbox controls, dev FAB, and notification rows so active label/value copy follows the same Midday size/weight/tone ordering as the rest of the migrated dashboard surfaces.
  - Dashboard Home quick actions now match Midday's widget quick-action anatomy more closely: actions are data-driven icon+label links using the shared icon namespace, reference `cursor-pointer group` action chrome, group-hover icon color, and centered wrapping while preserving the existing Plot Keys destinations.
  - Customer edit sheet action dropdown now follows Midday's edit-sheet menu prop order (`sideOffset={10}` before `align="end"`) while preserving the vertical dots trigger, delete confirmation, pending state, and destructive menu item styling.
  - The development pricing-plan QuickFill modal now uses Midday's compact `max-w-[455px]` dialog with an inner `p-4 space-y-4` body instead of the old wide `sm:max-w-xl` shell, while preserving every field, template update, fill action, and development-only guard.
  - Dashboard Home stat and connected-domain cards now follow Midday's `WidgetCard` shell contract with reference `h-full`, `cursor-pointer group`, and class ordering, and the stat skeleton ordering now matches Midday's overview skeleton while labels, counts, domain links, and empty-state content stay unchanged.
  - Dashboard Home connected-domains empty state wrapper now follows the same Midday overview widget-shell ordering (`border border-border p-5 min-h-[110px] flex ...`) while preserving the empty-state copy and domains link.
  - Compact builder modal summary cards now follow the same Midday-style border/background and title/meta class ordering: publish/recommend summaries use `border bg-white border-[#e6e6e6]` shells and explicit size/weight/tone title classes, and the publish live-note highlight uses explicit `text-xs font-medium text-foreground` while copy and modal behavior stay unchanged.
  - Builder/template control buttons now follow Midday's `variant`-first call shape across Template Sandbox shuffle/export actions, builder AI generation, builder drawer trigger, and focused-field editor actions while pending states, tooltips, drawer behavior, and save/fill mutations stay unchanged.
  - Shared `HorizontalPagination` table controls now match Midday's exact component shape more closely: import order, props interface, and `Button` prop order were aligned while Plot Keys' icon namespace and scroll behavior stay unchanged.
  - Another focused set of multiline shared `Button` calls now follows Midday's `variant`-first call shape across sign-in, sign-out, recommend-template, and domain-remove actions while labels, disabled states, destructive styling, and mutation behavior stay unchanged.
  - Remaining dashboard single-line `Button` prop-order residue was cleared where local calls placed `className` before `variant`; billing, builder template picker, notification bell, notification menu, and sign-in create-account actions now follow the Midday `variant`-first button call shape without behavior changes.
  - Shared dashboard `SearchField` now matches Midday's app/search-field setter and input prop shape more closely: local `void setSearch(...)` calls were removed and `autoComplete` / `autoCapitalize` ordering follows the reference while preserving Plot Keys' icon namespace and explicit icon sizing.
  - App Store app cards and skeleton now follow Midday's `UnifiedAppComponent` anatomy more closely: app-grid/card/header/content/action class ordering was normalized, enabled cards use the positive `Installed` pill, and local title-side `Available` / `Locked` status pills were removed while Plot Keys' plan-gated upgrade action and enable/disable toggle behavior stay intact.
  - Integrations overview grid and loading skeleton now match Midday's Apps grid density more closely: the local two-column layout moved to the reference responsive app grid, the skeleton renders 12 app-card shells, integration tile class ordering follows Midday's `UnifiedAppComponent` card anatomy, and the local "Not connected" title pill was removed in favor of Midday's positive installed-pill behavior while configure/docs behavior is preserved.
  - Integration settings credential inputs now match Midday settings-card input prop shape by using `spellCheck="false"` instead of the last boolean `spellCheck={false}` while preserving the existing settings form and mutation flow.
  - Remaining dashboard raw red utility tokens were replaced with Midday-style destructive tokens for error/negative states in project budget variance, the development tenant FAB error copy, and Template Sandbox floating field mutation-error chrome.
  - Template Sandbox floating configuration rail controls now follow Midday's square bordered control treatment: local rounded rail, row, icon-frame, and bottom-action button chrome was removed while expanded/collapsed behavior, tooltips, shuffle/live actions, and export links stay unchanged.
  - Settings danger-zone copy and disabled delete trigger now match Midday's danger-card surface more closely (`hover:bg-destructive text-muted` destructive trigger and "not reversible" wording) while preserving the current no-op disabled state until a workspace deletion flow exists.
  - Editable settings cards now match Midday's settings-card input/save-button shape more closely: company-name and primary-market inputs use the reference input prop ordering with `spellCheck="false"`, save actions use default `SubmitButton` sizing, and the logo card follows Midday's wrapper/avatar/image/input prop ordering while preserving Plot Keys upload and mutation behavior.
  - Settings read-only labels and workspace footer note now use Midday's `text-xs text-muted-foreground` class ordering while preserving Plot Keys billing/subdomain content.
  - Dashboard user menu rows now match Midday's plain text dropdown item pattern instead of icon-led local rows, while preserving the local `/settings` route, pending sign-out state, session clearing, redirect, and compact initials avatar trigger.
  - Shared search-filter dropdown root/content props now follow Midday's invoice search-filter ordering while preserving generic filter descriptors, chips, and date-range submenu behavior.
  - The standalone builder template picker now reuses the shared Midday-shaped builder picker button instead of a hand-rolled rounded desktop trigger, with reference dropdown spacing preserved.
  - Builder template, theme, font, and page dropdown selectors now let the Midday-matched shared radio-item primitive own item chrome instead of adding local rounded/padded radio-item classes, and their sidebar dropdown menus use explicit `sideOffset={10}` spacing.
  - Builder dropdown content props and font-group labels now match Midday selector/reference ordering and label styling more closely, while preserving grouped font options and save behavior.
  - The embedded Customers table row action menu now matches the Midday row-action wrapper/trigger/button contract used by standalone table action menus, while preserving edit/detail/status/delete behavior and the delete confirmation flow.
  - Customer edit sheet action menus now match Midday edit-sheet menu orientation by using a shared `Icon.MoreVertical` namespace fallback for the sheet action trigger, while row table action menus continue to use horizontal dots and existing delete confirmation behavior is preserved.
  - Shared `StackedSheetHeader` close controls now follow Midday's create/edit sheet close-button contract more closely with `size="icon"` and `variant="ghost"` on the button plus reference description class ordering, while preserving accessibility labels, button type, visible `h2` title ownership, and all stacked sheet callers.
  - Active stacked create/edit/invite sheet contents now match Midday's lean `<SheetContent stack>` prop surface instead of carrying Plot Keys-only `title` props; visible title ownership remains in `StackedSheetHeader`, with close controls, forms, URL close behavior, and stacking preserved.
  - Active create/edit/invite/drawer sheet roots now match Midday's `open={isOpen}` before `onOpenChange={...}` wrapper ordering across appointment, property, agent, employee invite, estate, customer, leave request, department, team invite, builder drawer, payroll entry, project, and agent invite sheets while preserving close behavior, content props, headers, and forms.
  - Property detail sheets now follow Midday's detail-sheet header/body pattern: generic `SheetTitle` / `SheetDescription` wrappers were removed, the visible `h2` / muted subtitle header uses the Midday detail layout, the body uses full-height `-mx-6` plus scrollable `px-6` content, and analytics/media management behavior is preserved. Customer/property detail sheet wrapper props now match Midday's `open` before `onOpenChange` ordering.
  - Builder publish/recommend dialogs now use Midday's compact modal shell (`max-w-[455px]` plus inner `p-4 space-y-4`) instead of the local `sm:max-w-md` direct-content layout, while preserving triggers, fields, mutation flow, errors, summaries, and footer actions.
  - Customer detail sheet headers now follow Midday's custom detail-sheet pattern instead of generic `SheetTitle` / `SheetDescription` wrappers: the body uses the full-height `-mx-6` sheet structure, the header renders visible `h2` / muted paragraph text, and the existing Plot Keys customer query/detail-row data contract is preserved without fake Midday-only enrichment or statement actions.
  - Analytics and Reports page headers now follow Midday's reports/metrics page shape more closely: Analytics no longer renders a non-reference workspace headline, Reports owns only a right-aligned `py-6` period-tab control row, and both skeletons no longer include removed page-headline placeholders.
  - Active table action menu wrappers now match Midday's invoice row action wrapper class contract (`flex items-center justify-center w-full`) and repeated ghost icon triggers use the Midday prop ordering while preserving accessibility labels, row stop-propagation, mutations, links, and destructive menu item styling.
  - Shared column visibility popovers now match Midday's popover body class ordering while retaining the generic table adapter, header-label fallback, and shared 450px max-height behavior.
  - Generic filter chips now use Midday's `little-date` `formatDateRange(..., { includeTime: false })` for populated date ranges, with `little-date` added to the dashboard package and existing generic end-key/remove behavior preserved.
  - Shared `DateRangeFilter` was rechecked against Midday and matches the preset select, two-month range calendar, date serialization, selected range, and calendar prop shape; import grouping was aligned, and the only remaining difference is blocked on a future Plot Keys `weekStartsOnMonday` user preference contract.
  - Shared search controls now match Midday search-field and invoice-search-filter anatomy more closely: wrapper/input class ordering, submit handler shape, and `spellCheck` props were aligned while preserving generic filters and explicit Plot Keys icon sizing.
  - `HeaderLinkTab` no longer carries the stale `variant` prop after all route-based tabs moved to the single Midday tab-strip visual contract; Billing and Reports callers were cleaned up accordingly.
  - Route-based header/status/period tab rows now use a shared `HeaderLinkTabList` Midday strip wrapper instead of separated `gap-2` tab groups, while preserving URL navigation, counts, labels, and Billing's savings copy.
  - Shared route-based `HeaderLinkTab` now uses Midday's tab trigger visual contract instead of the old inverted active pill, and Payroll period tabs now reuse it while preserving URL navigation and labels.
  - Shared UI `cn` imports now consistently use Midday's canonical `../utils` / `../../../utils` barrels across the remaining UI components and hooks; no `packages/ui/src` files still import from the old `lib/utils` compatibility path.
  - Shared `Button` now exposes Midday's size contract only (`default`, `sm`, `lg`, `icon`); the unused Plot Keys-only `xs`, `icon-xs`, `icon-sm`, and `icon-lg` variants were removed after active dashboard callers were migrated to explicit compact classes.
  - Active compact icon actions no longer rely on Plot Keys-only button size variants: notification bell, property pricing-plan remove, and bulk delete actions now use Midday-style explicit `h-8 w-8 p-0` icon button classes while preserving behavior.
  - Shared bottom-bar bulk actions now follow Midday's submit-action pending behavior: `BulkClientAction` renders through the shared `SubmitButton`, and Leave Requests, Leads, Notifications, and Payroll pass mutation pending state via `isSubmitting` while preserving bottom-bar chrome and selection clearing.
  - Shared core table headers now match Midday invoice table-header anatomy more closely: wrapper/row/primary-column/horizontal-pagination class strings were aligned, and sort indicators now use explicit `ArrowDown` / `ArrowUp` icon aliases instead of chevron aliases while preserving generic table configuration.
  - Active list page header rows now match Midday's `invoice-header` row class more exactly: Blog, Departments, Employees, Leave Requests, Notifications, Payroll, Projects, and Team removed local `gap-3` from `flex items-center justify-between` rows while preserving stacked tabs and action groups.
  - Dashboard header now includes a Midday-shaped global search trigger and store-backed search modal: `OpenSearchButton` uses the reference header button surface and command-key affordance, while `SearchModal` uses the Midday `Dialog` / transparent content / `search-container` / footer structure, searches the permission-filtered SiteNav registry, and navigates through the tenant-aware router from inside `SiteNav.Provider`.
  - Shared SiteNav main-menu behavior now follows Midday's flat `MainMenu` model: dashboard registry modules/sections are flattened for rendering, visible module/section headers are removed, child links expand by explicit item state instead of hover-delayed module expansion, and duplicate hrefs are deduped first-seen while preserving permissions and tenant links.
  - Shared SiteNav mobile navigation now matches Midday's `MobileMenu` sheet architecture with shared `Button`, `Sheet`, `SheetContent`, `Icon.Menu`, and `PlotKeysLogo` primitives; the desktop sidebar menu wrapper also now uses a plain Midday-style flex container instead of nested nav/sidebar-token scroll chrome.
  - Shared SiteNav item chrome now follows Midday main-menu item/child-item classes more closely: explicit light/dark active surfaces, text-primary hover emphasis, 40px icon cells with 20px icons, guide-line child links, and `NavsList` no longer owns extra overflow/bottom padding, while preserving generalized module/link behavior.
  - Shared `SiteNav.Sidebar` shell now matches Midday's sidebar container anatomy with `border-border border-r bg-background`, `items-center`, `pb-4`, and desktop left rounding, while preserving the shared nav model, hover expansion, children slot, logo rail, and nav item token behavior.
  - Settings secondary navigation was rechecked against Midday: shared `SecondaryMenu` is an exact package-renamed match, and the settings layout inner container now uses Midday's `max-w-[800px]` shape while retaining Plot Keys' page-owned `ScrollableContent` wrapper.
  - App Store tabs now match Midday's `apps-tabs` prop ordering and class-string anatomy while preserving Plot Keys imports, naming, query-state behavior, and labels; the adjacent App Store header and shared column visibility popover were rechecked against Midday and left aligned.
  - Active `Open*Sheet` trigger components now match Midday's `open-invoice-sheet` / `open-customer-sheet` wrapper anatomy with a containing `<div>` around the outline icon button, while preserving URL-state `setParams(...)` behavior, icon namespace usage, optional Project trigger `className`, and accessible labels.
  - Invoice-style list/table page header action groups now match Midday's `invoice-header` anatomy with `hidden sm:flex space-x-2` across Properties, Projects, Blog, Employees, Agents, Customers, Departments, Leads, Leave Requests, Payroll, Appointments, Team, and Notifications actions, while preserving search/filter components, status tabs, column visibility controls, permissions, and open/create/invite actions.
  - Shared `SearchFilter` now matches Midday's invoice filter empty-input behavior by clearing the full filter state when the search field is emptied, while keeping submit, Escape, chip removal, date-range, and option-selection behavior intact; the supporting helper/table audit also confirmed active `cn`, sticky, resize, header, select-column, virtual-row, and bottom-bar layers remain Midday-aligned or intentional domain-table generalizations.
  - Route-boundary exception audit rechecked authenticated `(app)` pages for `ScrollableContent`, `ErrorBoundary`, `Suspense`, and `HydrateClient`; the remaining hits are intentional settings-layout, redirect/control-flow, full-screen preview, or client-owned data route exceptions, and `joinClasses` remains outside the Midday dashboard pattern while `cn` is the shared UI utility.
  - Active table action menu triggers now match Midday's invoice action menu wrapper shape with `DropdownMenuTrigger asChild className="relative"` while preserving existing ghost icon buttons and row actions.
  - Destructive table/user dropdown actions now match Midday's class-based menu item pattern by using `className="text-destructive"` instead of the retired `DropdownMenuItem variant="destructive"` prop, while keeping destructive buttons and alerts unchanged.
  - Active shared `button`, `empty`, `native-select`, `field`, `input-group`, and builder `button-group` primitives now import `cn` from the canonical `../utils` path instead of the old `../lib/utils` compatibility shim, preserving their dashboard-facing behavior.
  - Shared stacked sheet headers now match Midday create/edit sheet header anatomy more closely with visible `h2` titles, row-centered header classes, Midday close-button class ordering, and class-based destructive customer edit menu styling.
  - Shared `sheet`, `dialog`, and `alert-dialog` primitives now import `cn` from the canonical `../utils` path like Midday instead of the old `../lib/utils` compatibility shim, preserving primitive behavior.
  - Shared `SearchFilter` now matches Midday's invoice filter layout, input/trigger class shape, and Escape behavior more closely by using the reference mobile stack class shape and clearing active input without local focus-gated state.
  - Shared `SearchField` now matches Midday's query-state boundary by retaining the reference-compatible `shallow` prop surface but using `useQueryState("q")` directly, preserving q-only header search behavior across App Store and table headers.
  - `OpenPropertySheet` and `OpenCustomerSheet` now render direct Midday-style icon-button triggers without no-op wrapper elements while preserving URL param behavior and accessible labels.
  - Project AI summary, risk-analysis, customer-draft, and no-risk feedback labels now use shared `@plotkeys/ui/icons` namespace icons instead of inline emoji glyphs while preserving action behavior and result rendering.
  - AI Credits top-up, Billing checkout, Publish Confirmation dialog, Recommend Template, Project AI action, and Domains operational error notices now use shared destructive `Alert` / `AlertDescription` surfaces instead of raw destructive text paragraphs while preserving purchase, checkout, publish, fallback, AI mutation, sync, DNS, and remove-domain behavior.
  - Agent, Employee, and Team pending invite revoke errors now use shared destructive `Alert` / `AlertDescription` notices instead of raw destructive text paragraphs while preserving invite revoke behavior.
  - Route and surface residue audit checkpoint: table/detail routes keep the migrated `HydrateClient` / `ScrollableContent` / `ErrorBoundary` / `Suspense` boundary shape, settings subroutes intentionally match Midday's route-level `Suspense` handoff under the shared settings layout, remaining non-builder shared `Card` imports are the verified App Store/Integration/Notification/Settings card patterns, and dashboard-owned `joinClasses`, direct `lucide-react`, and `DashboardTopbar` residues remain absent.
  - Active Blog create, Sign In benefit/dev picker, Integrations header/card/catalog, Notifications table, Notification Preferences skeleton, Dashboard user menu settings/sign-out, shared table header/drag handle, Template Sandbox visibility/template/config/live-code controls, Builder preview mobile menu, Dev FAB, Property Details media, App Store locked-plan action, Onboarding brand edit badge, Pricing Plan add/remove, Project Budget/Workforce remove, Billing plan/repair actions, and Estate Detail launch-asset icons now use the shared `@plotkeys/ui/icons` namespace instead of direct `lucide-react` imports; central fallbacks were added for missing action/control glyphs.
  - `OpenInviteAgentSheet` now uses the shared `@plotkeys/ui/icons` namespace instead of a direct `lucide-react` icon import, matching the other open/invite sheet triggers.
  - Dashboard header ownership now lives directly in `DashboardChrome` through `SiteNav.Header`; the one-use `DashboardTopbar` wrapper component was removed.
  - Reports period loading placeholders now mirror the squared period tab controls, and Join invite error states use shared destructive `Alert` surfaces instead of rounded destructive paragraphs.
  - Builder Template Preview inner website canvas frame and matching loading skeleton now use square plain bordered frames instead of rounded explicit `border-border` wrappers.
  - Onboarding property-type and content-readiness choice rows now use square bordered `bg-background` blocks instead of rounded label-box chrome while preserving checkbox wiring.
  - Estate Detail payment-plan mini table now uses a square plain bordered frame instead of a rounded explicit `border-border` wrapper.
  - Project Detail child panels for phases, milestones, customer access, team assignments, issues, updates, and generated AI outputs now use plain bordered surfaces instead of rounded local boxes.
  - Reports period tabs now reuse the shared `HeaderLinkTab`, and the shared muted tab variant no longer carries rounded tab chrome, aligning Reports and Billing interval tabs with Midday's square bordered tab treatment.
  - Final App Store, Integration overview, notification/integration settings, and workspace settings `Card` residues were rechecked against Midday's `unified-app`, `apps.skeleton`, and settings-card patterns and remain intentional reference-aligned surfaces.
  - Join invite not-found, expired, accept/sign-up, and profile-completion surfaces now use local plain bordered auth/status sections instead of shared `Card` primitives.
  - Shared FlowShell primary content column now uses a local plain `border bg-background` section instead of shared `Card` primitives, matching the already-flattened side panel.
  - Billing available-plan comparison tiles now use local plain bordered surfaces instead of shared `Card` wrappers, preserving current-plan highlight and checkout behavior.
  - Template Sandbox configure and generated-profile index panels now use local plain bordered form/article surfaces instead of shared `Card` / `CardContent` wrappers.
  - Domains DNS instruction panels and Connect Domain setup/guidance panels now use local plain `border bg-background p-5` surfaces instead of shared `Card` wrappers.
  - Estate Detail launch brief and offer image panels now use local plain bordered surfaces instead of shared `Card` / `CardContent` primitives, preserving image and payment-plan behavior.
  - Estate Detail feature and plan import item panels now use plain bordered surfaces instead of shared `CardHeader` / `CardTitle` / `CardContent` anatomy, while retaining the dashed upload treatment.
  - Estates launch list items now render as plain `border bg-background` articles instead of shared `Card` / `CardHeader` / `CardContent` item chrome.
  - Shared FlowShell side panels for signup, verify-email, and onboarding now use a plain `border bg-background` surface instead of rounded explicit `border-border` shell chrome.
  - Onboarding side-panel handoff and checklist tiles now use plain bordered row/info surfaces instead of rounded explicit `border-border` helper panels, with duplicated completed-row `line-through` cleanup.
  - Builder configuration summary panels now use plain `border bg-background` surfaces in the desktop sidebar and mobile drawer instead of rounded explicit `border-border` mini-card chrome.
  - Builder unavailable/error branches now render a single plain bordered `Empty` surface instead of wrapping the empty state in shared `Card` / `CardContent` chrome.
  - Active builder workspace structural shells now use plain `border bg-background` surfaces; transient editor overlay labels/pickers remain as intentional controls.
  - Builder template preview loaded/skeleton outer grid, sidebar, publish status, and preview frame now use plain `border bg-background` shells while preserving inner website canvas framing.
  - Unused NotificationDemo dashboard component was removed after scan-confirming no active imports; remaining app/settings card anatomy was compared against Midday and left intentional.
  - Active sign-in scoped-host notice now uses the flat light/dark info-tile surface.
  - Estate Detail offer-card and Template Sandbox generated-profile empty states now use plain section panels instead of shared card-content wrappers.
  - Estate Detail Launch assets now uses a plain side-panel surface inside Launch brief while preserving brochure and estate-plan actions.
  - Live Preview loaded and loading frames now share the plain `overflow-hidden border bg-background` surface instead of the old rounded/explicit `border-border` shell.
  - Shared signup/onboarding subdomain preview now uses a flat light/dark bordered info-tile surface instead of nested shared card anatomy.
  - Estate Detail grouped inventory now uses the plain table shell, and purchase pipeline stages now use flat light/dark bordered metric widgets.
  - Modal summary boxes and the onboarding side-panel company preview now use flat light/dark bordered info-tile surfaces instead of rounded generic shells.
  - Template Sandbox and Estates loading panels now use plain shared surfaces, and Estate launch inventory metric cells now use flat light/dark bordered widgets.
  - Blog Detail edit form now relies on `BlogDetailSection` for heading/description and uses a plain form body surface, with Dashboard Home section skeleton and Publishing URL tile aligned to migrated flat surfaces.
  - Domains control/intake bodies, provisioned-domain table shell, and domain loading panels now use plain shared surfaces while preserving DNS/setup item cards.
  - Billing current-plan and repair-payment section bodies now use plain shared surfaces, and Billing history now uses the plain table shell while preserving plan comparison item cards.
  - AI Credits lower section bodies, usage table shell, and matching skeleton panels now use plain shared card/table surfaces after the summary widgets were flattened.
  - Project Budget/Workforce outer list and table shells now use the plain shared table/card surface while preserving internal table borders, grouped rows, badges, and empty states.
  - Estate Detail offer cards now use the same flat Midday-style nested info tile surface for Payment plan that Launch brief uses for Location/Landmarks.
  - Estate Detail skeleton lower section bodies now use the shared plain card surface, and Launch brief Location/Landmarks info tiles use flat Midday-style widget panels.
  - Project Detail overview sections now rely on `ProjectSection` for heading/description and render each section body in a plain shared card surface, with the overview skeleton body panel aligned.
  - Project AI insights now relies on its parent `ProjectSection` for heading/description and renders its action group in a plain shared card surface without a duplicate inner card title.
  - Project Workforce add-worker and create-payroll-run surfaces now rely on `ProjectSection` for heading/description and use plain shared card surfaces for form bodies, with the form skeleton panel aligned.
  - Project Budget summary and add-line-item surfaces now rely on `ProjectSection` for heading/description, use plain shared card surfaces for form/content bodies, and render budget summary metrics as flat Midday-style widgets with matching skeleton placeholders.
  - Customers summary stats and their fallback now use direct flat Midday-style widget panels instead of `CardHeader` / `CardTitle` / `CardContent` metric cards.
  - Billing loading section panels now follow the shared Midday-style `Card` primitive surface (`border bg-background`) instead of the old rounded `border-border bg-background` shell.
  - AI Credits, Estates, Estate Detail, and Project Detail summary stat skeletons now mirror the migrated flat stat widget surface and remove stale icon/oversized-value placeholders.
  - Blog, Payroll, Estates, Estate Detail, Project Detail, and AI Credits summary stat tiles now use direct flat Midday-style widget panels instead of simple `Card` / `CardContent` metric wrappers.
  - Analytics and Reports loading skeletons now mirror the migrated flat panel surfaces instead of retaining rounded generic `border-border bg-background` fallback shells.
  - Analytics sections now follow Midday's flat metric/widget surface pattern: metric cards, chart/list panels, agent performance, and recent events use direct bordered panels with internal headings, while nested list rows use divided row treatment instead of rounded mini-cards.
  - Reports sections now follow Midday's flat report/widget surface pattern: business summary, agent performance, and listings performance use direct bordered panels with heading/action rows, and summary metric tiles no longer use nested rounded card styling.
  - Keep route files thin and compositional.
  - Split page-level header, summaries, table, columns, empty states, and loading states into feature-owned modules.
  - Dashboard header collapse/chrome now follows Midday's direct header placement and class shape through the shared `SiteNav.Header`, without an extra sticky topbar wrapper.
  - Dashboard user menu chrome now follows Midday's direct avatar trigger and compact `w-[240px]` dropdown shape while preserving PlotKeys settings/sign-out behavior.
  - Dashboard sidebar rail action ownership now follows Midday more closely: the rail keeps logo/navigation only, while sign-out remains in the avatar user menu instead of appearing as a duplicate sidebar control.
  - Global sheets follow Midday's dynamic provider pattern, and Customer/Property Details sheet content now uses the Midday `maxWidth: 620` plus `pb-4` shape without extra responsive width classes.
  - Shared sheet overlay chrome now matches Midday's soft light/dark overlay and desktop rounded treatment; stacked sheet title usage remains accessibility metadata matching Midday.
  - Shared sheet primitive implementation now follows Midday's alias, `forwardRef`, `sheetVariants`, inner content panel, header/footer/title, overlay, and portal export shape without the old non-Midday default close-button branch; existing `stack` call sites remain accepted without forking the primitive layout.
  - Shared dialog primitive implementation now follows Midday's alias, `forwardRef`, `hideClose`, frameless content, overlay, content animation, and header/footer/title/description shape without the old `DialogClose` export; dashboard modal cancel actions now close through modal-owned controlled state.
  - Shared alert-dialog primitive implementation now follows Midday's alias, `forwardRef`, soft overlay, content animation, header/footer/title/description, and `buttonVariants` action/cancel shape; old `size`, `AlertDialogMedia`, and `variant="destructive"` action usage has been removed from active delete confirmations.
  - Shared UI `cn` utility ownership now follows Midday's package path: `@plotkeys/ui/cn` exports from `packages/ui/src/utils/cn.ts`, with old top-level/lib utility files retained only as compatibility shims during the migration.
  - Shared button primitive now follows Midday's `forwardRef` / `ButtonProps` contract and default class surface without shadcn data attributes; PlotKeys-only size variants remain temporarily for active migrated dashboard call sites.
  - Shared badge primitive now follows Midday's `BadgeProps` / `HTMLDivElement` contract, reference variant classes, and tag variant vocabulary without the old shadcn `asChild` / data-attribute surface.
  - Shared input primitive now follows Midday's `forwardRef` / `InputProps` contract and reference class surface with autofill reset selectors, without the old shadcn `data-slot` and heavier local focus/invalid styling.
  - Shared textarea primitive now follows Midday's `forwardRef` / `TextareaProps` contract and reference class surface without the old shadcn `data-slot` and heavier local focus/invalid styling.
  - Shared label primitive now follows Midday's client `forwardRef` / `labelVariants` contract and reference class surface without the old shadcn `data-slot` and heavier local group-disabled styling.
  - Shared checkbox primitive now follows Midday's `forwardRef` contract, Radix `CheckIcon` indicator, and reference checked/focus class surface without the old shadcn `data-slot` and heavier local invalid/primary styling.
  - Shared switch primitive now follows Midday's client `forwardRef` contract and fixed root/thumb class surface without the old PlotKeys-only `size` API, shadcn data attributes, or group-data thumb sizing.
  - Shared radio-group primitive now follows Midday's client `forwardRef` root/item contracts and reference indicator dot markup without the old local `Icon.Circle`, shadcn data attributes, or heavier local invalid/dark styling.
  - Shared separator primitive now follows Midday's client `forwardRef` contract and explicit orientation branch classes without the old shadcn `data-slot` or data-orientation sizing classes.
  - Shared skeleton primitive now follows Midday's shimmer-gradient `animate` contract, backed by the `animate-shimmer` utility in UI globals instead of the old shadcn pulse/accent surface.
  - Shared spinner primitive now follows Midday's inline SVG `SpinnerProps` contract with numeric sizing and neutral stroke defaults instead of wrapping the local `Icon.Loader`.
  - Shared submit-button primitive now follows Midday's explicit `isSubmitting` / overlay-spinner contract instead of the old `useFormStatus` / `loadingLabel` server-action helper.
  - Shared progress primitive now follows Midday's `forwardRef` contract and reference `h-4 bg-secondary` track surface without the old shadcn data attributes or local rounded/primary tint styling.
  - Shared tabs primitive now follows Midday's direct Radix root plus `forwardRef` list/trigger/content contracts without the old local orientation wrapper, `tabsListVariants`, shadcn data attributes, or line-variant API.
  - Shared avatar primitive now follows Midday's client `forwardRef` root/image/fallback contracts plus `AvatarImageNext`, without the old local size API, badge/group exports, shadcn data attributes, or group-data size styling.
  - Shared table primitive now follows Midday's `forwardRef` table section/cell contracts and reference border/cell classes without the old shadcn data attributes or primitive-owned overflow wrapper.
  - Shared card primitive now follows Midday's `forwardRef` panel/header/title/description/content/footer contracts and reference card classes without the old shadcn data attributes, rounded shadow shell, or unused `CardAction` API.
  - Shared popover primitive now follows Midday's direct Radix root/trigger aliases and `forwardRef` content contract with `portal` support, without the old helper exports, shadcn data attributes, or bg-popover/rounded/origin styling.
  - Shared dropdown-menu primitive now follows Midday's direct Radix root/trigger/group/portal/sub aliases plus `forwardRef` content/item/label/separator contracts, with portal/container and `asDialogTrigger` support instead of the old shadcn data attributes, destructive variant API, or local icon indicators.
  - Shared tooltip primitive now follows Midday's direct Radix provider/root/trigger aliases and `forwardRef` content contract without the old shadcn data attributes, foreground bubble styling, transform-origin classes, or embedded arrow.
  - Shared select primitive now follows Midday's direct Radix root/group/value aliases plus `forwardRef` trigger/content/item/label/separator/scroll-button contracts, with `hideIcon` support instead of the old shadcn data attributes, size API, or local icon indicators.
  - Shared collapsible primitive now follows Midday's direct Radix root/trigger/content aliases without the old shadcn data attributes or local wrapper functions.
  - Shared accordion primitive now follows Midday's direct Radix root plus `forwardRef` item/trigger/content contracts, with `chevronBefore` and lucide chevron support instead of the old shadcn data attributes, local wrapper functions, or local icon abstraction.
  - Shared hover-card primitive now follows Midday's direct Radix root/trigger aliases and `forwardRef` content contract without the old shadcn data attributes, local wrapper functions, or rounded/bg-popover transform-origin styling.
  - Shared slider primitive now follows Midday's `forwardRef` root contract and fixed reference track/range/thumb structure without the old shadcn data attributes, dynamic value-derived thumb generation, or vertical-orientation styling branch.
  - Shared scroll-area primitive now follows Midday's `forwardRef` root/scrollbar contracts with `hideScrollbar` support, without the old shadcn data attributes, local wrapper functions, viewport focus-ring styling, or always-rounded thumb surface.
  - Shared context-menu primitive now follows Midday's direct Radix root/trigger/group/portal/sub aliases plus `forwardRef` content/item/label/separator contracts, with lucide indicators instead of the old shadcn data attributes, destructive variant API, local wrapper functions, or local icon abstraction.
  - Shared navigation-menu primitive now follows Midday's `forwardRef` root/list/trigger/content/viewport/indicator contracts plus direct Radix item/link aliases, without the old shadcn data attributes, viewport option, local link wrapper styling, or local icon abstraction.
  - Shared command primitive now follows Midday's `forwardRef` cmdk contracts and compact command-dialog chrome, without the old shadcn data attributes, search-icon input wrapper, dialog header metadata, or data-selected item styling.
  - Shared form primitive now follows Midday's `forwardRef` item/label/control/description/message contracts and `useFormContext` form-state lookup, without the old shadcn data attributes, `useFormState` subscription, grid item spacing, or data-error label styling.
  - Shared alert primitive now follows Midday's `forwardRef` root/title/description contracts and default/destructive/warning variant surface, without the old shadcn data attributes, grid alert layout, card background surface, or data-slot-dependent destructive styling.
  - Shared drawer primitive now follows Midday's `shouldScaleBackground` root contract, direct Vaul trigger/portal/close aliases, and `forwardRef` overlay/content/title/description contracts, without the old shadcn data attributes, multi-direction class branches, or group-data handle/header styling.
  - Shared calendar primitive now follows Midday's lucide chevrons, exact `../utils/cn` import, `2rem` cell sizing, bracketed cell-size tokens, and rounded-full range/day styling instead of the old local icon abstraction, spacing function tokens, square selected ranges, or focus-ring-heavy day buttons.
  - Shared currency-input primitive now follows Midday's thin `NumericFormatProps` contract with only `thousandSeparator` defaulted, while PlotKeys naira/no-decimal/non-negative behavior is explicit at the three dashboard money input callers.
  - Shared input-otp primitive now follows Midday's `forwardRef` root/group/slot/separator contracts with `SlotProps` and Radix `DashIcon`, without the old shadcn data attributes, context-index slot lookup, containerClassName extension, or local icon abstraction.
  - Shared carousel primitive now follows Midday's `forwardRef` root/content/item/previous/next contracts, `scrollTo` context helper, exported `useCarousel`, and carousel/slide ARIA metadata without the old shadcn data attributes or local arrow-key capture handler.
  - Shared combobox primitive now follows Midday's focused cmdk-backed `Combobox` and `Option` contract, without the old Base UI subcomponent suite, data-slot surface, chip helpers, or `useComboboxAnchor`.
  - Shared chart primitive now follows Midday's client `forwardRef` container/tooltip/legend contracts with typed payload props, display names, `../utils`, and reference Recharts class surfaces, without the old shadcn data-slot, payload type filtering, or font-mono tooltip value styling.
  - Shared toast infrastructure now exposes Midday-style Radix `toast`, `use-toast`, and `toaster` contracts with progress/footer/update support; the old Sonner export remains only as a compatibility shim because no active dashboard toast call sites exist yet.
  - Shared quantity and time-range input infrastructure now exposes Midday-style `quantity-input` and `time-range-input` contracts for future invoice/tracker-style form migrations, with only the missing Midday icon aliases adapted to PlotKeys' local icon namespace.
  - Shared combobox-dropdown infrastructure now exposes Midday's generic selector contract for future settings/account/category/customer picker migrations, composed from the migrated `Button`, `Command`, and `Popover` primitives.
  - Shared multiple-selector infrastructure now exposes Midday's default multi-tag selector contract for future tag/vault/settings migrations, including grouped options, creatable entries, async/sync search, fixed badges, and imperative focus/reset support.
  - Shared email-tag input infrastructure now exposes Midday's `email-tag-input` contract backed by shared `@plotkeys/utils` email-list validation/parsing helpers for future customer/contact form migrations.
  - Shared date-range picker and morph-feedback infrastructure now exposes Midday's date filter, animated size, text morph, text shimmer, submit-button morph, and resize-observer hook contracts for future filter, modal, onboarding, and import workflows.
  - Shared UI hooks now expose Midday's direct `@plotkeys/ui/hooks` barrel with `useMediaQuery`, `useEnterSubmit`, and existing PlotKeys hook exports for future responsive modal and textarea-submit migrations.
  - Dashboard generic page filters now support Midday-style date-range filter menus with preset selection, two-month range calendar selection, `start`/`end` filter state, range chip formatting, and paired range clearing for future date-aware table/page filters.
  - Leave Requests now uses the Midday-style date-range filter contract end to end: the page filter renders a `Leave date` range submenu, URL params carry `start`/`end`, tRPC accepts the range, and the DB query filters leave intervals by overlap with the selected range.
  - Projects now uses the Midday-style date-range filter contract end to end: the page filter renders a `Timeline` range submenu, URL params carry `start`/`end`, tRPC accepts the range, and the DB query filters project timelines by overlap with the selected range.
  - Appointments now uses the Midday-style date-range filter contract end to end: the page filter renders a `Schedule` range submenu, URL params carry `start`/`end`, tRPC accepts the range, and the DB query filters `scheduledAt` while composing with the existing upcoming view.
  - Blog now uses the Midday-style date-range filter contract end to end: the page filter renders a `Published date` range submenu, URL params carry `start`/`end`, tRPC accepts the range, and the DB query filters `publishedAt`.
  - Leads now uses the Midday-style date-range filter contract end to end: the page filter renders a `Captured date` range submenu, URL params carry `start`/`end`, tRPC accepts the range, and the DB query filters `createdAt`.
  - Employees now uses the Midday-style date-range filter contract end to end: the page filter renders a `Start date` range submenu, URL params carry `start`/`end`, tRPC accepts the range, and the DB query filters `startDate`.
  - Customers now completes the existing Midday-style customer `start`/`end` URL contract by exposing an `Added` date-range filter descriptor, accepting the range in `customers.get`, and filtering the visible `createdAt` / `Added` table column while keeping the Midday-matching simple customer header.
  - Notifications now uses the Midday-style date-range data contract end to end: URL params carry `start`/`end`, tRPC accepts the range, the DB query filters the visible created-time metadata via `createdAt`, and the table no-results branch treats unread/date/search as active filters.
  - Team now completes the Midday-style date-range data contract for the visible `Joined` column: URL params carry `start`/`end`, `team.listMembers` accepts the range, and the DB membership query filters `createdAt` while preserving the simple Midday-style q-only header.
  - App Store now follows Midday's Apps route/header/body split: route-owned `searchParams` feed feature-owned `AppStoreHeader` / `AppStoreTabs`, `q` and `tab=all|installed` drive `AppStoreView`, and the async body sits behind route-level `ErrorBoundary` / `Suspense` with `AppStoreSkeleton`.
  - App Store list composition now follows Midday's single app-grid flow: the local plan summary and category section wrappers are removed, empty states render inside the grid, and `AppStoreSkeleton` uses Midday's 12-card app loader shape.
  - App Store cards now follow Midday's app-store tile anatomy: top logo/status strip, compact title/status header, short description body, bottom action area, and Midday's `md` / `lg` / `2xl` responsive app grid.
  - App Store toggles now use Midday-style compact switch chrome without local inline error stacks while preserving server-context refresh after `apps.setEnabled`.
  - Live Preview now follows the Midday route/body boundary pattern: the route owns URL `searchParams`, `ScrollableContent`, and `ErrorBoundary` / `Suspense`, while `LivePreviewContent` owns session/data loading and `LivePreviewSkeleton` owns the loading chrome for the framed preview surface.
  - Connect Domain now follows the Midday route/body boundary pattern: the route owns the onboarded-session guard, `ScrollableContent`, and `ErrorBoundary` / `Suspense`, while `ConnectDomainContent` owns provisioning config readiness and `ConnectDomainSkeleton` owns loading chrome for the intake and DNS setup surface.
  - Settings notification and integration subroutes now mirror Midday's settings subroute handoff by wrapping the feature-owned settings list components in route-level `Suspense`, while keeping the root settings page on Midday's prefetch plus `HydrateClient` vertical stack shape.
  - Builder Preview now follows the migrated dashboard route-boundary pattern: the route wraps the client preview in `ErrorBoundary` / `Suspense`, and `BuilderTemplatePreviewSkeleton` owns the loading chrome for the sidebar, toolbar, publish strip, and framed template preview.
  - Settings root profile editing now follows Midday's discrete settings-card pattern: company name and primary market render as separate cards with save actions in `CardFooter`, while preserving the existing `workspace.updateCompanyProfile` API contract.
  - Settings root profile forms now use Midday-style shared form primitives and `SubmitButton` spinner-overlay submission for company name and primary market updates instead of local manual state plus button labels.
  - Settings logo editing now follows Midday's minimal `CompanyLogo` card pattern: the feature component owns the full card, header row, clickable square avatar target, hidden file input, spinner, and `CardFooter` guidance while using the existing `/api/upload` plus `workspace.setCompanyLogo` path.
  - Settings workspace and danger sections now follow Midday settings-card chrome: workspace uses shared card spacing plus a footer note, and the disabled destructive workspace action sits directly in a destructive card footer instead of a nested local panel.
  - Notification Preferences now follows Midday's grouped notification-settings pattern: notification types carry category/order metadata and render in accordion sections with category-header skeletons while preserving existing in-app/email toggle mutations.
  - Integrations overview cards now follow Midday's app-store tile structure: top logo/status strip, compact title header, short description body, and bottom full-width outline actions with matching skeleton anatomy.
  - Integration Settings now follows Midday settings-card footer ownership: the feature client form owns `CardContent` / `CardFooter`, renders bordered setting rows, and keeps the existing `workspace.updateCompanyIntegration` mutation contract.
  - Integration Settings now uses Midday-style shared form primitives and `SubmitButton` spinner-overlay submission instead of local manual input state plus saving-label buttons, while preserving blank-field disconnect semantics.
  - Dashboard Home widgets now follow Midday's simpler overview widget/action surfaces: stat cards are text-first bordered links, quick actions render as a compact action strip, and publishing status no longer uses decorative icon-card header chrome.
  - Dashboard Home connected-domain cards now use Midday-style text-first widget links and a plain bordered empty state instead of generic card-header/card-description chrome.
  - Billing current-plan and plan-comparison cards now follow Midday's plain padded subscription/plan-card surfaces instead of nested `CardHeader` / `CardDescription` chrome, while preserving checkout and repair-payment behavior.
  - Domains management and connect-domain cards now follow Midday-style plain padded surfaces instead of nested card-header/card-description chrome, amber DNS card tinting, decorative setup icons, or framed destructive DNS errors.
  - Settings company-name editing now matches Midday's 32-character `CompanyName` contract and company-or-department guidance while preserving the Midday settings-card header/content/footer anatomy.
  - Settings company-name validation now matches Midday's exact `min(2).max(32)` contract, and the logo card avatar spinner/image/fallback rendering now follows Midday's `CompanyLogo` props instead of PlotKeys-only spinner size, image padding, or fallback rounding.
  - Project detail forms previously moved raw native selects onto shared form primitives; the remaining dashboard parity pass is migrating safe no-placeholder and sentinel-backed cases onward to Midday-shaped shared `Select` while preserving mutations and field names.
  - Property and Estate Launch forms previously moved raw native selects onto shared form primitives; property listing type/status/featured and estate publish-state now use controlled Midday-shaped shared `Select` while preserving optional values, defaults, and payload conversion.
  - Template Sandbox configuration previously moved raw selects onto shared form primitives; template and plan-context selectors now use Midday-shaped shared `Select` while preserving create-form field names, defaults, and option values.
  - Project Budget and Workforce subpage mutation errors now use shared destructive `Alert` / `AlertDescription` primitives instead of raw destructive panels while preserving table placement and mutation behavior.
  - Blog detail status/delete notices now delegate to shared `Alert` / `AlertDescription` primitives instead of carrying local primary/destructive notice class variants.
  - Builder preview and workspace error notices now use shared destructive `Alert` / `AlertDescription` primitives instead of hand-rolled destructive panel classes.
  - Domains provisioned-table last-error cells now use shared destructive `Alert` / `AlertDescription` primitives instead of a hand-rolled destructive boxed cell.
  - Onboarding content-readiness form errors now use shared destructive `Alert` / `AlertDescription` primitives instead of raw destructive paragraph styling.
  - Blog saved notices and builder workspace status notices now use the shared default `Alert` surface instead of local primary-colored alert overrides.
  - Tenant onboarding and verify-email development shortcut alerts now use the shared default `Alert` surface instead of local primary-colored alert overrides.
  - Onboarding property-type and content-readiness checkbox controls now use the shared `Checkbox` primitive instead of raw native checkbox inputs with `accent-primary` styling.
  - Notification Preference channel controls now match Midday's per-channel `Checkbox` / `Label` pattern instead of local icon pill toggles with primary selected styling.
  - Notification Preference rows now follow Midday's `NotificationSetting` anatomy more closely with a left label/description block and right channel checkbox group, removing the extra inline type-code label.
  - Builder workspace toolbar onboarding status now uses the shared `Badge` secondary variant instead of custom primary-tinted badge classes.
  - Dashboard Home publishing control now uses a direct Midday-style bordered overview panel instead of nested `CardHeader` / `CardContent` card anatomy.
  - Template Sandbox configure form now uses a direct plain padded card instead of nested `CardHeader` / `CardDescription` / `CardContent` anatomy, while generated-profile cards remain framed list items.
  - Migrated table empty/no-results adapters now all use shared core empty-state chrome; domain adapters keep only copy, create actions, permission gates, URL params, and filter reset behavior.
  - Blog, Projects, Customers, and Properties empty/no-results adapters now use core empty-state chrome while preserving create actions, permission gates, URL params, and domain reset behavior.
  - Leads, Leave Requests, Notifications, and Payroll empty/no-results adapters now use core empty-state chrome while preserving status/unread/period copy and domain-specific filter reset behavior.
  - Shared table empty-state chrome now supports optional actions; Agents, Appointments, Departments, and Team empty/no-results adapters use core empty-state chrome while preserving domain copy and filter-clear hooks.
  - Shared table skeleton adapters now use `createTableSkeleton`; migrated skeleton files declare only columns and sticky-column config instead of repeating table-state defaults and `TableSkeleton` prop plumbing.
  - Existing one-to-one table-core files now keep Midday's import ordering for `cn` before UI primitives while preserving PlotKeys-specific package names and intentional shared-table abstractions.
  - Shared table runtime hooks follow the Midday reference placement: sticky columns, DnD, horizontal table scroll, infinite scroll, sort-toggle, and scroll-header behavior live under top-level dashboard hooks and are imported from `@/hooks`.
  - `useTableDnd` now follows Midday's simple `useTableDnd(table)` behavior; non-reorderable column policy stays in the shared table config/header filtering boundary.
  - PlotKeys table-core wrappers still compose those Midday-placed hooks where the migrated shared table architecture needs one typed runtime boundary.
  - Shared table infinite scroll now inherits Midday's default threshold of 20 rows, with Customers explicitly opting into the Midday customers threshold of 50 through `useDashboardTableRuntime`.
  - Shared table settings persistence follows the Midday reference placement: `use-table-settings` remains in top-level dashboard hooks and `update-table-settings-action` remains in top-level actions, while `useDashboardTableSettings` wraps the hook from table core for migrated column-id bootstrapping.
  - Shared table render content now renders table headers from a header component plus `contentRuntime` table-scroll state; migrated data tables pass `header={DataTableHeader}` instead of pre-rendering repeated header elements.
  - Shared table header adapters now use `createCoreDataTableHeader`; migrated table-header files declare only table id and primary-column config instead of repeating generic wrapper props.
  - Shared table draggable header rendering now matches the Midday invoices call-site shape, including the inner flex/overflow wrapper, prop ordering, non-reorderable header class shape, and `&&` resize-handle guards.
  - Shared table header label fallback now follows Midday by using `getHeaderLabel(columnId)` for sortable and non-sortable fallback labels instead of raw ids or cast TanStack header values.
  - Shared table resize-handle call sites now guard rendering with `header.column.getCanResize()` in both reorderable and non-reorderable header branches, matching the Midday invoices header shape while preserving the shared handle's internal null guard.
  - Shared table content and shell chrome now keep Midday's data-table class shape for table body ordering, scroll-container border utilities, and header-offset spacer markup.
  - Non-delete bulk-action tables no longer destructure `selectedCount` directly; selected-count display stays in `CoreDataTableShell` via `shellRuntime`, while delete-confirmation tables still pass counts to `BulkClientDeleteAction`.
  - Shared dashboard table runtime now returns a typed `shellRuntime` bundle for shell deselect, scroll-ref, and selected-count behavior; migrated data tables pass it into `CoreDataTableShell` instead of repeating shell runtime props.
  - Shared dashboard table runtime now carries table identity into `CoreDataTableContent`; migrated data tables no longer repeat content-level `tableId` props for DnD context ids.
  - Shared table render content now derives column order, sizing, visibility, and fallback column count from the TanStack table instance; migrated data tables pass `table={table}` instead of repeating column-state props.
  - Shared dashboard table runtime now returns a typed `contentRuntime` bundle for sticky/DnD/row/virtualizer render details; migrated data tables pass that bundle into `CoreDataTableContent` instead of exploding render-runtime props.
  - Single-action migrated table bottom bars now pass their bulk action directly into `CoreDataTableShell`; only Leads and Leave Requests retain fragments because they render multiple bulk actions.
  - Shared dashboard table runtime now also owns selected-row derivation; migrated data tables receive `clearSelection` and `selectedIds` from `useDashboardTableRuntime`, while only delete-confirmation tables also receive `selectedCount`.
  - Shared table shell now owns selected-row bottom-bar visibility from `selectedCount`; migrated data tables no longer derive or pass `showBottomBar`.
  - Shared dashboard table runtime now lives in `useDashboardTableRuntime`; migrated data tables pass table id, table instance, paging state, scroll ref, and store column setter into table core instead of owning repeated column sync, sticky/DnD, virtualizer, scroll-ref, infinite-scroll, row-height, and row-model wiring.
  - Shared table render content now derives DnD context ids from the table id carried in `contentRuntime`; migrated data tables no longer own repeated `<tableId>-table-dnd` strings or content-level `tableId` props.
  - Shared dashboard table settings bootstrap now lives in `useDashboardTableSettings`; migrated data tables pass table id, initial settings, and resolved columns into table core instead of owning repeated `getColumnIds` / `useTableSettings` wiring.
  - Shared table bulk-action button chrome now lives in `BulkClientAction`; Leads, Leave Requests, Notifications, and Payroll pass only domain labels, variants, pending state, and action callbacks instead of owning repeated `Button` defaults.
  - Migrated table bulk-action callbacks now stay above empty/no-results returns so Agents, Appointments, Departments, Employees, Notifications, Payroll, and Team preserve stable hook ordering across empty and populated states.
  - Shared table row-click guard policy now lives in table core; migrated data tables rely on the default `select` / `actions` non-clickable column set instead of declaring identical local constants.
  - Shared table shell chrome now lives in `CoreDataTableShell`; migrated data tables pass table content and bottom-bar action children into the core shell instead of owning repeated scroll container, header-offset spacer, `AnimatePresence`, and `BottomBar` wrapper markup.
  - Shared table render content now lives in `CoreDataTableContent`; migrated data tables pass their header, row data, row-click behavior, and column count into the core component instead of owning repeated DnD/table-body/virtual-row render shells.
  - Shared dashboard table instance setup now lives in `useDashboardTable`; migrated data tables pass domain columns/data/settings/selection/meta into the core hook instead of owning repeated TanStack `useReactTable` construction defaults.
  - Shared table column runtime now lives in `useTableColumnRuntime`; migrated data tables pass table id, table instance, and column visibility into the core hook instead of owning repeated DnD, sticky-column, horizontal-scroll, and sticky-config lookup wiring.
  - Shared table row-selection state now lives in `useTableRowSelection`; migrated data tables pass row-selection store state into the core hook instead of owning repeated selected-id/count/bottom-bar visibility/deselect derivation.
  - Shared table virtualizer setup now lives in `useTableVirtualizer`; migrated data tables pass row count, row height, and scroll ref into the core hook instead of owning repeated TanStack virtualizer construction.
  - Shared table infinite-scroll wiring now lives in `useTableInfiniteScroll`; migrated data tables pass query/virtualizer state into the core hook instead of owning repeated `loadMore` adapters and raw `useInfiniteScroll` calls.
  - Agents table rows now use Midday-style sheet-backed row interaction where supported: non-action cells set URL-owned `agentId` and open the globally mounted `AgentEditSheet`; create/invite-only domains remain without row-click navigation until detail/edit sheets exist.
  - Shared table scroll-container ref bridging now lives in `useTableScrollContainerRef`; migrated data tables call the core hook instead of owning repeated `MutableRefObject` casts.
  - All migrated table data tables now wire `useScrollHeader` to their scroll container; Customers and Properties use summary-grid offsets for Midday-style header/summary collapse behavior.
  - Shared table column synchronization now lives in `useTableColumnSync`; migrated data tables call the core hook instead of owning their own `setColumns(table.getAllLeafColumns())` effect.
  - Shared column visibility chrome now lives in `CoreColumnVisibility`; domain column visibility modules only read their store columns and pass them into the core control.
  - Shared table header machinery now lives in `CoreDataTableHeader`; domain table-header modules are thin adapters that declare table id and primary sticky-column sort config.
  - Notifications header now follows the server-composition header boundary; unread-count action behavior lives in focused client child `NotificationsActions`, unread filter tab state lives in `NotificationsFilterTabs`, and q-only search uses the shared `SearchField`.
  - Appointments header now follows the server-composition header boundary; status/upcoming tab querying, stats normalization, and active-state derivation live in focused client child `AppointmentsStatusTabs`.
  - Leave Requests header now follows the server-composition header boundary; status tab querying and active-state derivation live in focused client child `LeaveRequestsStatusTabs`.
  - Employees header now follows the server-composition header boundary; status tab querying, active-state derivation, and department-aware tab href construction live in focused client child `EmployeesStatusTabs`.
  - Leads header now follows the server-composition header boundary; status tab querying and active-state derivation live in focused client child `LeadsStatusTabs`.
  - Projects header now follows the server-composition header boundary; status tab querying and active-state derivation live in focused client child `ProjectsStatusTabs`.
  - Blog header now follows the server-composition header boundary; status tab querying and active-state derivation live in focused client child `BlogStatusTabs`.
  - Payroll header now follows the server-composition header boundary; period tab querying/rendering lives in focused client child `PayrollPeriodTabs`.
  - Team header now follows the server-composition header boundary; invite cap checking lives in focused client child `TeamInviteAction`.
  - Customers header now matches Midday's plain server-composition header boundary with no unnecessary `async` wrapper.
  - Departments header now matches Midday's server-composition header boundary: it arranges search, column visibility, and open-sheet controls without owning a client boundary.
  - Agents, Departments, Team, and Payroll headers now use the shared Midday-style `SearchField` directly for q-only search, and their redundant q-only `DashboardSearchFilter` wrapper files have been removed.
  - Active dashboard table live queries now reuse the same feature-owned `resolve*ListInput()` helpers as route prefetches, including deferred client search overrides, so table files no longer own status/filter/list-query normalization.
  - Properties route and table now use `resolvePropertyListInput()` for server prefetch and client deferred-search list query input construction, leaving filter spreading isolated inside the property filter module.
  - Blog, Appointments, Leave Requests, Projects, Notifications, Payroll, Team, Agents, and Departments now use feature-owned `resolve*ListInput()` helpers for list query input construction, leaving route files focused on session, params, prefetch, settings, and composition.
  - Leads filter normalization now uses `resolveLeadListInput()` for status validation and list input construction, leaving the route focused on data handoff and composition.
  - Employees filter normalization now uses `resolveEmployeesListInput()` for status validation, department trimming, and list input construction, leaving the route focused on data handoff and composition.
  - Team, Agents, and Employees invite-capable routes now use `getWorkspaceInviteContext()` for invite base URL and development-preview state, leaving route files focused on data handoff and composition.
  - Customers and Settings routes now reuse `components/workspace/workspace-access.ts` for customer-management and workspace-settings edit role policy, leaving route files focused on data handoff and composition.
  - Team, Agents, and Employees invite-capable routes now reuse `components/team/team-access.ts` for workspace-member management role checks and invite development-preview detection, leaving routes focused on data handoff and composition.
  - Billing callback subscription-tier metadata validation now uses the billing-owned `isBillingSubscriptionTier` helper, leaving the callback route focused on Paystack verification, activation, revalidation, and redirects.
  - Billing interval URL normalization, plan-tier/status normalization, and shared billing plan/status types now live in `billing-utils.ts`, leaving the route and content modules focused on handoff/query composition.
  - Dashboard Home route now delegates builder configuration bootstrap, request-header/cookie caller construction, and active-draft redirect decisions to a dashboard-home server helper, leaving the route focused on session, live-site URL, prefetch, and composition.
  - Dashboard Home loading state now splits header, stat-card, and section-panel skeleton chrome into focused dashboard-home skeleton modules, leaving `DashboardHomeSkeleton` focused on fallback composition.
  - Dashboard Home now splits publishing control, quick actions, connected domains, stat cards, and overview types into focused dashboard-home modules, leaving `DashboardHome` focused on query and composition.
  - Builder workspace changed-field counting and template access/lock/read-only state derivation now lives in `builder-workspace-state.ts`, leaving `BuilderWorkspace` focused on async data loading, unavailable-state branching, preview data resolution, and composing layout/sidebar/toolbar/preview modules.
  - Builder sidebar theme update mutation, router refresh, local theme error handling, `FormData` parsing, and silent-save fallback now live in `use-builder-sidebar-theme-actions.ts`, leaving `BuilderSidebarControls` focused on deriving active template image slots and composing focused control modules.
  - Builder template draft creation failures now render locally inside `TemplatePicker` through `useBuilderTemplateSelection` instead of writing builder route-level `?error=` state, while successful draft navigation still owns `configId` and page URL state.
  - Builder preview field update and smart-fill failures now render inside the preview shell through `useBuilderPreviewActions` local state instead of writing builder route-level `?error=` state, while successful mutations still refresh route data.
  - Builder sidebar image, section visibility, and SEO field state/save plumbing now lives in `use-builder-sidebar-section-fields.ts`, with shared section labels in `builder-section-labels.ts`, leaving `builder-sidebar-sections.tsx` focused on sidebar field presentation.
  - Builder sidebar template/page picker selection now lives in `use-builder-template-page-selection.ts`, leaving `TemplatePicker` and `PagePicker` focused on picker trigger, tab/menu layout, option rendering, access badges, and selected-value display.
  - Builder preview register shell now splits class resolution, header/mobile navigation chrome, and footer chrome into focused register-shell modules, leaving `PreviewRegisterShell` focused on register config lookup, render-year derivation, and header/content/footer composition.
  - Builder sidebar theme picker optimistic selection and save plumbing now lives in `use-builder-theme-selection.ts`, leaving style preset, color system, and font picker components focused on dropdown trigger/menu presentation and option rendering.
  - Builder preview section editor field state now lives in `builder-preview-field-editor.tsx`, and section label/field prefix metadata now lives in `builder-preview-section-fields.ts`, leaving `PreviewSection` focused on focus/keyboard behavior, hover/edit-state chrome, section component rendering, and composing focused field editors.
  - Builder workspace preview data resolution now lives in `builder-workspace-preview-data.ts`, leaving `BuilderWorkspace` focused on async workspace data loading, unavailable-state branching, changed-field counting, template access derivation, and composing focused sidebar/toolbar/preview modules.
  - Builder preview runtime provider branches now live in `builder-preview-template-runtime.tsx` and `builder-preview-website-runtime.tsx`, leaving `BuilderPreviewRuntimeBody` focused on preview scroll container layout, frame class/style derivation, and choosing the template-page vs website section runtime branch.
  - Builder preview section-list rendering now lives in `builder-preview-section-list.tsx`, leaving `BuilderPreviewRuntimeBody` focused on scroll container layout, Registry/Website runtime provider selection, template-page vs section-list branching, and guarded frame composition.
  - Builder preview guarded frame composition now lives in `builder-preview-guarded-frame.tsx`, leaving `BuilderPreviewRuntimeBody` focused on scroll container layout, Registry/Website runtime provider selection, template-page vs section-list branching, and section rendering data wiring.
  - Builder preview canvas/framed container classes and frame-header/read-only/body ordering now live in `builder-preview-shell.tsx`, leaving `BuilderPreviewPanel` focused on action/routing/presentation hook composition, section focus state, preview chrome data inputs, and runtime body wiring.
  - Builder preview register-template lookup, template-page handle resolution, family override resolution, visible-section filtering, content mapping, and register shell wrapping now live in `use-builder-preview-presentation.tsx`, leaving `BuilderPreviewPanel` focused on action/routing hook composition, section focus state, preview chrome branching, and runtime body wiring.
  - Builder preview field update, smart-fill mutation, local preview error handling, and inline editable-text action adapters now live in `use-builder-preview-actions.tsx`, leaving `BuilderPreviewPanel` focused on preview routing, section focus state, register-template/page resolution, content/section derivation, shell chrome composition, and runtime body wiring.
  - Builder workspace notice alert chrome now lives in `builder-workspace-notice-alerts.tsx`, leaving `BuilderWorkspaceNotices` focused on notice state composition and status-message derivation.
  - Builder workspace root spacing, responsive sidebar/content grid, and preview-column wrapper now live in `builder-workspace-layout.tsx`, leaving `BuilderWorkspace` focused on server data loading, unavailable-state branching, template/page/live URL derivation, lock/read-only derivation, presentation resolution, and focused module composition.
  - Builder workspace toolbar context now lives in `builder-workspace-toolbar-context.tsx`, leaving `BuilderWorkspaceToolbar` focused on the outer toolbar shell and composing focused context/actions modules.
  - Builder workspace toolbar actions now live in `builder-workspace-toolbar-actions.tsx`, leaving `BuilderWorkspaceToolbar` focused on outer toolbar layout, mobile drawer placement, current page/embedded context labels, and passing action state into the focused action module.
  - Builder mobile sidebar drawer body composition now lives in `builder-sidebar-drawer-content.tsx`, leaving `BuilderSidebarDrawer` focused on URL-backed open state, trigger/header/content composition, `SheetContent` shell configuration, and close behavior.
  - Builder mobile sidebar drawer trigger button/icon chrome now lives in `builder-sidebar-drawer-trigger.tsx`, leaving `BuilderSidebarDrawer` focused on URL-backed open state, sheet composition, summary/control/editable-fields wiring, and close behavior.
  - Builder workspace desktop sidebar now reuses `BuilderSidebarSectionGroup` for AI content and onboarding tool groups instead of keeping repeated section-title chrome in `BuilderWorkspaceSidebar`.
  - Builder sidebar control stack/item wrappers now live in `builder-sidebar-control-stack.tsx`, leaving `BuilderSidebarControls` focused on theme mutation ownership, local error display, template lookup, read-only branching, and wiring focused picker/section modules.
  - Builder sidebar image-slot, section-visibility, and SEO controls now share `builder-sidebar-section-group.tsx` for section title and field-label chrome, leaving `builder-sidebar-sections.tsx` focused on optimistic values, debounced saves, visibility toggles, and theme-key payload construction.
  - Builder sidebar picker trigger chrome now lives in `builder-sidebar-picker-button.tsx`, with style preset, color system, font, template, and page pickers consuming `BuilderSidebarPickerButton` so theme pickers no longer own shared sidebar trigger/chrome concerns.
  - Builder sidebar template selection now reuses the shared builder sidebar picker trigger and removes its duplicated local chevron helper/import, keeping template selection, tier badge display, draft creation, URL replacement, and page picker behavior intact while centralizing picker trigger chrome.
  - Builder AI/onboarding tools now split shared action button, pending icon, error text, description text, and result-message chrome into `builder-ai-tool-control.tsx`; `onboarding-tools.tsx` owns mutation state, router refresh, page-label derivation, and result-message derivation while using icon-backed ASCII labels.
  - Builder sidebar controls now split read-only upgrade alert chrome and plan-label fallback copy into `builder-sidebar-read-only-notice.tsx`, leaving `BuilderSidebarControls` focused on control composition, template lookup, workspace theme mutation ownership, local error display, named image-slot derivation, and save callback wiring.
  - Builder floating config panel dead path has been removed after scans found no active imports/usages; active builder configuration ownership stays centered on `BuilderWorkspaceSidebar`, `BuilderSidebarDrawer`, and focused builder template preview modules.
  - Builder template preview now splits mobile template picker/header action bar, back-to-builder control, theme toggle, previous/next template buttons, current page label, and published-toggle status row into `builder-template-preview-header.tsx` and `builder-template-publish-status.tsx`, leaving `BuilderTemplatePreview` focused on local preview state, template/page selection, publish-toggle demo state, preview presentation resolution, and high-level sidebar/header/status/frame composition.
  - Builder template preview now splits desktop sidebar shell, style preset grid, color-system swatches, and preview info rows into `builder-template-preview-sidebar.tsx`, leaving `BuilderTemplatePreview` focused on local preview state, template/page selection, publish-toggle demo state, preview presentation resolution, mobile picker/header actions, and high-level sidebar/frame composition.
  - Builder template preview now splits duplicated desktop/mobile template picker dropdown trigger, tier tabs, catalog grouping, selected-template menu chrome, browser-bar preview chrome, frame styling, section count display, and section-component rendering into `builder-template-picker.tsx` and `builder-template-preview-frame.tsx`, leaving `BuilderTemplatePreview` focused on local preview state, template/page selection, publish-toggle demo state, preview presentation resolution, and high-level composition.
  - Builder workspace now splits builder error/success/onboarding notices, locked-template upgrade alert chrome, and repeated unavailable/empty-state card rendering into `builder-workspace-notices.tsx` and `builder-workspace-unavailable.tsx`, leaving `BuilderWorkspace` focused on status branching, data loading, template/page/live URL derivation, lock-message derivation, preview presentation resolution, and focused module composition.
  - Builder workspace now splits desktop website-configuration sidebar composition and preview toolbar action chrome into `builder-workspace-sidebar.tsx` and `builder-workspace-toolbar.tsx`, with shared builder sidebar summary/editable-fields note modules owning configuration/status/count presentation variants; `BuilderWorkspace` stays focused on server data loading, template/page resolution, live-site URL derivation, lock/notice derivation, preview presentation resolution, and focused module composition.
  - Builder preview now splits preview scroll/body layout, Registry/Website runtime provider branching, ClickGuard/SmartFill/InlineOverview composition, template-page rendering container, and section-list rendering into `builder-preview-runtime-body.tsx`, leaving `BuilderPreviewPanel` focused on mutation error handling, preview routing hook composition, template/page handle derivation, section/content derivation, shell chrome composition, and focused-section state.
  - Builder preview now splits internal href normalization, preview page resolution, page-query href building, preview page navigation, register link click interception, and the registry link bridge component into `use-builder-preview-routing.tsx`, leaving `BuilderPreviewPanel` focused on mutation error handling, registry/runtime providers, preview shell/body composition, and focused-section state.
  - Builder preview now splits framed-preview browser dots, page navigation tabs, preview URL/status text, and read-only upgrade banner chrome into `builder-preview-frame-chrome.tsx`, leaving `BuilderPreviewPanel` focused on preview routing/link behavior, registry/runtime providers, field mutations, preview body composition, and focused-section state.
  - Builder preview now splits register preview header/nav/footer rendering, menu radius/header/nav/CTA class resolution, register nav/footer config lookup, and mobile menu icon chrome into `builder-preview-register-shell.tsx`, leaving `BuilderPreviewPanel` focused on preview routing/link resolution, registry/runtime providers, field mutations, URL error handling, preview body composition, and focused-section state.
  - Builder preview now splits section label mapping, editable-field filtering, inline field editor UI/state, section hover/focus chrome, and section component fallback rendering into `builder-preview-section.tsx`, leaving `BuilderPreviewPanel` focused on preview routing, registry/runtime providers, field mutations, URL error handling, preview shell composition, and focused-section state.
  - Builder sidebar controls now split template selection, template draft creation/navigation, template usage/tier display, page selection, page inventory lookup, and picker dropdown chrome into `builder-sidebar-template-page-pickers.tsx`, leaving `BuilderSidebarControls` focused on control stack composition, read-only messaging, theme mutation ownership, URL error handling, named image-slot derivation, and save callback wiring.
  - Builder sidebar controls now split style preset, color system, font picker dropdown chrome, shared picker trigger, theme option constants, and optimistic theme-selection save helper into `builder-sidebar-theme-pickers.tsx`, leaving `BuilderSidebarControls` focused on template/page routing, read-only messaging, mutation ownership, URL error handling, and picker composition.
  - Builder sidebar controls now split image-slot inputs, section visibility toggles, SEO fields, section label mapping, and their local optimistic/debounced save state into `builder-sidebar-sections.tsx`, leaving `BuilderSidebarControls` focused on picker composition, read-only messaging, mutation ownership, URL error handling, and save callback wiring.
  - Builder mobile/sidebar drawer now splits sheet title chrome, active-configuration summary, and editable-fields guidance into builder-owned modules, leaving `BuilderSidebarDrawer` focused on URL-backed open state, mobile trigger behavior, sheet composition, and control wiring.
  - Builder publish modal footer actions now live in `PublishConfirmationActions`, leaving `PublishConfirmationDialog` focused on mutation, URL notice handling, form validation/submission, disabled branching, and composition.
  - Builder recommendation modal now splits dialog header copy, select-field chrome, updated-profile summary, mutation error text, and footer actions out of `RecommendTemplatePanel`, leaving the root modal focused on local state, mutation, refresh/close behavior, disabled trigger, and submit composition.
  - Builder publish modal splits disabled upgrade chrome, dialog header copy, publish summary formatting, current-live note rendering, and footer action chrome out of `PublishConfirmationDialog`.
  - Dashboard user menu now splits avatar trigger and dropdown content chrome out of `DashboardUserMenu`, leaving the root menu focused on initials, pending sign-out state, session clearing, and navigation refresh.
  - Notification topbar chrome now splits popover trigger, content, item rendering, and notification item type ownership out of `NotificationBell`, leaving the root bell component composition-only.
  - Dashboard nav no longer carries unused `AppRail`, `CurrentAppIndicator`, `DashboardSidebarSkeleton`, `DashboardTopbar`, or dead navigation helper exports; active chrome ownership is centered on `DashboardChrome`, `DashboardSidebar`, and direct `SiteNav.Header` composition.
  - Customer details sheet now splits title/status chrome, not-found chrome, and profile field rows/date formatting into customer-owned modules instead of keeping them inside the data-query component.
  - Customer edit sheet now splits title/action/delete-dialog chrome into `CustomerEditSheetHeader`, leaving the sheet component focused on URL state, data lookup, mutation invalidation, and form composition.
  - Top-level Blog, Leads, Appointments, Notifications, Leave Requests, Employees, Projects, and Billing headers now share the dashboard-owned `HeaderLinkTab` primitive instead of duplicating local header tab helper functions.
  - Live Preview now splits published-site header chrome, rendered website frame/section rendering, and unavailable-state chrome into Live-owned modules instead of keeping them inside the preview orchestration component.
  - Blog detail now splits title/status/action chrome, mutation error notice chrome, and edit-article section chrome into blog-owned modules instead of keeping them inside the query/mutation content module.
  - Dashboard Home now splits workspace overview header chrome and repeated section chrome into dashboard-home-owned modules instead of keeping them inside the main data/query module.
  - Billing Current Plan, Available Plans, Repair Payment, and Billing History sections now share a billing-owned section primitive instead of keeping section chrome inside the billing action/table module.
  - Analytics content sections now share an analytics-owned section primitive instead of keeping section chrome inside the analytics data-rendering module.
  - Reports Business Summary, Agent Performance, and Listings Performance sections now share a report-owned section primitive instead of keeping section chrome inside the data/export module.
  - Integrations overview now uses an integrations-owned section primitive instead of keeping section chrome inside the overview grid client module.
  - AI Credits top-up and usage sections now share an AI Credits-owned section primitive instead of keeping section chrome inside the mutation/table module.
  - Domains status and custom-domain connect surfaces now share a domain-owned section primitive instead of duplicating section wrappers across `domains-sections` and `connect-domain-view`.
  - Estate list and Estate detail sections now share an estate-owned section primitive with optional action-slot support instead of keeping duplicate local section wrappers in client modules.
  - Project detail, Budget, and Workforce subpages now share a project-owned section primitive; Budget and Workforce also share a project-owned subpage header for their title/description/back-navigation chrome instead of duplicating it inline.
  - Project and Estate detail pages now split their data-backed detail shells into feature-owned header and stats modules, leaving the client detail content modules focused on query orchestration, not-found handling, and the remaining section composition.
  - Integration Settings and Notification Preferences routes intentionally keep Midday's settings-subroute handoff shape: the route wraps the feature-owned settings-list component in a no-fallback `Suspense`, while the list wrapper owns the Card, ErrorBoundary, inner Suspense fallback, and skeleton around the query-backed settings body.
  - Analytics and Reports now render top-level route-owned headers outside their Suspense/data boundaries; their client content modules only own query-backed metric/report sections.
  - Template Sandbox create/clone navigation no longer emits write-only `created` / `cloned` query flags, and the create form now uses an explicit client submit handler instead of a form `action` callback.
  - Final onboarding completion now uses client-owned `onboarding.saveProgress` plus `onboarding.complete` mutations, then clears the pending-onboarding cookie through `/api/session/onboarding`; the retired broad `apps/dashboard/src/app/actions.ts` file has been deleted after scans found no active imports.
  - Join invite signup now uses `auth.signUpForInvite` through a route-local client mutation plus the existing session bridge instead of `signUpForInviteAction`.
  - Join invite profile completion for accepted agent/staff invites now uses `team.completeInviteProfile` through a route-local client mutation instead of `completeInviteProfileAction`; the unused action wrapper has been removed.
  - Join invite acceptance for already-signed-in users now uses a route-local client mutation against `team.acceptInvite` instead of `acceptInviteAction`.
  - Onboarding logo upload no longer calls `setPendingOnboardingLogoAction`; the authenticated upload route persists the pending logo cookie for pre-workspace onboarding uploads, and the retired logo action wrapper has been removed from `app/actions.ts`.
  - Template Sandbox index/workbench no longer import `@/app/actions`; sandbox create/clone/archive/content save/smart-fill/style shuffle/live generation now use direct `templateSandbox` client mutations with local invalidation/navigation, and the retired sandbox action wrappers have been removed from `app/actions.ts`.
  - Onboarding step-save forms now call `onboarding.saveProgress` through client mutations with local onboarding URL/error handling instead of `saveOnboardingStepAction`; final completion now uses the same mutation-owned onboarding boundary instead of a server action.
  - Builder workspace no longer imports Builder save/publish/draft actions from `app/actions.ts`; preview field saves, smart fill, template draft creation, theme updates, and publish run through feature-owned client mutations against the dedicated Website and Templates routers, and the retired Builder action wrappers have been removed.
  - Dashboard Home no longer imports `ensureBuilderConfigurationExists` from `app/actions.ts`; its route-level bootstrap now calls `workspace.ensureBuilderConfigurationExists` through a local server caller before prefetching the dashboard overview.
  - Dead list CSV server actions for Leads, Properties, Customers, Appointments, and Employees have been removed from `app/actions.ts`; active Reports exports remain client-side from hydrated `workspace.getReports` data.
  - Team, Agent, and Employee invite sheet submissions now use direct `team.inviteMember` client mutations with API-owned workspace invitation email dispatch, pending-invite invalidation, and sheet close instead of `inviteMemberAction` / `inviteAgentAction` / `inviteEmployeeAction`; the unused dashboard invite action wrappers and dashboard-local invite notification helper have been removed.
  - Estate create sheet submissions now use direct `estates.create` client mutations with API-owned company-scoped slug generation, estate-list invalidation, and sheet close instead of `createEstateAction`; the unused Estate create server action has been removed from `app/actions.ts`.
  - Property create/edit sheet submissions now use direct `workspace.createProperty` / `workspace.updateProperty` client mutations with list/detail invalidation and sheet close instead of `createPropertyAction` / `updatePropertyAction`; the unused Property form server actions and property create `returnTo` handoff have been removed from the active path.
  - Agent create/edit sheet submissions now use direct `workspace.createAgent` / `workspace.updateAgent` client mutations with list/detail invalidation and sheet close instead of `createAgentAction` / `updateAgentAction`; the unused Agent form server actions have been removed from `app/actions.ts`.
  - Payroll Entry create sheet submissions now use a new `workspace.createPayrollEntry` client mutation with active-company employee validation, payroll entry/summary/period invalidation, and sheet close instead of `createPayrollEntryAction`; the unused payroll entry create action has been removed from `app/actions.ts`.
  - Leave Request create sheet submissions now use a new `workspace.createLeaveRequest` client mutation with company-owned employee validation, leave request list/stat invalidation, and sheet close instead of `createLeaveRequestAction`; the unused leave request create action has been removed from `app/actions.ts`.
  - Department create sheet submissions now use a new `workspace.createDepartment` client mutation with department-list invalidation and sheet close instead of `createDepartmentAction`; the unused department create action has been removed from `app/actions.ts`.
  - Project create sheet submissions now use the existing `projects.create` client mutation with project list/stat invalidation and sheet close instead of `createProjectAction`; the unused project create action has been removed from `app/actions.ts`.
  - Appointment create sheet submissions now use the existing `workspace.createAppointment` client mutation with list/stat invalidation and sheet close instead of `createAppointmentAction`; the unused appointment create action and stale domain-sync shim have been removed from `app/actions.ts`.
  - App Store toggles now use the dedicated `apps.setEnabled` client mutation with admin/plan-gate validation and `router.refresh()` instead of route-local/global server actions; obsolete install-button action files have been removed.
  - Reports CSV exports now generate client-side from the hydrated `workspace.getReports` data instead of calling Reports-specific server actions; unused report export server actions have been removed from `app/actions.ts`.
  - Billing checkout now uses the existing `workspace.initializeCheckout` client mutation and provider URL navigation, while repair-payment actions navigate directly to the existing callback verifier; unused Billing checkout/repair server actions have been removed from `app/actions.ts`.
  - AI Credits top-up now uses the existing `workspace.purchaseAiCredits` client mutation with credit-info invalidation instead of a route-owned server-action form; the unused AI Credits purchase server action has been removed from `app/actions.ts`.
  - Domains sync, custom-domain connect, and custom-domain remove now use workspace tRPC client mutations with status/DNS-instruction invalidation instead of route-redirecting server-action forms; unused Domains server actions have been removed from `app/actions.ts`.
  - Settings logo save/remove now uses the existing `workspace.setCompanyLogo` client mutation with company-settings invalidation instead of a settings-owned server-action form; the unused company logo server action has been removed from `app/actions.ts`.
  - General Settings company profile updates now use `workspace.updateCompanyProfile` client mutations with company-settings invalidation instead of a settings-owned server-action form; the unused company profile server action has been removed from `app/actions.ts`.
  - Integration Settings now submits through the existing `workspace.updateCompanyIntegration` client mutation with integration-query invalidation instead of a settings-owned server-action form; the unused integration settings server action has been removed from `app/actions.ts`.
  - Notification Preferences channel toggles now use `notifications.updatePreference` client mutations with preference-list invalidation instead of settings-owned server-action forms; the unused notification preference server action has been removed from `app/actions.ts`.
  - Shared table core no longer exports the retired `BulkFormAction` / `BulkDeleteAction` server-action helpers; active table/header paths now scan clean for table-owned `@/app/actions` imports and form action bindings.
  - Leave Requests row approve/reject/cancel actions and bulk status updates now use workspace tRPC client mutations with list/stat invalidation instead of table-owned server-action forms; unused Leave Request status server actions have been removed from `app/actions.ts`.
  - Leads row status updates, lead conversion, and bulk status updates now use workspace tRPC client mutations with list/stat invalidation instead of table-owned server-action forms; unused Lead status/conversion server actions have been removed from `app/actions.ts`.
  - Properties row feature toggling and bulk delete now use workspace tRPC client mutations with list invalidation instead of table-owned server-action forms; unused Property bulk-delete/feature-toggle server actions have been removed from `app/actions.ts`.
  - Payroll row mark-paid and bulk mark-paid now use workspace tRPC client mutations with list/summary invalidation instead of table-owned server-action forms; unused Payroll mark-paid server actions have been removed from `app/actions.ts`.
  - Notifications header mark-all-read, row mark-read, and bulk mark-read now use notifications tRPC client mutations with list/unread-count invalidation instead of page/table-owned server-action forms.
  - Appointments row status/cancel/delete actions and bulk delete now use workspace tRPC client mutations with list/stat invalidation instead of table-owned server-action forms; unused Appointment status/delete server actions have been removed from `app/actions.ts`.
  - Agents row feature toggling and bulk delete now use workspace tRPC client mutations with list invalidation instead of table-owned server-action forms; unused Agent bulk-delete/feature-toggle server actions have been removed from `app/actions.ts`.
  - Departments row delete and bulk delete now use workspace tRPC client mutations with list invalidation instead of table-owned server-action forms; unused Department update/delete server actions have been removed from `app/actions.ts`.
  - Employees row status/delete actions and bulk delete now use workspace tRPC client mutations with list/stat invalidation instead of table-owned server-action forms; unused Employee create/update/delete server actions have been removed from `app/actions.ts`.
  - Team, Agent, and Employee pending invite revocation now uses local client `team.revokeInvite` mutations with list invalidation and inline errors instead of the retired shared `revokeInviteAction` server action.
  - Team bulk member removal and pending invite revocation now use client tRPC mutation boundaries with local invalidation/error behavior instead of route-redirecting server action forms; the active Team path no longer produces `/team?error=...` action redirects.
  - Team row role, suspend/reactivate, and remove actions now use table action-menu client mutations with member-list invalidation instead of route-redirecting server action forms.
  - Team, Agent, and Employee invite sheet submissions no longer redirect failures through route `?error` state; shared invite failures now return `{ error }` and render inside their respective invite forms, while success redirects remain plain.
  - Payroll Entry create sheet submissions no longer redirect failures through `/hr/payroll?error`; required-field, database, and employee-not-found errors now render inside `PayrollEntryForm`, while successful submissions still revalidate and preserve intentional `year` / `month` period state.
  - Leave Request create sheet submissions no longer redirect failures through `/hr/leave?error`; required-field, database, and employee-not-found errors now render inside `LeaveRequestForm`, while successful submissions still revalidate and return to plain `/hr/leave`.
  - Department create sheet submissions no longer redirect failures through `/hr/departments?error`; missing-name and database errors now render inside `DepartmentForm`, while successful submissions still revalidate and return to plain `/hr/departments`.
  - Project create sheet submissions no longer redirect failures through `/projects?error`; mutation errors now render inside `ProjectForm`, while successful submissions still revalidate and return to plain `/projects`.
  - Property row delete now follows the table action-menu mutation pattern with local pending state and list invalidation; the old route-redirecting single `deletePropertyAction` has been removed.
  - Agent row delete now follows the table action-menu mutation pattern with local pending state and list invalidation; the old route-redirecting `deleteAgentAction` has been removed.
  - Agent create/edit sheet submissions no longer redirect failures through `/agents?error`; mutation errors now render inside `AgentForm`, while successful submissions still revalidate and return to plain `/agents`.
  - App Store no longer owns unused `locked` route state or route-level locked-app banner chrome; the only remaining simple object route params in `(app)` are Reports period selection and Billing callback references.
  - Migrated table pages no longer receive dead `created=1` / `invited=1` success flags from team, customer, employee, department, leave, or payroll actions; payroll keeps only its intentional `year` / `month` period state.
  - Estate detail no longer owns route-level `error` query banners; property create/update failures now surface inside the property sheet form, and the retired property detail redirect no longer forwards obsolete error query state into the active list/sheet route.
  - Blog detail no longer owns `created` / `saved` query notice state; create navigates to plain `/blog/[id]`, and edit success remains local to the blog form.
  - Domain connect no longer owns route-level `error` query state; failed custom-domain provisioning returns a form-local action error, while success redirects to plain `/domains`.
  - Dashboard home no longer owns unused `domains` / `error` query banner state; `syncTenantDomainsAction` redirects to plain `/`, and the home route directly composes `HydrateClient` > `ScrollableContent` > boundary/suspense > `DashboardHome`.
  - Billing no longer owns route-level payment success/failure banner chrome; callback and repair redirects now return to plain `/billing`, the feature header is named `BillingHeader`, and `interval` remains as the monthly/annual plan-toggle URL state.
  - Estates index no longer owns query-string error banner chrome; estate create failures no longer redirect the index through `?error`, and the feature header is named `EstatesHeader`.
  - Integrations and AI Credits active headers now use feature names (`IntegrationsHeader`, `AiCreditsHeader`) instead of old `*PageHeader` suffixes; AI Credits route no longer wraps its suspense boundary in an extra route-level flex container.
  - Domains index no longer owns query-string success/error banner chrome; sync/connect/remove success redirects now return to plain `/domains`, and the feature header is named `DomainsHeader`.
  - Integration Settings now follows the same settings-list split: a wrapper owns the Card, ErrorBoundary, and Suspense, while the client settings module owns the prefetched integration query, form fields, submit action, and skeleton.
  - Notification Preferences now follows Midday's settings-list split: a server wrapper owns the Card, Card header/content, ErrorBoundary, and Suspense while the client settings module owns the prefetched notification preferences query, row rendering, and skeleton.
  - General Settings now composes independent settings cards directly in the route inside `HydrateClient`, and each card owns the prefetched company settings query instead of flowing through a `SettingsContent` aggregator/skeleton/unavailable-state wrapper.
  - The shared `SecondaryMenu` now matches Midday's settings/account menu styling for hidden scrollbars, inactive link color, and active underline classes while retaining PlotKeys' shared `cn` import.
  - Settings child routes no longer own query-string success/error banner chrome; General Settings and Integration Settings now redirect to plain settings routes after saves, matching Midday's route-level settings composition.
  - Settings modules now render as direct card-first modules inside the shared settings layout: redundant `SettingsSection` wrappers, settings shortcut cards, notification summary/info chrome, and integration/notification child section wrappers have been removed.
  - Notification Preferences and Integration Settings no longer render redundant child page headers or Back-to-settings buttons now that the settings secondary menu owns navigation.
  - Settings now has a Midday-style nested settings layout with a shared `SecondaryMenu`, one `ScrollableContent` owner, a constrained content column, and child settings pages that render their content directly.
  - Builder workspace/sidebar controls, Template Sandbox floating controls, and dev FAB shell now use the shared `cn` helper for conditional class composition instead of ad hoc class array joins.
  - Dashboard-owned Builder preview class composition now uses the shared `cn` helper instead of a local `joinClasses` helper, matching Midday dashboard helper conventions.
  - Table core and table-adjacent class composition now uses a Midday-shaped `@plotkeys/ui/cn` export path, while row action menus remain feature-owned after audit because their mutation/dropdown bodies are domain-specific and the virtual row already treats `actions` as non-clickable.
  - Shared domain filter composition now uses a neutral `SearchFilter` module and Midday-shaped `@plotkeys/ui/cn` import, removing the old `DashboardSearchFilter` public surface from Projects, Blog, Properties, Employees, Leave Requests, Appointments, and Leads.
  - Shared form layout chrome now uses neutral `FormBody` / `FormFooter` exports and the Midday-shaped `@plotkeys/ui/cn` import, removing the old `DashboardFormBody` / `DashboardFormFooter` public surface from migrated sheet/form modules.
  - Shared auth/menu/header-tab/pagination/payroll/onboarding/project row chrome now imports `cn` through the Midday-shaped `@plotkeys/ui/cn` path instead of the lower-level utils package when no domain utility constants are needed.
  - Builder, Template Sandbox, and dev FAB class-composition chrome now imports `cn` through the Midday-shaped `@plotkeys/ui/cn` path, with mixed files retaining only domain utility types on `@plotkeys/utils`.
  - Shared stacked sheet header chrome now follows Midday create/edit sheet anatomy with visible `h2` titles, optional muted description text, and direct row-centered close controls instead of `SheetTitle` / `SheetDescription` visible wrappers.
  - Active dashboard modal footers now avoid the retired `DialogClose` export; publish confirmation and recommendation template cancel actions close through modal-owned controlled state.
  - Builder publish confirmation failures now render inside the dialog instead of route-level `?error=` state, and the retired `published` builder notice query state has been removed after publish success stopped writing it.
  - Builder sidebar theme update failures now render inside the sidebar controls via local hook state instead of route-level `?error=` state, while silent background saves remain silent and successful saves still refresh the route.
  - Builder preview mobile menu chrome no longer uses custom `shadow-lg`; the remaining high-signal style scan hit is the Midday-matching selected-row `BottomBar` blur.
  - The unused project-local `components/projects/create-project-form.tsx` duplicate has been removed, leaving project creation owned by the active forms/sheets boundary.
  - Remaining dashboard-local route-relative imports across components/app/hooks/store have been swept into `@/` aliases, including auth, onboarding, settings logo upload, builder, template sandbox, project helper, and dev tenant FAB dependencies.
  - The unused `components/nav/dashboard-shell.tsx` compatibility wrapper has been removed, and active nav chrome imports now use `@/` aliases so authenticated dashboard shell ownership stays with `(app)/layout.tsx` and `DashboardChrome`.
  - Customers route now composes summary cards, `CustomersHeader`, and `DataTable` directly like Midday's customer route; the interim `components/tables/customers/index.tsx` wrapper has been removed.
  - Customers route shell now returns `HydrateClient` > `ScrollableContent` directly like Midday; the old `DashboardPage` wrapper and route-level customer success/error alerts have been removed from this page.
  - Customers now use a Midday-style singular `use-customer-filter-params.ts` file that owns both `useCustomerFilterParams` and `loadCustomerFilterParams`; the schema includes `q`, `sort`, `start`, `end`, and PlotKeys' status `filter`, and the old plural hook/split loader have been removed.
  - Customers now follow Midday's customer-specific table shape more closely: page-level `CustomerHeader`, a single sticky `name` column, row-click details, and action-menu-only row controls.
  - Customer row mutations now follow Midday's table-owned pattern: `CustomersDataTable` owns update/delete mutations and passes row action handlers through table meta to the column-owned `ActionsCell`.
  - Customer shared row/status types now live in feature-owned `components/customer/types.ts`, so sheets and table data modules no longer depend on table column ownership for domain row shape.
  - Customer sheets now follow Midday's customer-specific sheet split: `CustomerCreateSheet`, `CustomerDetailsSheet`, and `CustomerEditSheet` mount globally, with `customers.getById` powering details/edit defaults.
  - Customer create content, Agent create/edit/invite sheets, Appointment create sheet, and Property create/edit sheets now share `components/stacked-sheet-header.tsx` for stacked sheet title/description/close behavior, replacing duplicate domain-specific sheet-header modules.
  - Customer edit sheet keeps its action-bearing header, but its actions trigger now follows Midday's plain button pattern instead of using the styled shared `Button` primitive.
  - Department create, Employee invite, Estate create/edit launch, Leave Request create, Payroll Entry create, Project create, and Team invite sheets now also use `StackedSheetHeader`, leaving only action-bearing Customer edit and the Builder mobile drawer with custom sheet headers.
  - Customer create/edit sheets now use the shared Midday-style `SheetContent stack` surface, while detail/edit queries reuse cached infinite-list rows as placeholder data before `customers.getById` refreshes.
  - Customer table rows now use the Midday `VirtualRow.onCellClick` route-to-details interaction and the ported `useScrollHeader` hook for header/summary collapse behavior.
  - Customers table header now lives at the Midday reference path `components/tables/customers/table-header.tsx`; the interim `data-table-header.tsx` name has been retired for this page.
  - Customers route shell now uses Midday-style `ScrollableContent` and `CollapsibleSummary`, and the generic `DashboardTablePage`/table-local title wrapper has been removed from the Customers page.
  - Customers page header now uses `customers-header.tsx` / async `CustomersHeader`, a Midday-style `SearchField` for `q`, column visibility, and create-sheet controls only; the create trigger uses the shared UI icon namespace and the extra CSV export button has been removed from this Midday-parity header.
  - Customers loading fallback is now table-only like Midday's Customers skeleton; summary and header skeletons are no longer duplicated inside `components/tables/customers/skeleton.tsx`.
  - Customers empty states now use Midday's local `EmptyState` / `NoResults` export names and centered customer empty-state layout instead of the shared PlotKeys `DashboardEmptyState` wrapper; `NoResults` owns its clear behavior rather than receiving it from `DataTable`.
  - Customers summary is now split into independent Midday-style top-level summary card components with a shared `CustomerSummarySkeleton`, rather than a single aggregate dashboard stat-grid component.
  - Customers summary cards and `CustomerSummarySkeleton` now match Midday's card title typography class shape and explicit skeleton heights.
  - Properties route shell migration has started: `apps/dashboard/src/app/(app)/properties/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and URL error alert removed.
  - Properties page-level header now lives at `components/properties-header.tsx`, while `components/tables/properties/table-header.tsx` is reserved for the actual table header; the interim `data-table-header.tsx` name has been retired for Properties.
  - Properties table composition no longer uses the old `DashboardTablePage` / `DashboardTablePageBody` wrapper; `PropertiesTable` now returns the page header and table/empty state directly like the migrated Midday-style table pages.
  - Properties header now follows Midday's lean table-control row: `PropertiesSearchFilter` on the left, column visibility plus `OpenPropertySheet` on the right, with CSV export/view-site actions and route-level `siteUrl` plumbing removed from this table header path.
  - Properties list empty states now use Midday's local `EmptyState` / `NoResults` export names and centered layout instead of the shared `DashboardEmptyState` wrapper; `NoResults` owns clearing `usePropertiesFilterParams`.
  - Properties route now composes `PropertiesHeader` directly above `DataTable`, while `components/tables/properties/data-table.tsx` owns filter/sort/infinite-query state, empty/no-results decisions, and table settings like the Midday invoice table pattern; the old `components/tables/properties/index.tsx` wrapper has been removed.
  - Properties loading fallback now uses the Midday table skeleton row count of 25 rows.
  - Properties row actions now live in `components/tables/properties/actions-menu.tsx` and render as a compact icon dropdown in an 80px sticky actions column instead of a wide inline button strip.
  - Properties row click/details workflow now uses URL-owned `propertyId` / `details` state via `use-property-params.ts`; `PropertyDetailsSheet` is mounted globally and both row clicks and the action-menu "View listing" action open the details sheet instead of navigating from the list table to the old full-page detail route.
  - Properties create/edit workflow now uses URL-owned sheet state: `createProperty` opens `PropertyCreateSheet`, `propertyId` without `details` opens `PropertyEditSheet`, both use `SheetContent stack`, and `OpenPropertySheet`, list empty state, and row action menu all open sheets through `use-property-params.ts`.
  - Properties full-page detail route has been retired from active navigation: `/properties/[id]` now redirects to the list page with details sheet params, analytics/estate links use the same sheet-param URL, and the old `components/tables/properties/detail.tsx` / `detail-skeleton.tsx` modules have been removed.
  - Properties media management has started moving into the sheet path: `PropertyDetails` now lists property media and supports add-by-URL, set cover, and delete through the existing `propertyMedia` tRPC router with targeted query invalidation.
  - Properties sheets now follow a clearer Midday split: `PropertyDetailsSheet` delegates `SheetContent` ownership to `PropertyDetailsContent`, `PropertyDetailsHeader` owns detail title/status presentation, and the shared `StackedSheetHeader` is reused by create/edit sheets.
  - Properties URL filter state now follows Midday's combined hook/loader module pattern: `use-property-filter-params.ts` owns both `usePropertyFilterParams` and `loadPropertyFilterParams`, and the old split plural hook plus `lib/properties-filter-params.ts` have been removed from the active page.
  - Agents route shell migration has started: `apps/dashboard/src/app/(app)/agents/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and route-level invite/error alerts removed.
  - Agents table composition no longer uses the old `DashboardTablePage` / `DashboardTablePageBody` wrappers; the table path now returns its header and virtualized table directly for the next Midday ownership pass.
  - Agents header now uses top-level `components/agents-header.tsx` with the Midday-style search-left/actions-right control row; the old table-folder page/table header module has been removed, and the loading fallback now uses the shared table-only skeleton with 25 rows and sticky `agent` configuration.
  - Agents route now composes `AgentsHeader`, pending invites, and `DataTable` directly; the obsolete `components/tables/agents/index.tsx` wrapper has been removed.
  - Agents table query ownership now lives in `components/tables/agents/data-table.tsx`, including filter/sort/infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Agents table header now lives at the Midday reference path `components/tables/agents/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Agents URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-agent-filter-params.ts`, and the old split plural hook plus `lib/agents-filter-params.ts` have been removed from the active page.
  - Agents empty states now use local `EmptyState` / `NoResults`, and row actions now use a compact `actions-menu.tsx` dropdown with an icon-only actions column instead of wide inline action buttons.
  - Agents create/edit/invite sheet state now follows the global URL-owned pattern via `hooks/use-agent-params.ts`; `AgentCreateSheet`, `AgentEditSheet`, and `AgentInviteSheet` mount in `GlobalSheets`, with dedicated `OpenAgentSheet` / `OpenInviteAgentSheet` triggers.
  - Agents edit sheet now has a URL-loadable `workspace.getAgent` path backed by `getAgentForCompany`, while still using cached list rows as placeholder data.
  - The old local `AgentSheet` and `InviteAgentSheet` components have been removed from the active Agents path, and agent invite success now returns to `/agents` instead of the retired route-level invite alert query.
  - Agents pending invites no longer use the old `DashboardSection` primitives, and the shared Agents sheet header now uses the valid `Icon.Close` entry for its close action.
  - Appointments route shell migration has started: `apps/dashboard/src/app/(app)/appointments/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - Appointments now has a top-level `components/appointments-header.tsx` using the Midday-style search-left/actions-right control row while preserving appointment status/upcoming tabs, column visibility, and create-sheet controls; the header CSV export action has been removed.
  - Appointments table composition no longer uses `DashboardTablePage` / `DashboardTablePageBody`; the virtualized table renders directly and the loading fallback is now table-only with 25 rows.
  - Appointments table header now lives at the Midday reference path `components/tables/appointments/table-header.tsx`; the old table-folder page/table header module and interim `data-table-header.tsx` name have been retired for this page.
  - Appointments route now composes `AppointmentsHeader` directly above `DataTable`; the obsolete `components/tables/appointments/index.tsx` wrapper has been removed.
  - Appointments table query ownership now lives in `components/tables/appointments/data-table.tsx`, including filter/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Appointments stats and status/upcoming tab ownership now lives in `components/appointments-header.tsx`, while the route stays responsible only for prefetch/hydration and shell composition.
  - Appointments URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-appointment-filter-params.ts`, and the old split plural hook plus `lib/appointments-filter-params.ts` have been removed from the active page.
  - Appointments empty states now use local `EmptyState` / `NoResults`, and `NoResults` owns filter clearing instead of receiving a wrapper-level clear handler.
  - Appointments create sheet state now follows the global URL-owned pattern via `hooks/use-appointment-params.ts`; `AppointmentCreateSheet` mounts in `GlobalSheets`, and `OpenAppointmentSheet` replaces the old local sheet trigger.
  - Appointments row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of wide inline status/cancel buttons.
  - Leads route shell migration has started: `apps/dashboard/src/app/(app)/leads/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - Leads route now composes `LeadsHeader` directly above `DataTable`; the obsolete `components/tables/leads/index.tsx` wrapper has been removed.
  - Leads now has a top-level `components/leads-header.tsx` using the Midday-style search-left/actions-right control row while preserving lead status tabs and column visibility; the header CSV export action has been removed.
  - Leads table query ownership now lives in `components/tables/leads/data-table.tsx`, including filter/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Leads table header now lives at the Midday reference path `components/tables/leads/table-header.tsx`; the old table-folder page/table header module and interim `data-table-header.tsx` name have been retired for this page.
  - Leads URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-lead-filter-params.ts`, and the old split plural hook plus `lib/leads-filter-params.ts` have been removed from the active page.
  - Leads empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Leads row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of wide inline status/convert buttons.
  - Team route shell migration has started: `apps/dashboard/src/app/(app)/team/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and invite/error alert banners removed.
  - Team route now composes top-level `TeamHeader`, pending invites, and `DataTable` directly; the obsolete `components/tables/teams/index.tsx` wrapper has been removed.
  - Team table query ownership now lives in `components/tables/teams/data-table.tsx`, including filter/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Team table header now lives at the Midday reference path `components/tables/teams/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Team URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-team-filter-params.ts`, and the old split `lib/team-filter-params.ts` loader has been removed from the active page.
  - Team empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Team row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline role/action form strip.
  - Team pending invites no longer use the old `DashboardSection` primitives and now render as a direct page-level list below the header.
  - Team header now keeps the control group to search, column visibility, and invite-sheet controls; inline member/plan stats and the Billing upgrade navigation action have been removed from the header.
  - Departments route shell migration has started: `apps/dashboard/src/app/(app)/hr/departments/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and created/error alert banners removed.
  - Departments route now composes top-level `DepartmentsHeader` and `DataTable` directly; the obsolete `components/tables/departments/index.tsx` wrapper has been removed.
  - Departments header now keeps the control group to search, column visibility, and create-sheet controls; the extra Employees navigation action has been removed from the header.
  - Departments table query ownership now lives in `components/tables/departments/data-table.tsx`, including filter/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Departments table header now lives at the Midday reference path `components/tables/departments/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Departments URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-departments-filter-params.ts`, and the old split `lib/departments-filter-params.ts` loader has been removed from the active page.
  - Departments empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Departments row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline view/delete button strip.
  - Notifications route shell migration has started: `apps/dashboard/src/app/(app)/notifications/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - Notifications route now composes top-level `NotificationsHeader` and `DataTable` directly; the obsolete `components/tables/notifications/index.tsx` wrapper has been removed.
  - Notifications table query ownership now lives in `components/tables/notifications/data-table.tsx`, including unread/search/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Notifications table header now lives at the Midday reference path `components/tables/notifications/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Notifications URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-notifications-filter-params.ts`, and the old split `lib/notifications-filter-params.ts` loader has been removed from the active page.
  - Notifications empty states now use local `EmptyState` / `NoResults`, `NoResults` owns search clearing while unread-empty stays a table-local empty state, and the loading fallback is now table-only with 25 rows.
  - Notifications row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline view/mark-read button strip.
  - Notifications unread/all tabs, column visibility, search, and mark-all-read control now live in top-level `components/notifications-header.tsx`; unread/all tabs use the same bordered link-tab pattern as the other active list headers.
  - Leave Requests route shell migration has started: `apps/dashboard/src/app/(app)/hr/leave/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and created/error alert banners removed.
  - Leave Requests route now composes top-level `LeaveRequestsHeader` and `DataTable` directly; the obsolete `components/tables/leave-requests/index.tsx` wrapper has been removed.
  - Leave Requests table query ownership now lives in `components/tables/leave-requests/data-table.tsx`, including status/search/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Leave Requests table header now lives at the Midday reference path `components/tables/leave-requests/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Leave Requests URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-leave-requests-filter-params.ts`, and the old split `lib/leave-requests-filter-params.ts` loader has been removed from the active page.
  - Leave Requests empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Leave Requests row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline approve/reject/cancel button strip.
  - Leave Requests status tabs, column visibility, search, and create-sheet trigger now live in top-level `components/leave-requests-header.tsx`; extra Employees navigation and Button-tab chrome have been removed from the header.
  - Payroll route shell migration has started: `apps/dashboard/src/app/(app)/hr/payroll/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and created/error alert banners removed.
  - Payroll route now composes top-level `PayrollHeader`, `PayrollSummary`, and `DataTable` directly; the obsolete `components/tables/payroll/index.tsx` wrapper has been removed.
  - Payroll table query ownership now lives in `components/tables/payroll/data-table.tsx`, including period/search/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Payroll table header now lives at the Midday reference path `components/tables/payroll/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Payroll URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-payroll-filter-params.ts`, and the old split `lib/payroll-filter-params.ts` loader has been removed from the active page.
  - Payroll empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Payroll row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline mark-paid button strip.
  - Payroll period tabs, column visibility, search, and create-sheet trigger now live in top-level `components/payroll-header.tsx`; extra Employees navigation and Button-tab chrome have been removed from the header.
  - Payroll summary now owns its prefetched `workspace.getPayrollSummary` query in `components/tables/payroll/summary.tsx`, so the route remains a thin composition layer.
  - Blog route shell migration has started: `apps/dashboard/src/app/(app)/blog/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and error alert banner removed from the list page.
  - Blog route now composes top-level `BlogHeader`, `BlogSummary`, and `DataTable` directly; the obsolete `components/tables/blog/index.tsx` wrapper has been removed.
  - Blog table query ownership now lives in `components/tables/blog/data-table.tsx`, including status/search/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Blog table header now lives at the Midday reference path `components/tables/blog/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Blog URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-blog-filter-params.ts`, and the old split `lib/blog-filter-params.ts` loader has been removed from the active list page.
  - Blog empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Blog row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline edit button.
  - Blog status tabs, column visibility, search, and create-post trigger now live in top-level `components/blog-header.tsx`; Button-tab chrome has been replaced with bordered link tabs.
  - Blog summary now owns its prefetched `workspace.getBlogPostStats` query in `components/tables/blog/summary.tsx`, so the route remains a thin composition layer.
  - Employees route shell migration has started: `apps/dashboard/src/app/(app)/hr/employees/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and invite/error alert banners removed from the list page.
  - Employees route now composes top-level `EmployeesHeader`, pending employee invites, and `DataTable` directly; the obsolete `components/tables/employees/index.tsx` wrapper has been removed.
  - Employees table query ownership now lives in `components/tables/employees/data-table.tsx`, including department/status/search/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Employees table header now lives at the Midday reference path `components/tables/employees/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Employees URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-employees-filter-params.ts`, and the old split `lib/employees-filter-params.ts` loader has been removed from the active page.
  - Employees empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Employees row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline leave/reactivate/remove button strip.
  - Employees status tabs, column visibility, search, and invite-sheet trigger now live in top-level `components/employees-header.tsx`; header CSV export, extra total-stat copy, and Departments navigation have been removed.
  - Employees pending invites no longer use the old `DashboardSection` primitives and now render as a direct page-level list below the header.
  - Projects route shell migration has started: `apps/dashboard/src/app/(app)/projects/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and error alert banner removed from the overview page.
  - Projects overview route now composes top-level `ProjectsHeader` and `DataTable` directly; the obsolete `components/tables/projects/index.tsx` wrapper has been removed.
  - Projects overview table query ownership now lives in `components/tables/projects/data-table.tsx`, including status/search/sort interpretation, infinite-query state, empty/no-results selection, virtualization, DnD, sticky columns, and table settings.
  - Projects overview table header now lives at the Midday reference path `components/tables/projects/table-header.tsx`; the interim `data-table-header.tsx` file name has been retired for this page.
  - Projects URL filter state now follows the singular combined hook/loader module pattern via `hooks/use-projects-filter-params.ts`, and the old split `lib/projects-filter-params.ts` loader has been removed from the active overview page.
  - Projects overview empty states now use local `EmptyState` / `NoResults`, `NoResults` owns filter clearing, and the loading fallback is now table-only with 25 rows.
  - Projects overview row actions now use a compact `actions-menu.tsx` dropdown with an icon-only sticky actions column instead of a wide inline activate/view/delete button strip.
  - Projects status tabs, column visibility, search, and create-project sheet trigger now live in top-level `components/projects-header.tsx`; extra stats copy and report navigation have been removed from the control area.
  - Analytics route shell migration has started: `apps/dashboard/src/app/(app)/analytics/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - Analytics header and sections no longer depend on the shared `DashboardPageHeader` / `DashboardSection` primitives; `components/analytics/sections.tsx` now owns a local Midday-style header and section block pattern.
  - Analytics summary metrics now render through a local responsive grid instead of `DashboardStatGrid`, keeping the analytics component tree feature-owned.
  - Analytics empty state now uses a local centered empty state instead of `DashboardEmptyState`.
  - Analytics and other static/detail empty states now avoid decorative circular icon capsules, matching Midday's plain centered text/action empty-state pattern.
  - Analytics loading fallback now uses feature-local skeleton structure with stable skeleton keys and no `dashboard-page.tsx` imports.
  - Reports route shell migration has started: `apps/dashboard/src/app/(app)/reports/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - Reports header and sections no longer depend on the shared `DashboardPageHeader` / `DashboardSection` primitives; `components/reports/sections.tsx` now owns a local Midday-style header, period tabs, and section block pattern.
  - Reports empty state now uses a local centered empty state instead of `DashboardEmptyState`.
  - Reports loading fallback now uses feature-local skeleton structure with stable skeleton keys and no `dashboard-page.tsx` imports.
  - Reports period tabs and matching loading placeholders now use rectangular bordered tab treatment instead of pill-shaped primary/outlined chrome.
  - Billing route shell migration has started: `apps/dashboard/src/app/(app)/billing/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - Billing payment success/error notices now render as route-local Midday-style notice bands instead of shared alert banners.
  - Billing header no longer depends on the shared `DashboardPageHeader` / `DashboardFilterTabs` primitives; `components/tables/billing/table-header.tsx` now owns a local feature header and interval tabs.
  - Billing interval tabs now use rectangular bordered link-tab treatment instead of pill-shaped primary/outlined chrome.
  - Billing content sections no longer depend on the shared `DashboardSection` primitives; `components/tables/billing/table.tsx` now owns a local billing section pattern.
  - Billing history empty state now uses a local centered empty state instead of `DashboardEmptyState`.
  - Billing loading fallback now uses feature-local skeleton structure with stable skeleton keys and no `dashboard-page.tsx` imports.
  - AI Credits route shell migration has started: `apps/dashboard/src/app/(app)/ai-credits/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - AI Credits header no longer depends on the shared `DashboardPageHeader` primitives; `components/tables/ai-credits/table-header.tsx` now owns a local feature header.
  - AI Credits summary no longer depends on `DashboardStatGrid`; `components/tables/ai-credits/summary.tsx` now owns a local responsive metric grid.
  - AI Credits content sections no longer depend on the shared `DashboardSection` primitives; `components/tables/ai-credits/table.tsx` now owns a local AI credits section pattern.
  - AI Credits usage empty state now uses a local centered empty state instead of `DashboardEmptyState`.
  - AI Credits loading fallback now uses feature-local skeleton structure with stable skeleton keys and no `dashboard-page.tsx` imports.
  - Domains route shell migration has started: `apps/dashboard/src/app/(app)/domains/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and shared alert banners removed.
  - Domains connect route now uses `ScrollableContent` directly and no longer wraps `ConnectDomainView` in the old `DashboardPage` primitive.
  - Domains header no longer depends on shared `DashboardPageHeader` or toolbar primitives; `components/tables/domains/table-header.tsx` now owns the local feature header.
  - Domains table content no longer depends on shared `DashboardSection` primitives; `components/tables/domains/table.tsx` now owns the local `DomainSection` pattern.
  - Connect Domain view no longer depends on shared `DashboardPageHeader`, `DashboardSection`, or `Alert` primitives; it owns its local header, notice band, and section blocks.
  - Domains empty and loading states now use local centered/skeleton surfaces with stable skeleton keys and no `dashboard-page.tsx` imports.
  - Integrations route shell migration has started: `apps/dashboard/src/app/(app)/integrations/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper removed.
  - Integration Settings route now uses `HydrateClient` > `ScrollableContent`, with route-local notice bands replacing shared alert banners.
  - Integrations overview header no longer depends on shared `DashboardPageHeader` or toolbar primitives; `components/tables/integrations/table-header.tsx` owns the local feature header.
  - Integrations overview and settings content no longer depend on shared `DashboardSection` primitives; `components/tables/integrations/table.tsx` and `settings.tsx` own local section patterns.
  - Integrations empty and loading states now use local centered/skeleton surfaces with stable skeleton keys and no `dashboard-page.tsx` imports.
  - Settings route shell migration has started: `apps/dashboard/src/app/(app)/settings/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with route-local notice bands replacing shared alert banners.
  - Notification Preferences route now uses `HydrateClient` > `ScrollableContent`, with the old route-level `DashboardPage` wrapper and shared saved alert removed.
  - Settings and Notification Preferences headers no longer depend on shared `DashboardPageHeader` primitives; their table-header modules own local feature headers.
  - Settings cards and Notification Preferences event routing no longer depend on shared `DashboardSection` primitives; their table modules own local section patterns.
  - Notification Preferences summary no longer depends on `DashboardStatGrid`; it owns a local responsive metric grid.
  - Settings and Notification Preferences empty/loading states now use local centered/skeleton surfaces with stable skeleton keys and no `dashboard-page.tsx` imports.
  - Dashboard Home route shell migration has started: `apps/dashboard/src/app/(app)/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and shared alert banner removed.
  - Dashboard Home header no longer depends on shared `DashboardPageHeader` primitives; `components/dashboard/home/index.tsx` owns its local feature header.
  - Dashboard Home publishing, quick action, and connected-domain blocks no longer depend on shared `DashboardSection`; the home module owns a local section pattern.
  - Dashboard Home summary metrics no longer depend on `DashboardStatGrid` or `DashboardStatCard`; the home module owns a local responsive metric grid and stat-card link surface.
  - Dashboard Home loading fallback now uses local skeleton surfaces with stable keys and no `dashboard-page.tsx` imports.
  - Estates list route shell migration has started: `apps/dashboard/src/app/(app)/estates/page.tsx` now uses `HydrateClient` > `ScrollableContent` directly, with the old route-level `DashboardPage` wrapper and shared alert banner removed.
  - Estate detail route shell now uses `HydrateClient` > `ScrollableContent`, with the old route-level `DashboardPage` wrapper and shared alert banner removed.
  - Estates list header no longer depends on shared `DashboardPageHeader` primitives; `components/tables/estates/table-header.tsx` owns the local feature header.
  - Estates list content no longer depends on shared `DashboardSection`; `components/tables/estates/table.tsx` owns a local section pattern.
  - Estates list summary no longer depends on `DashboardStatGrid`; `components/tables/estates/summary.tsx` owns a local responsive metric grid.
  - Estates list empty/loading states and the estate detail loading fallback now use local centered/skeleton surfaces with stable keys and no `dashboard-page.tsx` imports.
  - Estate detail body no longer depends on shared `DashboardPageHeader`, `DashboardSection`, `DashboardStatGrid`, or `DashboardEmptyState`; `components/tables/estates/detail.tsx` owns its local header, sections, metrics, and not-found state.
  - Live Preview no longer depends on shared `DashboardPage`, `DashboardPageHeader`, `DashboardSection`, or `DashboardEmptyState`; `apps/dashboard/src/app/(app)/live/page.tsx` now owns the `ScrollableContent` shell while `components/live/live-preview.tsx` owns only unavailable and published-preview content surfaces.
  - Project Detail route shell now uses `HydrateClient` > `ScrollableContent`, with the old route-level `DashboardPage` wrapper removed.
  - Project Detail body no longer depends on shared `DashboardPageHeader`, `DashboardSection`, `DashboardStatGrid`, or `DashboardEmptyState`; `components/projects/project-detail-content.tsx` owns its local header, sections, metrics, and not-found state.
  - Project Detail loading fallback now uses local skeleton surfaces with stable keys and no `dashboard-page.tsx` imports.
  - Project Budget route shell now uses `HydrateClient` > `ScrollableContent`, with the old route-level `DashboardPage` wrapper removed.
  - Project Budget body no longer depends on shared `DashboardPageHeader`, `DashboardSection`, `DashboardStatGrid`, `DashboardTablePage`, or shared alert primitives; `components/projects/project-budget-content.tsx` owns local header, sections, table shell, metric grid, and delete-error notice.
  - Project Budget not-found and loading states use local centered/skeleton surfaces with stable keys and no `dashboard-page.tsx` imports.
  - Project Workforce route shell now uses `HydrateClient` > `ScrollableContent`, with the old route-level `DashboardPage` wrapper removed.
  - Project Workforce body no longer depends on shared `DashboardPageHeader`, `DashboardSection`, `DashboardTablePage`, or shared alert primitives; `components/projects/project-workforce-content.tsx` owns local header, worker/payroll table shells, form sections, and mutation-error notice bands.
  - Project Workforce not-found and loading states use local centered/skeleton surfaces with stable keys and no `dashboard-page.tsx` imports.
  - Payroll and Blog summary modules no longer depend on shared `DashboardStatGrid`; their feature-owned summary components now render local responsive metric grids.
  - Blog Detail route shell now uses `HydrateClient` > `ScrollableContent`, with the old route-level `DashboardPage` wrapper removed.
  - Blog Detail body no longer depends on shared `DashboardPageHeader`, `DashboardSection`, `DashboardEmptyState`, or shared alert primitives; `components/tables/blog/detail.tsx` owns its local header, notices, edit section, and not-found state.
  - Blog Detail loading fallback now uses local skeleton surfaces with stable keys and no `dashboard-page.tsx` imports.
  - App Store route shell now uses `ScrollableContent`, with the old route-level `DashboardPage` wrapper removed.
  - App Store view no longer depends on shared `DashboardPageHeader`, `DashboardPageToolbar`, or `DashboardSection`; `components/app-store/app-store-view.tsx` owns its local header, enabled-count toolbar row, plan-gated app cards, and category sections.
  - Template Sandbox Profiles route shell now uses `HydrateClient` > `ScrollableContent`, with the old route-level `DashboardPage` wrapper removed.
  - Template Sandbox index no longer depends on shared `DashboardPageHeader` or `DashboardSection`; `components/template-sandbox/template-sandbox-index.tsx` owns its local header and generated-profile section.
  - Template Sandbox Profiles loading fallback now uses local skeleton surfaces with stable keys and no `dashboard-page.tsx` imports.
  - Retired the old shared dashboard primitive modules `components/dashboard/dashboard-page.tsx` and `components/dashboard/dashboard-empty-state.tsx` after verifying no dashboard app source imports or symbol references remain.
  - Dashboard shell now follows the Midday 70px sidebar/header baseline more closely: `DashboardChrome` uses `md:ml-[70px]`, removes the decorative radial background layer, and lets the header remain a utility/action surface instead of a page-title/quick-link strip.
  - Dashboard sidebar and shared `site-nav` sidebar now use the Midday-style 70px collapsed rail and 240px hover-expanded width, with child links remaining in the sidebar menu instead of the topbar.
  - Shared `site-nav` nav items and child items now use flatter Midday-like dimensions and borders, replacing the earlier rounded card-like active states.
  - Sheet surfaces no longer depend on `DashboardSheetHeader`; ordinary stack-mode create/invite/edit sheets now share `StackedSheetHeader`, and `components/sheets/dashboard-sheet-layout.tsx` has been retired.
  - Departments create flow now follows the URL-owned global sheet pattern via `use-department-params.ts`, `OpenDepartmentSheet`, and `DepartmentCreateSheet`; `DepartmentsHeader` no longer mounts the old local `DepartmentSheet`.
  - Team member invite flow now follows the URL-owned global sheet pattern via `use-team-params.ts`, `OpenInviteMemberSheet`, and `TeamInviteSheet`; the old local `components/sheets/invite-member-sheet.tsx` module has been removed.
  - Employee invite flow now follows the URL-owned global sheet pattern via `use-employee-params.ts`, `OpenInviteEmployeeSheet`, and `EmployeeInviteSheet`; the old local `components/sheets/invite-employee-sheet.tsx` module has been removed.
  - Leave request creation now follows the URL-owned global sheet pattern via `use-leave-request-params.ts`, `OpenLeaveRequestSheet`, and `LeaveRequestCreateSheet`; the active employee lookup is enabled only while the global sheet is open.
  - Projects create flow now follows the URL-owned global sheet pattern via `use-project-params.ts`, `OpenProjectSheet`, and `ProjectCreateSheet`; both `ProjectsHeader` and the Projects empty state use the same URL-param open button.
  - Payroll entry creation now follows the URL-owned global sheet pattern via `use-payroll-params.ts`, `OpenPayrollEntrySheet`, and `PayrollEntryCreateSheet`; the active payroll period is derived from the current URL and active employees load only while the global sheet is open.
  - Estate create and estate launch edit flows now follow URL-owned sheet state via `use-estate-params.ts`, `OpenEstateCreateSheet`, `OpenEstateLaunchDetailsSheet`, `EstateCreateSheet`, and `EstateLaunchDetailsSheet`; launch editing loads `estates.get` by `estateSlug` while open.
  - Estate detail land-listing creation now uses the existing global Property create sheet with URL-backed defaults for `estateId`, `propertyLocation`, `propertyType`, and `returnTo`; the old local `components/sheets/property-sheet.tsx` module has been removed.
  - Builder mobile settings drawer now uses URL-owned `builderSettings` state via `use-builder-params.ts` instead of `SheetTrigger`, removing the last direct sheet-trigger primitive from `components/sheets`.
  - Estate detail and builder drawer touched surfaces have been flattened away from translucent `bg-card/*` surfaces, exaggerated tracking utilities, and card-like muted header blocks toward simpler Midday-style token surfaces.
  - Project detail, budget, workforce, and related AI/skeleton surfaces have been flattened away from translucent `bg-card/82`, oversized `rounded-[1.25rem]` sections, muted translucent panels, and exaggerated tracking utilities toward simpler Midday-style token surfaces.
  - Project detail, budget, workforce, AI insight result panels, and their skeleton frames now rely on default Card or plain `border-border bg-background` surfaces instead of explicit softened border/card background overrides.
  - Project budget, worker, and payroll compatibility shims now re-export list/summary components from feature-owned project content modules instead of retired `components/tables/projects/*` modules.
  - Project detail now imports budget, worker, payroll, and workforce form/list components directly from their feature-owned modules, and the old `project-budget.tsx`, `project-workers.tsx`, `project-payroll.tsx`, and `project-workforce.tsx` compatibility shims have been removed.
  - Settings and Notification Preferences surfaces have been flattened away from translucent card backgrounds, oversized skeleton containers, rounded calculated event rows, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Settings profile, workspace, branding, shortcut, danger, and skeleton surfaces now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Notification Preferences info copy now renders as a plain text block instead of a framed card with a decorative icon bubble or migration-facing copy.
  - Notification Preferences event-routing card, rows, channel toggles, and skeleton frames now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides; event type labels now render as muted text instead of decorative badge chips.
  - AI Credits and Billing surfaces have been flattened away from translucent card backgrounds, oversized table/skeleton shells, muted translucent row hovers, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Billing and AI Credits current/action cards, table frames, header rows, and skeletons now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Billing history rows, AI Credits usage rows, Domains DNS instruction rows, and provisioned-domain rows now use plain `border-border` row treatment instead of softened row borders.
  - Domains, Integrations, Integration Settings, and Dashboard Home surfaces have been flattened away from translucent card backgrounds, oversized table/skeleton/stat shells, muted translucent row hovers, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Domains and Integrations overview cards, table frames, and skeletons now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Connect Domain and Integration Settings setup cards, icon treatments, and skeleton frames now rely on default Card or plain token surfaces instead of softened border/card background overrides and decorative icon bubbles.
  - Dashboard Home publishing, quick-action, connected-domain, stat-link, primary URL, and skeleton surfaces now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Analytics and Reports surfaces have been flattened away from translucent card backgrounds, oversized skeleton/card shells, rounded translucent row panels, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Analytics metric, chart, ranked-list, share, agent-performance, event-type, recent-events, and skeleton surfaces now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Reports business summary, agent performance, listings performance, summary metric cells, and skeleton frames now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Payroll summary, Blog summary/detail, Estates summary/cards/detail panels, and related skeleton surfaces have been flattened away from translucent card/background treatments, oversized rounded skeleton shells, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Estate launch cards plus Estate list/detail skeleton frames now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Estate detail body cards, offer-card payment panels, inventory table frame, launch brief panels, plan import cards, and purchase pipeline cards now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Payroll, Blog, Estates, AI Credits, and Notification Preferences summary cards now rely on default Card surfaces instead of explicit `bg-card` / softened border classes; AI Credits and Notification Preferences summary cards no longer use decorative icon capsules.
  - App Store, Template Sandbox index/rail/skeleton, and Connect Domain surfaces have been flattened away from translucent card backgrounds, oversized skeleton shells, decorative rail blur, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - App Store app cards and Template Sandbox index/skeleton surfaces now rely on default Card or plain token surfaces instead of explicit softened border/card background overrides.
  - Template Sandbox floating configuration rail labels now use plain Midday-style text instead of uppercase chrome, and the rail uses the standard `rounded-lg` token instead of an arbitrary radius.
  - Template Sandbox floating configuration rail now relies on plain `border-border` / `bg-background` / foreground-muted token surfaces across the shell, row fields, section labels, dividers, select popovers, swatches, toggles, and secondary actions instead of dark custom `zinc` and `white/*` chrome.
  - Live Preview, publish/recommend modals, and Project Budget metric cells have been flattened away from translucent card/background treatments, oversized preview framing, decorative blur, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Live Preview published-site frame and Blog detail edit/skeleton surfaces now use plain/default token surfaces instead of explicit card background, softened border, or custom shadow overrides.
  - Builder workspace, floating config panel, template preview, and builder sidebar control dropdown surfaces have been flattened away from translucent card/popover backgrounds, oversized rounded chrome, decorative blur, muted translucent panels, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Builder floating config panel and its corner restore button now avoid custom `shadow-soft` chrome, and the restore control uses the standard `rounded-lg` token instead of circular FAB styling.
  - Builder preview panel shell, preview chrome, inline edit hover affordances, field editor popover, and preview footer labels have been flattened away from translucent backgrounds, oversized rounded preview shells, decorative blur, and uppercase tracking labels toward simpler Midday-style token surfaces.
  - Builder preview focused field editor now uses the plain border/background surface without custom `shadow-card` chrome, and the preview page switcher now uses rectangular bordered tabs instead of pill-shaped soft-primary styling.
  - Builder sidebar drawer and publish/recommend template modals have been flattened away from card-colored sheet/dialog chrome, decorative header badges, `border-border/70`, modal emoji triggers, and remaining decorative backdrop/gradient/tracking utilities toward plain Midday-style token surfaces.
  - Publish confirmation and template recommendation dialogs now rely on the shared `DialogContent` default `border bg-background` surface instead of restating those tokens locally.
  - Builder template preview swatches, sidebar swatches, inline preview hover labels, and preview chrome dividers now use plain `border-border` token treatment instead of softened opacity borders or shadowed hover pills.
  - Dashboard topbar, shared form footer, Dashboard Home icon/skeleton surfaces, Connect Domain/Integration icon capsules, AI Credits/Notification Preferences icon/status capsules, and Builder preview nav hover states have been flattened away from translucent background tokens, decorative form-footer blur, and oversized icon radii toward simpler Midday-style token surfaces.
  - Shared dashboard form footer, dashboard sidebar skeleton, and FlowShell brand pill now use plain `border-border` / `bg-background` token treatment instead of softened dividers or card-colored brand chrome.
  - Sign-in helper notice, onboarding brand avatar/edit affordance, Agents pending-invites wrapper, and dev FAB panel now use plain Midday token surfaces instead of softened borders, card-colored backgrounds, or decorative shadows.
  - The dev FAB shell trigger and panel now avoid amber warning chrome, circular/shadow FAB styling, emoji/glyph controls, and translucent card treatment while preserving dev-only behavior.
  - Sign-in now follows the Midday public login reference more closely by keeping the login form in an unframed text/form column, removing the `Badge`/`Card` shell, pill/shadow logo link chrome, card-colored helper rows, and custom `shadow-card` frame.
  - FlowShell auth routes now avoid the remaining pill brand wrapper and tinted side-panel mini cards: signup and verify-email side panels render as plain divider-list rows while keeping form, redirect, and verification behavior intact.
  - The remaining selected-row `BottomBar` blur layer is intentionally retained because it matches Midday's invoices/vault/core bottom-bar implementation.
  - Dashboard topbar now follows Midday's global utility-header shape more closely: page-title, quick-link, company, role, and user badge chrome have been removed from the shell, while account context and sign-out live behind a compact avatar user menu.
  - Auth, tenant signup, verify-email, onboarding flow shell, onboarding side panels, onboarding brand avatar, and notification demo surfaces have been flattened away from uppercase tracking labels, translucent backgrounds, muted translucent panels, and decorative blur toward simpler Midday-style token surfaces.
  - Builder template preview controls/browser bar, subdomain preview card, settings logo upload placeholders, and the dev FAB shell have been flattened away from muted translucent backgrounds, translucent dev-card chrome, decorative blur, and uppercase/tracking label treatments toward simpler Midday-style token surfaces.
  - Builder workspace, standalone template preview, preview panel, floating config rail, and builder dropdown surfaces no longer expose remaining decorative `Studio` badge chrome, translucent/card-colored builder frames, oversized builder radii, muted preview bodies, softened border-opacity controls, or builder chrome-only pill labels.
  - Builder sidebar and standalone template preview dropdown contents now rely on shared dropdown content surface defaults for border/background/shadow/radius, keeping only width, height, overflow, and padding overrides locally.
  - Domains, Billing, Integrations, and Live Preview page headers now present header status/count copy as plain text instead of top-level badge chrome, keeping badges reserved for content/table/card state.
  - Remaining opaque table-folder `index.tsx` composition wrappers for AI Credits, Billing, Domains, Estates, Integrations, Notification Preferences, and Settings have been retired; their routes now import explicit `content.tsx` modules with domain-specific `*Content` component names.
  - Page-level detail/settings composition owners for Blog detail, Estate detail, Integration Settings, Project detail, Project Budget, and Project Workforce now use `*Content` names instead of `*Table`; real data-table modules continue using `DataTable`.
  - Project detail, Project Budget, and Project Workforce page-level content, skeleton, row/helper, and empty-state owners now live under `components/projects`; `components/tables/projects` is reserved for the Projects overview data table modules.
  - Blog detail, Estate detail, and Integration Settings page-level content/skeleton owners now live under feature folders instead of `components/tables/*`; the integration catalog also moved to `components/integrations` so table modules consume it from a feature boundary.
  - AI Credits, Billing, Domains, Estates, Integrations, Notification Preferences, and Settings page-level `content.tsx` wrappers now live under feature folders; `components/tables/*` no longer contains generic `content.tsx` page composition wrappers.
  - Page-level headers and header-owned search/create controls now follow the Midday feature-boundary pattern: non-table page headers moved out of table folders, active search filters moved to top-level feature modules, Blog create moved to `components/blog-create-button.tsx`, and table-folder `table-header.tsx` files are reserved for `DataTableHeader`.
  - Feature summaries, invite strips, static report tables, and non-data-table section modules have moved out of `components/tables/*`; table folders are now reserved for table-specific columns, data-table modules, `DataTableHeader`, skeletons, empty states, and row actions.
  - Feature-level skeletons, empty/info cards, helper cells, and constants for AI Credits, Billing, Domains, Estates, Integrations, Notification Preferences, and Settings have moved out of table folders; `components/tables` now represents true list-table domains plus core table utilities.
  - Retired stale table settings/config entries for the old Estates table after the active Estates launch page became feature-owned card/list composition rather than a data-table page.
  - Active column visibility controls now match Midday's feature-level store-backed pattern and use the Midday `max-h-[450px]` popover body height.
  - Active true list tables now expose the Midday-style sticky select column, store-backed row selection, select-all header control, and animated selected-row bottom bar with deselect-all behavior; all active selected-row bars now expose domain actions instead of empty action slots and clear selection after bulk form submission.
  - Active true list table sticky metadata now keeps `select` at `md:left-0` and offsets first data columns to Midday's `md:left-[50px]`.
  - Active true list table Suspense skeletons now use the shared Midday-style `TableSkeleton` with 25 rows and sticky `select` plus first-domain-column metadata across Agents, Appointments, Blog, Customers, Departments, Employees, Leads, Leave Requests, Notifications, Payroll, Projects, Properties, and Team.
  - Active true list table empty/no-results states now follow Midday's local centered text/action pattern without decorative icon capsules, compact `min-h-72` wrappers, or "Clear search" copy; Blog empty state now imports the feature-level create button rather than a table-local create path.
  - Reports static tables and Project Budget/Workforce subtables now use plain `border-border` and `hover:bg-muted` row/cell surfaces instead of softened border and muted-hover opacity variants.
  - Blog, Leave Requests, Payroll, and Notifications list headers now keep the first header row focused on search plus column/action controls, without extra count/date badge chrome beside the controls.
  - Invite, department, and leave forms touched by the URL-owned sheet migration now expose `onSuccess` callbacks so global sheets can clear their URL params after successful non-redirecting submissions.
  - Builder side panels and drawer headers no longer use decorative linear/radial gradient backgrounds in the migrated sheet/builder surfaces.
  - Builder mobile settings drawer now follows the same plain sheet treatment as the larger builder chrome: configuration status and editable-field counts render as muted text instead of badge chrome.
  - Template recommendation modal result state now uses a plain bordered text summary instead of a tinted card with badge chips.
  - Dashboard/auth/onboarding surface scan no longer reports old dashboard/sheet helper symbols, decorative linear/radial background utility classes, or negative tracking utility classes under `apps/dashboard/src/app` and `apps/dashboard/src/components`.
  - Re-anchored row selection to the active Midday invoices/vault table-selection pattern across true list tables instead of leaving Customers as a one-off selection experiment.
  - Reuse the new list-query helper for upcoming dashboard migrations before adding page-specific pagination logic.
  - Update `.brain/progress.md` after each page migration.
- Created Date: 2026-05-22

### Apply Pending Prisma Migrations
- Priority: High
- Description: Run Prisma generate and apply the pending migrations for modular dashboard apps and customer offers so completed code paths are backed by the local/database schema.
- Related Feature: Modular Dashboard Sidebar; Customer Portal Phase 1C
- Status: In Progress
- Created Date: 2026-05-21
# Cross-product QA email and cleanup

- Product implementation is integrated. Schema rollout, secret propagation,
  delivery canaries, and first reviewed purge remain deployment work.

## Dashboard Midday Conformance F6

- Status: In Progress
- The onboarding flow now owns five Midday-style step modules plus a shared
  navigation/save contract; the former multi-step form bundle is removed.
- App Store state and enablement mutations moved from the monolithic
  `workspace` router to dedicated `apps.get` and `apps.setEnabled` procedures.
- Dashboard App Store server queries, client mutations, and exact invalidation
  keys now use the dedicated router.
- Reports and Analytics moved to dedicated `reports.get` and `analytics.get`
  procedures; report-period input now lives in a domain schema.
- AI Credits reads and top-ups moved to dedicated `aiCredits.get` and
  `aiCredits.purchase` procedures.
- Integrations reads and admin-gated updates moved to dedicated
  `integrations.get` and `integrations.update` procedures with a domain input
  schema.
- The custom-domain lifecycle moved to dedicated `domains.status`,
  `domains.dnsInstructions`, `domains.search`, `domains.connect`,
  `domains.sync`, and `domains.remove` procedures with domain-owned schemas.
- Workspace profile and logo settings now follow Midday's `team.current` and
  `team.update` contract instead of separate `workspace` procedures.
- Appointments list, stats, create, status, single-delete, and bulk-delete
  contracts moved to the dedicated `appointments` router with domain schemas.
- Appointment update/delete DB helpers now include the active company
  predicate, closing the former cross-tenant mutation gap.
- Leads list, stats, status, bulk-status, and customer-conversion contracts
  moved to the dedicated `leads` router with domain-owned schemas.
- Unsafe id-only lead status writes were removed; single, bulk, and conversion
  updates now include the active company predicate.
- Agents list, detail, create, update, featured-toggle, single-delete, and
  bulk-delete contracts moved to the dedicated `agents` router with
  domain-owned schemas.
- Agent update, featured-toggle, and delete helpers now return explicit
  company-scoped outcomes instead of raw Prisma not-found behavior.
- Properties list, detail/analytics, create, update, featured-toggle,
  single-delete, and bulk-delete contracts moved to the dedicated
  `properties` router with domain-owned schemas.
- Property update, featured-toggle, and delete helpers now return explicit
  company-scoped outcomes, and estate-linked creates/updates validate the
  estate against the active company before writing.
- Employees, Departments, Leave Requests, and Payroll list/stat/create/status,
  paid-state, single-action, and bulk-action contracts moved to dedicated
  domain routers and API schemas.
- Employee, department, and payroll writes now return explicit company-scoped
  outcomes; leave and payroll creation continue to validate the selected
  employee against the active company.
- Blog list, stats, detail, create, update, status, single-delete, and
  bulk-delete contracts moved to the dedicated `blog` router with domain-owned
  schemas.
- Blog slug uniqueness and writes are company scoped, missing write targets
  return explicit outcomes, and bulk deletion is one deduplicated API
  operation rather than sequential client mutations.
- Estate launch, detail, create, update, delete, layout-version, and plot
  lifecycle contracts moved to the dedicated `estates` router with
  domain-owned schemas.
- Estate and plot writes now return explicit company-scoped outcomes; plot
  creation validates the estate against the active company, and update slug
  uniqueness excludes the current estate.
- Onboarding state, progress, recommendations, profile refresh/editing, and
  completion moved to the dedicated `onboarding` router with domain-owned
  schemas.
- Onboarding completion preserves the full tenant bundle transaction,
  starter-template access, free license grant, signup notification, and
  optional domain-sync trigger; the API notification adapter is now shared
  instead of embedded in a feature router.
- Dashboard Overview moved from `workspace.getDashboardOverview` to Midday's
  exact `overview.summary` contract. Counts, domain state, and published
  website-version state are company scoped, while provisioning capability is
  derived server-side.
- Website builder/live-preview, draft lifecycle, content/theme editing, AI
  generation, publishing, and public rendering moved to the dedicated
  `website` router and domain schema. Catalog/licensing moved to `templates`;
  stock-image licensing/purchase moved to `stockImages`.
- The generic Workspace router/schema and manual provider-bypassing
  `changePlan` mutation were removed. Billing remains the provider-verified
  subscription boundary.
- Focused router/query verification passes 126 tests with 334 assertions
  covering membership, tenant scope, role and plan gates, report aggregates,
  AI credit/integration writes, hostname normalization, paired-domain
  creation, DNS instructions, removal, provisioning guards, team settings,
  appointment mutations, lead conversion/status workflows, and agent
  property, employee, department, leave-request, payroll, blog, estate,
  layout-version, plot, onboarding, dashboard-overview, website, template
  entitlement, and stock-image purchase workflows.
- F6 API conformance is complete with zero generic Workspace namespace residue.
  F7 package checks and authenticated browser QA remain.
